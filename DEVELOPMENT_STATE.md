---
project: TransRealm
file_role: agent-entrypoint-and-runtime-checkpoint
protocol_version: 2
current_phase: Phase 1
current_release: V1.0
current_task: P1-T05
current_milestone: P1-T05-M02
state: ready
last_updated: 2026-08-08
baseline_commit: 3ef991d
baseline_integrity: committed_and_verified
worktree_disposition: baseline_committed_not_pushed
gate_status: open
plan_alignment: pending_reality_check
review_status: P1-T05-M01_independent_review_resolved
rollback_ref: 3ef991d
pending_decision: none
decision_prompt: DEVELOPMENT_STATE.md#pending-decision
decision_result: DECIDED方案2：采用”原始 bytes + 版本化 format metadata envelope + 格式专属定位信息”的保真底座；TXT 定位信息必须由 parser 生成并由 exporter 在替换前针对原始 bytes、hash、encoding/BOM/newline、span 边界和解码结果逐项验证。不得把所有格式强行压成一个通用 span 模型。旧 TXT Project 只做可升级迁移，不从 source_text 猜造原始 bytes；缺少可验证原始 bytes/metadata 时安全拒绝保真导出，用户提供并校验原文件后才允许显式补齐载体。
resume_milestone: P1-T05-M02
---

# 译境 / TransRealm — Agent 自动开工与开发状态

## 0. 唯一 Agent 交接入口

你正在接手 `D:\TransRealm` 的持续开发。**本文件是唯一交接入口。** 不要读取旧的 `AI_CONTEXT_INDEX.md` 或 `00_Project_Manager_Guide.md`；它们只为兼容旧链接而保留。

先读取本文件 frontmatter 和“当前开发现场”，再严格执行：

1. 确认工作目录为 `D:\TransRealm`，检查 Git staged、unstaged、untracked 和测试证据；不得覆盖、丢弃、reset、restore、clean 或删除既有工作。
2. 读取 `07_Developer_Task_List.md` 中 `current_task` 的完整定义；它定义目标、非目标、依赖、修改范围和验收。
3. 读取 `06_AI_Development_Guide.md`；它定义 FIT/ADAPT/REPLAN、最小设计门禁、实现流程和完成报告。
4. 按当前 Task 的“输入文档”读取必要契约：产品/范围读 `01_PRD.md` 与 `02_Development_Roadmap.md`；架构读 `03_Technical_Design.md` 与 `08_Architecture_Review.md`；数据库读 `04_Database_Schema.md`；Prompt/Context/输出读 `05_Prompt_Architecture.md`；高风险、例外、发布/回滚读 `09_Unattended_Development_Governance.md`；候选发布才读 `RELEASE_CHECKLIST.md`。
5. 在写代码前记录 FIT、ADAPT 或 REPLAN。FIT 直接执行；ADAPT 只改可逆内部实现；REPLAN 停止冲突部分并记录证据、最小替代方案、影响、回滚成本和待裁决项。
6. 状态为 `ready`、`in_progress` 或 `verification` 且没有 REPLAN 时，直接推进当前小目标：测试/验收样例 → 最小实现 → 功能与异常测试 → Review → 文档与本文件同步。
7. 完成或中断时，更新本文件中的实际结果、命令/输出、兼容性基线（受影响旧能力、旧测试、新测试和实测结果）、风险、阻塞、恢复动作和下一小目标。没有测试证据、旧功能回归、Review 或文档同步不得标记 `completed`。

只有产品或架构变化、破坏性或对外操作、用户专属凭据/文件、付费调用，或无法由代码/测试/Git 消除的实质歧义才询问用户。常规可逆选择自行决定。

## 1. 权威来源与冲突顺序

发生冲突时按以下顺序处理：

1. 用户在当前会话中的明确指令；
2. `01_PRD.md` 的产品范围；
3. `08_Architecture_Review.md` 的已批准架构决策；
4. `03_Technical_Design.md`、`04_Database_Schema.md`、`05_Prompt_Architecture.md` 的实现契约；
5. `02_Development_Roadmap.md` 的阶段与版本安排；
6. `07_Developer_Task_List.md` 的任务定义；
7. 本文件的运行状态。

本文件只记录执行现场，不得擅自改变产品或架构。发现本文件陈旧时，以代码、测试和权威编号文档为准，并修正本文件。

## 3. 固定项目基线

- 核心语言：Python 3.12；
- GUI：PySide6（Qt 6）；
- 数据库：SQLite；
- 测试：pytest，Qt 交互测试可使用 pytest-qt；
- 首发平台：Windows 11；
- 发布形态：绿色版优先；
- V1.0 Model Adapter：OpenAI-compatible HTTP；
- llama.cpp、Ollama：优先使用兼容端点；
- Sakura、Murasaki：Model Profile，不代表专有调用协议；
- V1.0 不包含 RAG、智能 TM、World State、节点式 Workflow 编辑器、插件、内置模型管理、多人协作或原生多厂商 API。

## 4. 当前开发现场

### 当前环境检查

- 2026-07-28 检测时 Python 3.12 缺失，已阻塞；当前会话 `py -3.12 --version` 返回 Python 3.12.10，阻塞解除。

### 当前计划治理状态

- 已启用 `06_AI_Development_Guide.md` V4：规划 Agent 定义目标、边界和验收，执行 Agent 对代码现场和范围内最小实现负责。
- 当前及未来 Milestone 开工前必须记录 `FIT`、`ADAPT` 或 `REPLAN`；没有 `REPLAN` 证据时不得仅因计划可能不完美而停工。
- 已完成的 P0-T01 至 P0-T08 默认保留，不做无证据的推倒重来；发现具体缺陷时以测试保护的最小修正处理。
- 未来 Task 中的类名、Manager/Service、文件落点和局部算法默认是参考实现，除非权威文档明确标注为强制契约。
- 无人开发的角色、风险、门禁、例外和发布/回滚规则以 `09_Unattended_Development_Governance.md` 为准。

### 待决策节点

- **pending_decision：** `DEC-P1-T01-FOUNDATION`
- **触发 Milestone：** P1-T01-M01
- **问题：** 选择后续六种格式共用的 format metadata、原始 bytes/span 表示和旧 TXT Project 升级策略。该选择决定 no-op byte identity、Revision 只替换目标 span 和 P1-T02 Project 容器的基础，不能由开发 Agent 自行猜测。
- **不可变约束：** 不重写 `001`–`007` migration；TXT no-op 必须字节一致；翻译仅改变目标 span；原编码/BOM/换行必须可恢复；metadata 与源内容版本同事务保存；缺失或错配安全失败；不实现其他格式或改变翻译状态机。
- **现场证据：** 当前 `TxtExporter` 按 Revision 顺序写 UTF-8，未保留原 bytes/encoding/BOM/换行；`SourceDocument` 当前只保存 format、encoding、source_hash、parser_version；P1-T01-M01 是后续 JSON/SRT/VTT/ASS/SSA 的共同底座。
- **候选方案：**
  1. 原始 bytes + 每 Segment byte span；保真最直接，但多字节编码/span 验证复杂。
  2. 原始 bytes + 格式专属定位 metadata；允许每格式安全定位，需严格契约避免五套分叉逻辑。
  3. 重序列化格式对象；拒绝，无法满足 byte identity。
- **技术负责人推荐：** 方案 2：持久化原始 bytes、encoding/BOM/newline 与版本化 format metadata；TXT 使用受验证的 byte/character replacement mapping，后续格式使用同一 metadata envelope 中的格式专属定位信息。理由：满足保真且不强迫 JSON/字幕共享错误的 span 模型。
- **待更新文档：** `03_Technical_Design.md` §9、`04_Database_Schema.md` §3/5、`07_Developer_Task_List.md` P1-T01、必要时 `01_PRD.md`。
- **验收/回滚条件：** 旧库升级有新 migration 与备份；TXT no-op/translated/error/metadata mismatch/不可写目标有 fixture；失败不污染数据库或目标文件；回滚使用迁移前备份或前向修复，不能删除用户数据。
- **决策裁定（2026-08-06）：** 唯一推荐方案 2：持久化原始 bytes，并在同一版本化 `format metadata envelope` 中保存 encoding、BOM、newline、parser/metadata version、源 hash 和格式专属定位信息；TXT 使用受验证的 byte/character replacement mapping，JSON/SRT/VTT/ASS/SSA 各自保存其安全定位 metadata，但共用 envelope、Revision 校验和原子 exporter seam。该方案同时满足 TXT no-op byte identity、仅替换目标 span、原编码/BOM/换行恢复，并避免错误地假设字幕/JSON 与 TXT 共享同一 span 语义。
- **候选否决：** 方案 1（统一原始 bytes + 每 Segment byte span）否决为基础契约：多字节编码、换行归一化、格式结构和跨 token 边界使统一 span 规则无法安全覆盖五类后续格式，容易把“可定位”误化为“可替换”；各格式仍可在 envelope 内使用 spans。方案 3（重序列化格式对象）明确否决：会改变空白、转义、字段/标签或换行，不能满足 byte identity 和非目标字节不变。
- **边界保留项：** 不在本裁决中决定 JSON path 的用户可配置选择、各格式 metadata 的最终字段细节、第三方 parser 依赖或 ASS/SSA 的具体 parser 实现；这些由各垂直 Milestone 在已批准 envelope/保真约束内用证据决定。旧 TXT 数据若没有原始 bytes/可验证 metadata，不得猜测恢复或静默重序列化；应安全失败并保留旧 Project/目标文件。
- **必须更新的权威文档：** `03_Technical_Design.md` §9（envelope、定位验证和 exporter seam）；`04_Database_Schema.md` §3/§5/§6（新增 migration 的 metadata/raw bytes 持久化、逻辑唯一域、同事务和失败策略）；`07_Developer_Task_List.md` P1-T01/P1-T01-M01（把方案 2 和旧 TXT 缺载体拒绝纳入硬约束/验收）；必要时 `01_PRD.md` 仅在用户可见格式语义变化时更新；`08_Architecture_Review.md` 记录本次取舍理由。实现前不得开始 P1-T01-M01 的冲突范围。
- **验收条件：** 先以迁移前备份创建旧库 fixture；验证旧 TXT Project 升级、缺 metadata/raw bytes 安全失败和可验证补齐路径。TXT 至少覆盖原始 bytes 含 UTF-8 多字节/UTF-8 BOM/UTF-16 BOM/CRLF、空行/首尾空白等非目标 bytes：no-op 输出逐字节一致；单/多 Segment Revision 输出只改变对应目标 span；原始 hash、span 越界/重叠/错位、encoding/BOM/newline 或 metadata version mismatch 均拒绝；目标不可写、编码错误和任一校验失败均不污染 DB、Revision 或既有目标文件。运行受影响旧 TXT import/export、migration backup/upgrade、Run/Revision 测试 nodeid，再运行新增保真测试、Ruff、mypy 和全量 pytest；migration/导出须有故障注入证据。
- **迁移/回滚条件：** 只追加新编号 migration，不修改 `001`–`007`；migration 必须在升级前一致性备份成功后执行，metadata/raw bytes 与既有 SourceDocument/Segment 版本同事务写入。发现唯一域冲突、bytes 缺失无法安全填充、checksum/结构校验失败时停止并保留原库与备份。回滚优先使用迁移前备份恢复；若已产生新格式但未发布，允许前向修复 migration，禁止删除用户数据、覆盖旧 Revision 或把 source_text 当 raw bytes。
- **恢复 Milestone：** `P1-T01-M01`。开发恢复前必须重新核验当前代码现场与旧 TXT 升级路径，记录 `FIT`/`ADAPT`/`REPLAN`；只实施上述裁定，不重新讨论 format metadata/raw bytes/span 的基础选择。

```text
请切换至决策 Agent。读取 D:\TransRealm\DEVELOPMENT_STATE.md 中的“待决策节点”。
只评估，不写业务代码。基于不可变约束、现场证据和候选方案，输出唯一推荐或明确保留项；说明否决理由、要更新的权威文档、验收、迁移/回滚条件和恢复 Milestone。完成后把 decision_result 与开发恢复提示词写回 DEVELOPMENT_STATE.md，并提醒用户切回开发 Agent。
```

### 切回开发 Agent

决策完成前不要开始 P1-T01-M01。决策 Agent 写回 `decision_result` 后使用：

```text
请切回开发 Agent。读取 D:\TransRealm\DEVELOPMENT_STATE.md 的 decision_result、resume_milestone 及更新后的权威文档。只实施已裁决范围；先重新完成 FIT/ADAPT/REPLAN，再继续目标 Milestone。不得重新讨论已裁决的产品/契约选择。
```

- **当前恢复状态：** `P1-T03-M05` 已完成（模式切换与 GUI Gate，Reality Check **ADAPT**——现场核验 M01–M04 落地与 seam 齐备，唯一缺口是模式切换 UI 无调用方、运行中切换无提示、无文档列表 + 切换 Project 保留旧 `_document_id`（M01 已知边界）、重启后文档/结果/锁定证据缺位）：`TranslationPage` 新增交互模式选择器（`QComboBox` Auto/Workbench，经 `ServiceWorker` 调 `ProjectService.set_mode` 持久化；运行中激活拒绝并提示 finish or Cancel、选择器恢复；`set_mode` 成功路径同步 `_mode` 防 refresh 失败失同步）与每 Project 文档选择器（`TranslationRunService.list_source_documents` 只读转发 `SegmentRepository.list_source_documents_by_project`，UI 经 Application Service 不违反结构性守卫；提交时捕获 `current`/`project_id` 无跨线程读；有效文档属该 Project 则沿用否则清空，消除跨 Project 陈旧 `_document_id`；无有效文档组合框不高亮首项）；`_set_running` 集中管理 `_running` 与 Translate/Cancel/Export/文档选择器按钮态，运行中切文档同样被拒；取消/关闭/重启门禁复用既有 seam（`request_stop` Segment 边界取消、closeEvent 有界 wait 收敛、共享 SQLite 重开自动恢复 mode/active/文档/结果/锁定）。全量 pytest **1298 passed**、受影响旧测试子集 **461 passed**、Ruff clean、mypy 126 files no issues；实际可用性 smoke（真实窗口 + 完整旅程驱动）SMOKE PASS；独立 Review **无 BLOCKER**（2 SHOULD-FIX 已修复并补测试：运行中文档切换无守卫、set_mode 成功但 refresh 失败失同步；MINOR 已处理/记录取舍），见 `08`。指针推进至 P1-T05-M01（ready，pending_reality_check）。

- **基线事实：** `master` 的 P0 基线已在 `5ef3be6` 固化，治理/CI 基线已在 `90f1c70` 固化并推送至公开 GitHub `BFRKQSB7/TransRealm`。此后 P1-T01/P1-T02/P1-T04/P1-T03-M01–M05 的业务与测试改动均为未提交工作树内容（`governance_commit_pending`），未获授权不提交/推送。
- **工作树处置：** 必须保留现有 staged、unstaged、untracked 与用户数据；禁止 `reset --hard`、`restore`、`checkout` 覆盖、`clean` 或递归删除。`x.db` 与备份数据库已由 `.gitignore` 忽略。
- **基线状态：** 当前回滚 ref 为 `90f1c70`；pytest 1298 passed、Ruff clean、mypy 126 source files 无问题，Candidate hygiene CI 已成功。

### 当前 Task

- **Task：** P1-T05 — V1.0 异常恢复与 Release Candidate Gate
- **状态：** in_progress（M01 completed；M02 ready）
- **完整定义：** `07_Developer_Task_List.md` 的"P1-T05"章节
- **前置依赖：** P1-T03、P1-T04 及所有前序 Task 完成（已完成）；工作树先形成可恢复 Git 基线（**已满足**——本地基线 `3ef991d`，2026-08-08 经用户授权 commit，未 push）；发布候选版本、依赖和文档冻结（依赖/文档已由 M01 冻结，候选版本号在 M04 构建时定）。

### 当前小目标

- **Milestone：** P1-T05-M02 — 翻译/恢复/长文本 Gate
- **状态：** ready
- **plan_alignment：** pending_reality_check
- **目标：** 自动覆盖请求与事务崩溃切点、断网/model-stop/timeout、lease/retry/cancel/locked；固定长文本用 fake/local server 跑完整旅程，状态全可解释、数据损失零、completed 不重复覆盖并记录资源基线（完整定义见 `07` §20 P1-T05-M02）。
- **禁止事项：** 不新增产品能力；不自动创建 GitHub 资源、不提交/推送/发布、不用真实付费 API 跑自动矩阵；不得删除既有用户数据、备份或既有工作树改动。
- **前置已满足：** 可恢复 Git 基线 `3ef991d`（2026-08-08 用户授权本地 commit，未 push）——`09` §6"候选仅能来自可定位基线"满足，`scripts/verify_task.py` 现可对 P1-T05 改动生效。

## 5. 历史 Milestone 状态索引

只有当前小目标完成后才激活下一项：

| Milestone | 目标 | 状态 |
|---|---|---|
| P0-T01-M01 | Python 3.12 `src/` 工程与 pytest 骨架 | completed |
| P0-T01-M02 | SQLite 连接、PRAGMA 与事务基础 | completed |
| P0-T01-M03 | migration discovery、顺序和 history/checksum | completed |
| P0-T01-M04 | `001_init` 与 Project create/open/save API | completed |
| P0-T01-M05 | migration 失败、checksum、只读/Unicode/长路径异常测试 | completed |
| P0-T01-M06 | P0-T01 Code Review、文档同步与完成验收 | completed |
| P0-T02-M01 | TXT 解析器与最小 Segment 模型 | completed |
| P0-T02-M02 | Parser 接口抽象与 TXT 异常测试 | completed |
| P0-T02-M03 | Segment 稳定 key 碰撞与重复导入策略 | completed |
| P0-T03-M00 | SQLite migration 升级前一致性备份 | completed |
| P0-T03-M01 | Provider Connection 非敏感配置模型与持久化 | completed |
| P0-T03-M02 | Model Capability/Profile 模型、持久化、Connection 外键与关闭重开测试 | completed |
| P0-T03-M03 | timeout/retry policy、JSON 配置和关闭重开的一致性验收 | completed |
| P0-T03-M04 | Task review、全回归、编号文档/索引/状态同步 | completed |
| P0-T04-M01 | Adapter Protocol、DTO、capability contract 与 normalized error | completed |
| P0-T04-M02 | OpenAI-compatible 成功请求/响应映射 | completed |
| P0-T04-M03 | timeout、限流、错误分类与有限 transport retry | completed |
| P0-T04-M04 | capability 降级、credential resolver 边界与脱敏验收 | completed |
| P0-T04-M05 | Review、全回归和文档/状态同步 | completed |
| P0-T05-M01 | Context Candidate / Manifest / 预算 DTO | completed |
| P0-T05-M02 | 当前与相邻 Segment 的最小规则预算 | completed |
| P0-T05-M03 | 锁定 Glossary 候选接口与预算不足行为 | completed |
| P0-T05-M04 | Profile 模板渲染与不可破坏输出契约 | completed |
| P0-T05-M05 | 可复现性 review、全回归和同步 | completed |
| P0-T06-M01 | 解析/校验 DTO、错误分类和 fixture 规范。 | completed |
| P0-T06-M02 | 单 Segment parser/validator。 | completed |
| P0-T06-M03 | 批量 items 的 ID/数量/顺序交叉校验。 | completed |
| P0-T06-M04 | 有限修复分类与恶意/异常输出矩阵。 | completed |
| P0-T06-M05 | Review、全回归和同步。 | completed |
| P0-T07-M01 | 可重开执行骨架：新 migration、只读内置 Workflow、TranslationRun 与 SegmentAttempt 持久化及重开测试。 | completed |
| P0-T07-M02 | 原子领取与幂等：一个 pending Segment 只能被一个 owner 以新 version 领取并关联新 Attempt；重复幂等键返回既有记录、不重复执行；未过期 lease、错误状态和 stale version 被拒绝。 | completed |
| P0-T07-M03 | 成功完成事务：对有效 Validator 结果一次性保存响应审计、追加 Revision、更新 current revision 并完成 Segment/Attempt；任一故障切点全部回滚或保持可恢复 processing，迟到 owner 和 locked/current 已变化时不得提交。 | completed |
| P0-T07-M04 | 失败、取消与启动恢复：normalized permanent/retryable/validation 错误形成可解释 Attempt；仅 retryable failed 可重新排队并创建新 Attempt；取消保留既有 Revision/原因；启动只回收过期 processing，不覆盖人工/locked current。验证 transport retry 同 Attempt、repair/业务 retry 新 Attempt。 | completed |
| P0-T07-M05 | 恢复矩阵与 Task Gate：崩溃注入与重启矩阵验证请求前/后、响应审计、Revision、current/status 各切点崩溃后无伪 completed、无孤立 current revision、历史不覆盖；重复启动恢复幂等；lease 冲突与锁定回归不越 fencing；全量 pytest/Ruff/mypy、范围 Code Review 和文档/索引/状态同步。 | completed |
| P0-T08-M01 | 无 UI 成功闭环：新增 `application/translation_service.py`（TranslationService）复用 ContextComposer/PromptRenderer/OutputParser/ModelAdapter/TranslationRunService，把一个 pending Segment 垂直串成 compose→render→claim→call→validate→T07 finalize，生成可重开 Revision；空 Segment/缺 Profile/预算不足/非 running Run 在 claim 前拦截不发请求；adapter 错误与无效输出 finalize 为 failed Attempt。新增 `tests/test_p0_t08_m01.py` 11 项（含真实 OpenAICompatibleAdapter + FakeTransport 组合）。 | completed |
| P0-T08-M02 | 生产调用边界：新增 `adapters/http_transport.py`（StdlibHttpTransport，urllib.request + asyncio.to_thread，手动重定向循环：同源保留头、跨源/HTTPS 降级剥离 Authorization/Cookie/Proxy-Authorization、只跟随 http/https）、`adapters/credential_resolvers.py`（EnvironmentResolver/WindowsCredentialResolver(ctypes CredReadW)/build_resolver，secret 不进错误消息）、`application/adapter_factory.py`（compose_adapter 组合 Connection+Profile→adapter）。用 stdlib ThreadingHTTPServer 验证 endpoint 组合、超时/断连/截断分类、同源/跨源重定向 Authorization 边界、401/429/500/418 稳定归类、secret 不泄（错误与审计）、TranslationService 对真实本地 server 的完整闭环。Reality Check 结论 **FIT**：零运行时依赖（stdlib），`dependencies=[]` 不变。新增 `tests/test_p0_t08_m02.py` 33 项。 | completed |
| P0-T08-M03 | 失败与有限修复 E2E：`TranslationService.translate_segment` 增加 `max_repair_attempts=1` 参数与有界 repair 重呼——无效但 `issues[0].repairable`（空译文/污染）输出经 `finalize_failure(retryable=True)`→`retry_failed`→`start_attempt`（新幂等键、同 rendered prompt）创建新 Attempt；repair 层独立记账于 repair Attempt 的 `validator_summary`（repair_attempt/repair_reason）；非 repairable 或 repair 耗尽→`finalize_failure(retryable=False)` 永久失败。验证：repair 成功（空译文/污染）、repair 耗尽、`max_repair_attempts=0`/负值守卫、非 repairable 单 Attempt 不重呼、永久 adapter 错误不业务重试（retry_failed 拒绝）、崩溃（repair 第 2 次调用抛 SimulatedCrashError）后重启 recover_expired_leases 回收并重译成功且原 run 可解释。Reality Check 结论 **FIT**：无 schema/公共契约/新依赖。新增 `tests/test_p0_t08_m03.py` 8 项。 | completed |
| P0-T08-M04 | Revision 驱动 TXT 导出：新增 `application/exporter.py`（`TxtExporter`/`ExportError`，构造运行 migration、只读连接，`export_document(source_document_id, target_path, *, revision_overrides=None, encoding='utf-8')`）：按 `ORDER BY sequence` 原顺序取 Segment，默认写 `current_revision_id` 对应 Revision；`revision_overrides: dict[segment_id, revision_id]` 显式选择，逐 Revision 校验存在/归属（`segment_id`），未知 segment 键报错；任一 Segment 缺可用 Revision 即失败、不回退源文；同目录 `tempfile.mkstemp` + `os.replace` 原子写，编码/目标不可写清理临时文件并保持目标不变；默认 UTF-8。`SegmentRepository` 增 `get_source_document_by_id` 只读方法。Reality Check 结论 **FIT**：零新依赖（stdlib tempfile/os）。新增 `tests/test_p0_t08_m04.py` 13 项。 | completed |
| P0-T08-M05 | 非阻塞桌面闭环：新增 `ui/main_window.py`（三 Tab：Settings/Project/Translation）、`ui/pages.py`（SettingsPage/ProjectPage/TranslationPage，经 `ServiceWorker` 调 Application Service）、`ui/worker.py`（`ServiceWorker` 通用任务执行器 + `TranslationWorker` 逐 Segment 翻译，槽经信号排队到各自线程）。PySide6 6.11.1 + pytest-qt 4.5.0 依赖评估完成（LGPL/MIT、Qt 官方维护、`dependencies=["PySide6>=6.8,<6.12"]`，全量锁属 V1.0 Gate）。验证：UI 完整闭环（建项目/导入/建 Profile/翻译/导出，worker 线程执行、主线程 QTimer 在翻译中触发证明不阻塞）、取消在 Segment 边界停止、关闭 request_stop+有界 wait 收敛无 processing 遗留。Reality Check 结论 **FIT**。新增 `tests/test_p0_t08_m05.py` 4 项。 | completed |
| P0-T08-M06 | Phase 0 Gate：复核无 UI 与 GUI 两条入口对成功/失败/关闭/reopen 的 E2E 证据（M01–M05）；全量质量命令 pytest 488 passed、Ruff clean、mypy 87 files no issues；PyInstaller 6.21.0 `--onedir --windowed` build/start 可行性 smoke（~58s、~123 MB、主窗口可见、SQLite 由 frozen app 创建）；**发现并验证修复迁移 SQL 数据文件不被自动收集**（`--add-data "src/transrealm/migrations;transrealm/migrations"` 后 6 迁移全应用）；记录 Qt 插件状态（platforms/styles 等已收集、sqldrivers 缺属预期因仅用 stdlib sqlite3）；不冻结工具/artifact，smoke 产物在 `%TEMP%`。Reality Check 结论 **FIT**。 | completed |
| P1-T01-M01 | **保真载体与 TXT 证明：** 裁决方案 2 落地——`008` migration 加 `source_documents.raw_bytes`（BLOB）/`format_metadata`（JSON envelope：encoding/BOM/newline/parser_version/source_hash + `txt.byte_spans` locator），逻辑唯一域换为 `(project_id, source_hash, format, parser_version)`；`infrastructure/fidelity.py` 共享 envelope/encoding/BOM/newline 规则；`TxtParser` 单遍生成逐 Segment byte span、Segment 映射与旧 `splitlines()` 一致；`TxtExporter` 改为保真 seam：替换前逐项验证 hash/encoding/BOM/newline/span 越界/重叠/解码，no-op（`apply_revisions=False`）逐字节输出、翻译仅改目标 span、缺载体安全拒绝；`ImportService.backfill_fidelity` 用户提供原文件且 hash+Segment 映射一致后同事务写载体。新增 `tests/test_p1_t01_m01.py` **35 项**（no-op UTF-8 多字节/BOM、UTF-16 LE/BE BOM、CRLF/CR、splitlines 边界、空/空白文件字节一致；单/多 Segment 替换只改目标 span 含 UTF-16/BOM/override；越界/重叠/错位/hash/BOM/newline/schema version/format/malformed/存储 bytes hash 篡改拒绝；编码错误/不可写目标不污染 DB/Revision/目标文件；旧库(001–007)升级备份不含 008、缺载体拒绝、U+2028 旧库 backfill；唯一域隔离 format/parser version + 同身份幂等）。受影响旧测试 195 项 + 全量 533 passed、Ruff clean、mypy 89 files no issues；独立 Review Agent 已验收（发现 splitlines 边界回归、BOM 单向校验、偏移内存 3 项均已修复）。Reality Check 结论 **ADAPT**。 | completed |
| P1-T01-M02 | **JSON 垂直闭环：** 裁决方案 2 落地——新增 `parsers/json_parser.py`（`JSONParser` + 单遍迭代 tokenizer，深度上限 512）：只把 value 位置字符串 token 生成为 Segment，记录 JSON Pointer 结构路径 + value token byte span（`json.paths` locator）；malformed/重复对象 key/超深度/未配对 `\u` 代理项导入期拒绝且不污染 Project；`fidelity.py` 加 `build_json_envelope`；`exporter.py` 提取 `FidelityExporter` 基类（共享 carrier/envelope 验证/Revision 校验/mkstemp+os.replace 原子写），`TxtExporter` 公共 API 与行为不变，新增 `JsonExporter`（替换前重解析原始 bytes 逐项验证 path/span/value==source_text，译文经 `json.dumps(ensure_ascii=False)` 转义替换目标 token）；`default_registry()` 注册 `JSONParser`。新增 `tests/test_p1_t01_m02.py` **71 项**（no-op 字节一致含嵌套/转义/非 ASCII/UTF-8 BOM/UTF-16 LE/BE/CRLF/空容器/无字符串文档；翻译仅改 value token 保留 key/order/number/whitespace/转义；UTF-16 替换；Revision override；语义 round-trip 重导入；malformed/深度/重复 key/未配对代理项拒绝无污染；envelope 篡改（path/span/value/sequence/hash/BOM/newline/schema/parser version/format/raw bytes/locator/数量）拒绝；编码失败/不可写目标不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize）。受影响旧测试 230 项 + 全量 604 passed、Ruff clean、mypy 91 files no issues；独立 Review Agent 已验收（发现根值后单尾随 token 误接受，已修复并加 6 用例；其余 TXT 回归/验证充分性/范围/测试缺口无问题）。Reality Check 结论 **ADAPT**。 | completed |
| P1-T01-M03 | **SRT 垂直闭环：** 裁决方案 2 落地——新增 `parsers/srt_parser.py`（`SRTParser` + 单遍物理行扫描 `extract_cues`）：每个 cue 的文本块（可选 index 行 + timing 行 + 非空文本行直到空行，内部换行原样保留）生成为 Segment，`srt.cues` locator 记录 sequence/index/原样 start/end/可选 settings/文本块 byte span；拒绝非法时间轴（缺 `-->`/坏分隔/分钟秒>59/毫秒位数错）、重复 cue 时间轴（规范化 start/end 含逗号/点差异）、空 cue 文本、缺失空行分隔、序号行后缺失 timing，导入期拒绝不污染 Project；`fidelity.py` 加 `build_srt_envelope`；`exporter.py` 新增 `SrtExporter`（复用 `FidelityExporter` seam，替换前用同一 `extract_cues` 重解析验证 sequence/index/timing/settings/span/text，译文直接编码替换目标文本块）；`default_registry()` 注册 `SRTParser`。新增 `tests/test_p1_t01_m03.py` **75 项**（no-op 字节一致含 LF/CRLF/CR-only/UTF-8 BOM/UTF-16 LE/BE/多行/首尾空行/settings/点分隔/index-less/EOF 无尾换行；翻译仅改 cue 文本块保留结构含多行块/CRLF/UTF-16/override/round-trip；非法时间轴/重复定位/空文本/缺分隔拒绝无污染；envelope 篡改（index/timing/settings/span/sequence/source_text/hash/BOM/newline/schema/parser version/format/raw bytes/locator/数量/re-parse 失败）拒绝；encoding override 匹配/错配；编码失败/不可写目标不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize）。受影响旧测试 301 项 + 全量 679 passed、Ruff clean、mypy 93 files no issues；独立 Review Agent 已验收（无 BLOCKER/SHOULD-FIX，4 MINOR 为已记录取舍/既有共享行为 + 3 测试缺口已补齐）。Reality Check 结论 **ADAPT**。 | completed |
| P1-T01-M04 | **VTT 垂直闭环：** 裁决方案 2 落地——新增 `parsers/vtt_parser.py`（`VTTParser` + 单遍物理行扫描 `extract_cues`）：先校验 `WEBVTT` 文件签名（可带 BOM、后随空格标题或行尾），`NOTE`/`STYLE`/`REGION` 块消费到空行为结构性 bytes 永不生成 Segment；每个 cue 的文本块（timing 行后非空行直到空行或含 `-->` 的行）生成为 Segment，`vtt.cues` locator 记录 sequence/id/原样 start/end/可选 settings/文本块 byte span；W3C 时间戳规则（完整 `HH:MM:SS.mmm` 与省略小时 `MM:SS.mmm`，点分隔，毫秒恰 3 位，分钟/秒 00-59，小时位数不限）；拒绝缺签名/签名后非空格/坏时间轴/重复 cue 时间轴/空 cue 文本/bad cue 标识/含 `-->` 非 timing 文本行，SRT 等非 WebVTT 内容不静默降级，导入期拒绝不污染 Project；`fidelity.py` 加 `build_vtt_envelope`；`exporter.py` 新增 `VttExporter`（复用 `FidelityExporter` seam，替换前用同一 `extract_cues` 重解析验证 sequence/id/timing/settings/span/text，译文直接编码替换目标文本块，内联 `<b>/<v>` 标签属目标 span）；`default_registry()` 注册 `VTTParser`。新增 `tests/test_p1_t01_m04.py` **86 项**（no-op 字节一致含 LF/CRLF/CR/UTF-8 BOM/UTF-16 LE/BE/header+NOTE/STYLE/REGION/内联标签/部分+完整时间戳/无空行分隔 cue/header-only；翻译仅改 cue 文本块保留 header/ID/timing/settings/blocks 含多行/换行/CRLF/UTF-16/override/round-trip；缺签名/逗号时间戳/SRT 内容/坏时间轴/重复定位/空文本/bad cue/越界分钟秒拒绝无污染；envelope 篡改（id/timing/settings/span/sequence/source_text/hash/BOM/newline/schema/parser version/format/raw bytes/locator/数量/re-parse 失败）拒绝；encoding override 匹配/错配；编码失败/不可写目标/缺载体不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize）。受影响旧测试 376 项 + 全量 765 passed、Ruff clean、mypy 95 files no issues；独立 Review Agent 已验收（无 BLOCKER，1 SHOULD-FIX 时间戳分钟/秒超 59 已修复并补测试，其余 MINOR 为已记录取舍/共享 seam 行为）。Reality Check 结论 **FIT**。 | completed |
| P1-T01-M05 | **ASS/SSA 垂直闭环：** 裁决方案 2 落地——新增 `parsers/ass_ssa_parser.py`（`AssSsaParseError`/`AssSsaEvent`/`extract_events` 单遍行扫描 + `AssParser`/`SsaParser` 共享 `_AssSsaParserBase`，只差 format/ScriptType，不创建两套无必要框架）：`[Script Info]` 开头签名，`ScriptType:` 必须匹配 format（ass=`v4.00+`、ssa=`v4.00`，错配拒绝防静默降级）；行分类经 strip + `^key\s*:` 归一化（尾空格 section 头/缩进行/`Key :` 变体均识别，不静默吞掉事件）；`[Events]` 必须声明标准 `Format:` 字段表（v4 `Marked,Start,...`/v4+ `Layer,Start,...`，缺/重复/非标准拒绝，避免猜测 Text 分割逗号数）；每个 `Dialogue:` 事件按字段数 split 取最后 Text 字段（逗号/标签/`\N`/`\h` 转义/首尾空格原样保留）生成 Segment，`ass.events`/`ssa.events` locator 记录 sequence/start/end/Text byte span；Start/End 按 `h:mm:ss.cc` 校验（分钟/秒 00-59、百分秒恰 2 位）；空/纯空白 Text 拒绝；重复时间轴允许（ASS/SSA 合法结构，与 SRT/VTT 不同）；`Comment:` 与其他事件类型为结构性 bytes；`fidelity.py` 加 `build_ass_envelope`/`build_ssa_envelope`；`exporter.py` 新增 `_AssSsaExporter(FidelityExporter)` + `AssExporter`/`SsaExporter`（替换前用同一 `extract_events` 重解析验证 sequence/start/end/span/text，译文直接编码替换目标 Text 字段）；`default_registry()` 注册 `AssParser`/`SsaParser`。新增 `tests/test_p1_t01_m05.py` **155 项**（no-op 字节一致含 LF/CRLF/CR/UTF-8 BOM/UTF-16 LE/BE/全文件含 section/style/comment/Comment 事件/标签/转义/逗号文本/EOF 无尾换行；翻译仅改 Dialogue Text 含多 Dialogue/Comment 不译/逗号/标签转义/首尾空格整字段替换/CRLF/UTF-16/override/round-trip；空白变体不静默吞掉；畸形拒绝无污染；envelope 篡改拒绝 + `locator.type` 单独篡改无害；encoding override 匹配/错配；编码失败/不可写目标/缺载体不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize；同身份幂等 + 同 bytes ass/ssa/txt 分离）。受影响旧测试 462 项 + 全量 920 passed、Ruff clean、mypy 97 files no issues；独立 Review Agent 验收 **APPROVE-WITH-MINORS**（1 SHOULD-FIX 行分类空白敏感会静默吞掉事件已修复并补 10 项空白变体测试，MINOR 为已记录取舍/共享 seam 行为，`locator.type` 单独篡改无害与 `\h` 转义各补测试）。Reality Check 结论 **FIT**。 | completed |
| P1-T01-M06 | **全格式 Gate：** Reality Check **FIT**；复核 M01–M05 六格式 no-op/translated/error/不可写/旧库升级矩阵证据与共享 `FidelityExporter` seam/envelope 一致性（TXT byte span / JSON path / SRT·VTT cue / ASS·SSA events 各 locator 与 parser 重解析逐项一致）；全量 pytest 920 passed（M01 TXT=35、M02 JSON=71、M03 SRT=75、M04 VTT=86、M05 ASS/SSA=155，合计 422 + P0 基线 498）、受影响旧测试子集 617 passed、Ruff clean、mypy 97 files no issues；独立 Review 无 BLOCKER/SHOULD-FIX，4 MINOR 已记录取舍（TXT exporter 不校验 parser_version、Revision 解析块重复、导出 0600 权限、backfill 行版本潜在隐患）；同步 `07` 状态（P1-T01 completed）与 M05 计数（141→155、906→920）。 | completed |
| P1-T02-M01 | **共享 manifest 与开放目录：** Reality Check **ADAPT**——现场 seam（`ProjectService`/`MigrationRunner`/P0-T03-M00 备份）与 `03` §3、`04` §2/7 双形态契约齐备，无 schema 变更不引入新 migration，M01 范围已在 `04` §7 裁决故不触发 DEC-P1-T02-CONTAINER（资源限额/隔离 staging 属 M03）；内部调整：容器化需清理新建库产生的空库 `*.pre-upgrade-*.db.bak` 与 WAL 附属文件、打开强制单 Project 身份并校验 schema_version 一致、manifest JSON 字段名自定并版本化。新增 `infrastructure/open_directory.py`（manifest 结构/版本校验、重复 JSON key 拒绝、`canonical_relpath` 拒绝绝对/盘符/UNC/`..`/控制字符、`is_link` symlink+Windows reparse point、逐 entry 大小+`sha256:` hash 校验、原子写、identity 折叠大小写/Unicode/尾部点空格别名）与 `application/open_directory_service.py`（create/open 容器：目录必须为空、失败清理自身产物且保留用户既有空目录、打开先校验 manifest 再校验路径/link/hash 最后打开 DB 并要求恰一个 Project 身份）；`ProjectService` 增只读 `list_projects()`。新增 `tests/test_p1_t02_m01.py` **76 项**（clean container、manifest 字段/hash、重开同身份、非空目录/文件路径/多 Project/篡改拒绝、附件声明、未知 format/schema 版本、缺字段、顶层与嵌套重复 key、critical 缺失/重复/改名、manifest 自别名含尾部点、大小写/Unicode/尾部点等价重复、16 种根外路径、缺文件/大小/hash/目录/junction/symlink/父目录 junction 逃逸拒绝、`is_link` reparse 单测、失败清理含既有空目录保留）。受影响旧测试子集 193 passed + 全量 pytest **996 passed**、Ruff clean、mypy 100 files no issues；独立 Review 无 BLOCKER，1 SHOULD-FIX（NUL 字节路径拒绝 + `is_link` 捕获 ValueError）与 2 项 MINOR 加固（尾部点/空格别名折入 identity、checkpoint 失败不删 WAL）已修复并补测试，其余 MINOR 记录取舍（`software` 不比对开放版本、大写 hash fail-closed、create 尾部 reopen 失败保留完整容器）。 | completed |
| P1-T02-M02 | **一致性单文件导出：** Reality Check **ADAPT**——`.aiproject` = 标准 ZIP（stdlib `zipfile`，零新依赖）+ `manifest.json` + 一致性 snapshot + 声明附件，内部 relpath 与 manifest 一致、解包即开放目录；导出用临时 staging 一致容器自校验后原子 zip；零/多 Project 明确拒绝。新增 `application/archive_service.py`（`export_open_directory_archive`：源开放目录经 M01 校验后导出；`export_database_archive`：源裸活动数据库 `list_projects()` 恰一个才导出，零/多 Project 拒绝 `ArchiveExportError`）、`infrastructure/migrations/backup.py` `create_consistent_snapshot`（SQLite backup API 一致性 snapshot，失败清理半成品、缺源拒绝）、`infrastructure/open_directory.py` `write_archive`（逐层拒绝 link/junction 目录、OSError 包装 `OpenDirectoryError`、原子写）。新增 `tests/test_p1_t02_m02.py` **23 项**（导出 zip 含 manifest/snapshot/附件、逐 entry 大小+hash 可复验、round-trip 解包重开同一 Project、附件子目录、成功原子覆盖既有目标、未声明 log/secret 不入包且归档无原始 secret、打包 DB 只保留 credential reference、WAL 未提交排除/已提交包含、篡改附件拒绝、多 Project 容器拒绝、缺源/缺目标目录拒绝、失败无半成品/既有目标保留、裸 DB 导出/空库/多 Project 拒绝、snapshot 失败清理/缺源拒绝/target 为目录/link 拒绝）。受影响旧测试子集 193 passed + 全量 pytest **1019 passed**、Ruff clean、mypy 102 files no issues；独立 Review 无 BLOCKER，1 SHOULD-FIX（snapshot 失败残留半成品）与 MINOR 加固（错误文案还原、junction 目录洞、OSError 包装、缺源静默建空快照拒绝）已修复并补测试，其余 MINOR 记录取舍。 | completed |

| P1-T02-M03 | **隔离安全导入：** Reality Check **ADAPT**——DEC-P1-T02-CONTAINER 不触发（限额维度已在 `04` §7 裁决、具体数值由 M03 fixture 固定）。新增 `infrastructure/open_directory.py` `parse_manifest_text`（`load_manifest` 复用）、`ArchiveLimits`（fixture 默认：entry 数 ≤1000、单项 ≤256 MiB、总 ≤512 MiB、压缩比 ≤5000:1，Release Gate 定稿）、`ArchiveError`/`ArchiveLimitError`、`extract_archive`（先读归档内 manifest 校验结构/版本/跨字段/路径——重复 key/未知版本/traversal/绝对路径/盘符/UNC/`..`/控制字符/大小写·Unicode·尾部点别名/自别名拒绝；拒绝重复/未声明/目录成员；对 ZIP 头写盘前施加限额；只解声明 entry 手工 canonical relpath；`verify_entries` 复核大小/hash/link）；`application/archive_service.py` 新增 `ArchiveImportError`/`import_archive_to_staging`（隔离 staging 生命周期、失败清理自身产物保留既有空目录、只读 DB 校验恰一 Project + schema_version 一致含非整数拒绝、成功后清 WAL 附属）。新增 `tests/test_p1_t02_m03.py` **37 项**（成功 round-trip 同契约可重开/隔离干净/手筑合法归档/高压缩非误伤；缺归档/非空 staging/非 zip/缺 manifest/非法 JSON/重复 key/未知版本/超大 manifest/缺附件/tamper/未声明/重复/目录成员/traversal/绝对路径/大小写·Unicode 冲突/自别名/四类限额/零·多 Project/schema 错配/非整数 schema/非 SQLite/失败清理均拒绝或保持）。受影响旧测试子集 216 passed + 全量 pytest **1056 passed**、Ruff clean、mypy 103 files no issues；独立 Review 无 BLOCKER，1 SHOULD-FIX（非整数 `projects.schema_version` 原始 `ValueError` 逃逸）已修复并补测试，其余 MINOR 记录取舍。 | completed |

| P1-T02-M04 | **双形态迁移与原子安装：** Reality Check **ADAPT**——现场 seam 齐备且确认 M01 已知边界"打开旧 schema 开放目录会迁移但留 stale manifest + `.bak`，二次打开 hash mismatch"为真实缺陷。开放目录容器经 `migrate_container` 就地向前迁移（只读校验 manifest/路径/hash + 恰一 Project + schema 匹配 → `ProjectService`/`MigrationRunner` seam 跑 pending migration（P0-T03-M00 先建一致性备份）→ 成功后先重算并写 manifest、最后删瞬时 pre-upgrade 备份与 WAL 附属、复验，容器只含受控条目；无 pending 不动容器）；修复 `open_open_directory_project` 缺陷（迁移后先关闭连接 checkpoint 再清理/重算/复验）。新增 `application/install_service.py`：`InstallError`/`TargetConflictError`（带 `suggested_name`）、`install_staging_to_target`（迁移 staging → 写 manifest `source_id` 供追踪 → 新/空目标原子安装：同父临时目录构建+验证+rename；目标已含 Project 时未经 `confirm_overwrite` 拒绝，同名冲突给确定性后缀建议名如 `Alpha (2)`；确认覆盖把既有目标移为 `.<target>.pre-replace-<ts>.bak` 恢复备份再安装、失败还原（还原失败点名恢复路径））。`open_directory.py` 增可选 `ManifestInfo.source_id` 与共享 helpers（`check_container_identity`/`purge_sqlite_sidecars`/`remove_pre_upgrade_backups`/`regenerate_manifest`/`cleanup_after_container_migration`）；`ProjectService` 增 `applied_migrations` 并在 init 失败关闭连接。新增 `tests/test_p1_t02_m04.py` **29 项**（旧 schema 打开迁移+保持干净+二次重开/数据保留/无 pending 不动/迁移执行失败保留旧 DB + 备份/备份失败阻断迁移/清理（manifest 重算）失败保留备份；旧归档→staging→安装新目标含 008+干净+重开/空目标/`source_id` 记录与重开保留/非目录·非容器 staging 拒绝/非空非容器目标拒绝/同名冲突建议名+拒绝/异名冲突拒绝/确认覆盖替换+保留 `.pre-replace` 恢复备份/替换 rename 失败还原目标/双重失败点名恢复备份/迁移失败目标不动/版本不兼容目标拒绝/install 创建失败领域错误+目标不动；manifest `source_id` round-trip/可选/非法拒绝）。受影响旧测试子集 282 passed + 全量 pytest **1085 passed**（基线 1056 + M04 29）、Ruff clean、mypy 105 files no issues；手工 smoke 验证 create→export→import→install→open round-trip（project identity/source_id/segment 数据/008 全保留、目标容器干净）。独立 Review 无 BLOCKER；2 SHOULD-FIX（清理顺序先删备份后重算会把容器卡死；install 恢复失败分支未测且双重失败掩蔽原错误）已修复并补 3 项测试，其余 MINOR 记录取舍。 | completed |

| P1-T02-M05 | **跨机器与双形态 Gate：** Reality Check **ADAPT**——M01–M04 全链路 seam 齐备且各段有单测，缺口是 `04` §7"目标机缺凭据时必须给可操作提示"缺应用层只读表面。新增 `adapters/credential_resolvers.py` 模块级 `credential_reference_is_available(reference: str\|None)`（只读判 `env:` 变量存在且非空 / `wincred:` 凭据存在且非空，从不返回 secret；wincred 经新增 `WindowsCredentialResolver._credential_exists` 只查 `CredReadW` 返回 size>0、不把 secret blob 读入内存）与 `application/credential_status.py`（`CredentialStatus` + `report_credential_availability(connections)`：按连接列出引用/available/可操作 hint，跳过无引用连接，永不解析 secret）。新增 `tests/test_p1_t02_m05.py` **26 项**（开放目录 -> `.aiproject` -> 独立临时根模拟"另一台 Windows" -> 安装 -> 重开 round-trip：project identity/manifest `source_id`/segments+008 fidelity carrier/connection 只存引用/profile/translation run/附件字节一致；从已安装目标再导出再导入"机器 C"；旧 schema(001-007)归档在目标机经备份+migration 安装且容器干净；缺 env/wincred credential 报告与可操作 hint（present/absent/空值/None/未知 scheme/secret 不泄）；secret 不入归档与已安装目标、未声明 secret/log 文件不跨机；tamper 矩阵（篡改开放目录拒绝导出；篡改附件/缺附件/篡改 manifest hash/截断/未声明成员/traversal/超限额拒绝导入且 staging 清理；篡改已安装目标拒绝打开）；同名目标冲突建议名+确认覆盖保留 `.pre-replace` 恢复备份；WAL 未提交不跨机）。受影响旧测试子集 384 passed + 全量 pytest **1111 passed**（基线 1085 + M05 26）、Ruff clean、mypy 107 files no issues；独立 Review 无 BLOCKER，1 SHOULD-FIX（空值凭据被误判 available 致目标机静默 401 无提示，改空值/空 blob 判不可用并补测试）与 MINOR 加固（签名 `str\|None`、wincred 存在性检查不读 blob、补 None/未知 scheme/空值用例）已修复，其余 MINOR 记录取舍（报告为应用层纯函数，UI 落地归 P1-T04；`CredentialResolver` protocol 未改只加独立函数）。 | completed |
| P1-T04-M01 | **Project active Profile 选择：** Reality Check **ADAPT**——复用现有 CRUD（不重写），`005`/`006` FK RESTRICT 与 `profile_snapshot` 历史快照 seam 齐备，唯一缺口是 `projects` 无 active 字段、`delete_profile` 遇 RESTRICT 抛不透明 `SqlExecutionError`。新增 `migrations/009_add_project_active_profile.sql`（`projects.active_profile_id` 可空，FK `model_profiles(id) ON DELETE RESTRICT`，`schema_version` 保持 1）；`Project` 加 `active_profile_id`/`ProjectError`/`with_active_profile`；`ProjectRepository` 读写新列 + `list_by_active_profile`；`ProjectService.select_active_profile`/`clear_active_profile`（预检 project/profile 存在给 `ProjectError`）；`ModelProfileService.delete_profile` 删除前预检——被 Project active 引用或历史 `segment_attempts.model_profile_id` 引用 → `ModelProfileInUseError`（区分两类、列出项目名/引用计数），FK RESTRICT 兜底，未引用删除与缺失返回 False 语义不变；`SegmentAttemptRepository.count_by_model_profile`。新增 `tests/test_p1_t04_m01.py` **18 项**（新建默认无 active、select/更换/clear 重开恢复、clear 幂等、跨 Project 隔离、缺失 project/profile 明确错误、仓库缺项目 ValueError、被 Project active 删除明确错误含项目名（两 Project 共享列出双名）、被历史 Attempt 引用删除明确错误、clear 后释放删除保护、未引用可删、DB 级 FK 兜底、旧库(001-008)升级 009 加列 NULL + 升级后可选）。受影响旧测试子集 393 passed + 全量 pytest **1129 passed**（基线 1111 + M01 18，1 skipped）、Ruff clean、mypy 108 files no issues。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX；MINOR 已处理：误导性测试改名+补仓库 ValueError 路径、两 Project 共享列出双名、clear 幂等；其余记录取舍：预检非原子由 FK 兜底、attempt 引用异常类型变化、负 id 文案）。指针推进至 P1-T04-M02。 | completed |
| P1-T04-M02 | **Project-scoped Glossary：** Reality Check **ADAPT**——`04` §3 已列 `glossary_entries` schema、`05` §4/5 已裁决 locked 注入按 priority+稳定次序、`07` §18 硬约束（Project 隔离/source·target 非空/scope·priority 稳定验证/V1.0 只注入 locked）与执行 Agent 自决（同词唯一域/priority 数值范围）齐备，故 **DEC-P1-T04-GLOSSARY 不触发**（priority 语义已裁决+范围已委托；重复词唯一域已委托+要求明确结果且不污染；scope 是用户可编辑分类标签、M03 注入不用 scope 过滤）。新增 `migrations/010_add_glossary_entry.sql`（`glossary_entries`：project_id FK CASCADE、source_term、target_term、scope、priority 0-100 默认 50、is_locked 0/1 默认 0、origin ('user','import')、UNIQUE(project_id, source_term)、索引 (project_id, priority)）；`domain/glossary_entry.py`（`GlossaryEntryError` + `GlossaryEntry`，strip 归一化 + 非空/范围/布尔/受控 origin 校验，非字符串给明确错误）；`infrastructure/repositories/glossary_entry_repository.py`（save/get_by_id/find_by_project_and_source/list_by_project(priority DESC, id)/list_locked_by_project/delete）；`application/glossary_service.py`（`GlossaryService`：create/update/get/list/locked/delete，构造跑 migration，create/update 前预检 project 存在 + 同 Project source_term 重复给明确错误，DB UNIQUE 兜底，`__init__` 迁移失败关闭连接）。新增 `tests/test_p1_t04_m02.py` **36 项**（旧库(001-009)升级 010 建表/数据保留/升级前无表、DB 级 UNIQUE+CHECK 兜底、CRUD+重开+repository 直读、origin='import'、priority/id 排序、update 校验失败不污染、锁定切换、跨 Project 同词隔离与删除隔离、空/非法值、重复策略无污染、repository 直存重复 SqlExecutionError+回滚、缺 Project 预检）。受影响旧测试子集 447 passed + 全量 pytest **1165 passed**（基线 1129 + 36，1 skipped）、Ruff clean、mypy 112 files no issues。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX；MINOR 已处理：非字符串 term 防御 + service `__init__` 迁移失败关连接 + 5 项测试缺口补齐；其余记录取舍：负/零 id 文案、预检非原子由 DB UNIQUE 兜底）。指针推进至 P1-T04-M03。 | completed |
| P1-T04-M03 | **确定性 Prompt 注入：** Reality Check **ADAPT**——`ContextComposer.compose`（P0-T05-M03）已实现 glossary 候选 priority 归一化 1、邻居前选择、Manifest selected/pruned、预算不足抛 `ContextBudgetError` 不截断；`list_locked_by_project`（`is_locked=1 ORDER BY priority DESC, id`）已提供确定稳定次序（M02）；唯一缺口是 `TranslationService.translate_segment` 未传 `glossary_candidates` 且无 `GlossaryEntry → ContextCandidate` 映射。**DEC-P1-T04-GLOSSARY 不触发**（M02 已判定，M03 只接线）。新增 `application/glossary_context.py`（`build_glossary_candidates` 纯函数：content `"{source_term} -> {target_term}"`、`segment_id="glossary:{source_term}"`、metadata 带 entry_id/source/target/scope/priority/origin，共享 `default_estimate` 估算）；`context_composer.py` 私有 `_default_estimate` 公开为 `default_estimate`；`TranslationService` 增持 `GlossaryEntryRepository`，`translate_segment` 查询 `run.project_id` 的 locked entries 传入 `compose(glossary_candidates=...)`，`close()` 关闭新 repository；不新建 Service/Manager、不新增依赖/schema、不改 composer 选择规则。新增 `tests/test_p1_t04_m03.py` **12 项**（映射单测 6：映射/保序/空/默认估算/自定义估算/元数据；注入垂直闭环 3：locked 按 priority+id 稳定次序先于邻居 + 跨 Project 隔离 + unlocked 不泄漏 + Manifest selected_sources/pruned_count、同 priority id 决胜、无 locked 不注入回归；预算行为 2：低优先级 glossary 裁剪且当前源文不截断 + 超预算当前段 `ContextBudgetError` 不发请求；composer 同 priority 平局 1：glossary 先于 distance-0 邻居）。受影响旧测试子集 **344 passed**（332 旧 + 12）+ 全量 pytest **1177 passed**（基线 1165 + 12，1 skipped）、Ruff clean、mypy 114 files no issues。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX；MINOR 已处理：补同 priority 平局测试；其余记录取舍：glossary 归一化 priority 1 依赖稳定排序、条目真实 priority 仅存 metadata）。指针推进至 P1-T04-M04。 | completed |
| P1-T04-M04 | **薄管理 UI：** Reality Check **ADAPT**——`ProviderConnectionService.delete_connection` 删除前预检，被任一 profile 引用抛新增 `ProviderConnectionInUseError` 并列出 profile 名（镜像 M01 `delete_profile` 预检先例，DB FK RESTRICT 兜底，缺失返回 False 语义不变）；UI 落地于既有 `SettingsPage`（Connection/Profile 删除 + 凭据可用性提示经 `report_credential_availability`，缺引用显示可操作 hint 永不泄 secret）与 `ProjectPage`（项目选择 `select_project`、active Profile 选择/清除、Glossary 增改删/lock/priority/scope），沿用 `ServiceWorker`+`WorkerPage`，UI 不直接查库。新增 `tests/test_p1_t04_m04.py` **15 项**（active Profile 设置/清除/重开恢复、Glossary round-trip 与重复/空术语可操作错误、仅 locked 注入、被引用 Profile/Connection 删除可操作错误、未引用删除成功、缺凭据 hint、secret 不显、UI 模块不导入 repositories/sqlite 结构性守卫、`ProviderConnectionInUseError` 服务层单测、Add-Profile 连接下拉刷新保留选择）。受影响旧测试子集 185 passed + 全量 pytest **1192 passed**（基线 1177 + M04 15，1 skipped）、Ruff clean、mypy 115 files no issues；独立 Review **APPROVE-WITH-MINORS** 无 BLOCKER/SHOULD-FIX 见 `08` §8。指针推进至 P1-T04-M05。 | completed |
| P1-T04-M05 | **持久化/双形态/GUI Gate：** Reality Check **ADAPT**——验证型 Gate，唯一改动为新增 `tests/test_p1_t04_m05.py` **2 项**（双形态 round-trip 与同形态重启重开：`active_profile_id`/`glossary_entries` 全字段/`list_locked_entries`/priority DESC 稳定次序/凭据只存 reference/segments 保留），补齐 P1-T02-M05 round-trip 早于 P1-T04 未覆盖的 `009`/`010` 新增数据持久化证据；重启/Project 隔离/预算/UI 回归由 M01–M04 套件 + 全量 pytest 覆盖。受影响旧测试子集 187 passed（185 旧 + M05 2）+ 全量 pytest **1194 passed**（基线 1192 + 2，1 skipped 因本环境无符号链接创建权限）、Ruff clean、mypy 116 files no issues；独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX，实测删除 `select_active_profile` 或 glossary 创建即断言失败证明不误报通过；MINOR 已处理：active profile 按 id 精确断言；其余记录取舍）。指针推进至 P1-T03-M01。 | completed |
| P1-T03-M01 | **自动模式最小旅程：** Reality Check **ADAPT**——active Profile（`009`）/内置只读 Workflow/ImportService 六格式/`TranslationWorker` 循环/`TxtExporter`/双形态持久化 seam 齐备；缺口是 mode 缺失、自动模式无解析 active Profile 路径、缺配置无引导；DEC-P1-T03-OVERRIDE 不触发（属 M03）。新增 `011_add_project_mode.sql`（`projects.mode` `NOT NULL DEFAULT 'auto'` + `CHECK ('auto','workbench')`，`schema_version` 保持 1，旧行回填）；`Project` 加 `MODE_AUTO`/`MODE_WORKBENCH`/`SUPPORTED_MODES`/`mode`/`with_mode()`（非法 `ProjectError`）；`ProjectRepository` 读写新列；`ProjectService.set_mode`/`get_project`；`TranslationWorker` 加 `start_translate_auto`/`config_missing`（worker 内解析 active Profile，缺 active 发 `config_missing` 不建 Run/不发请求，异常兜底发 `failed`，否则复用既有 `translate` 循环——不复制 orchestrator）；`TranslationPage` 移除手动 Profile 组合框，Translate 走自动解析，加 mode/active-profile 状态标签 + 缺配置可操作引导（"设置 active Profile…"→ `request_project_setup` → 切 Project Tab），active Profile 解析后自动隐藏引导；`ProjectPage` 加 `project_settings_changed`（active 变更后刷新 Translation 页）。新增 `tests/test_p1_t03_m01.py` **15 项**（默认 mode/set_mode 重开持久化/非法与缺失拒绝/旧库(001-010)升级回填+数据保留/DB CHECK 兜底/双形态 round-trip 保留 mode+active/自动旅程 import→translate→progress→export/无 pending 重翻终态/缺 active → config_missing+无 Run+引导一键跳转/恢复后引导清除/缺 project → config_missing/服务异常 → failed 不卡 UI/窗口重启后 mode+active 仍解析可翻译）；`test_p0_t08_m05.py` helper 更新（设置 active Profile——自动模式行为变化）。受影响旧测试子集 **83 passed**（68 旧 + M01 15）+ 全量 pytest **1209 passed**（基线 1194 + 15，1 skipped 因本环境无符号链接创建权限）、Ruff clean、mypy 117 files no issues；独立 Review 首轮 **REQUEST-CHANGES**（2 SHOULD-FIX：引导未自动清除、`translate_auto` 异常未兜底会卡 UI）已修复并各补测试，零 pending 终态修复补测试，复核通过。指针推进至 P1-T03-M02。 | completed |
| P1-T03-M02 | **capability-aware 工作台：** Reality Check **ADAPT**——active Profile（`009`）/capability snapshot（`get_capability`/`supported_parameters`/`filter_params` 终滤）/`TranslationWorker` 循环/`WorkerPage` pytest-qt seam 齐备；缺口是 `translate_segment` 无参数透传、Attempt 快照无实际请求参数、无按文档 Attempt 只读、无工作台 UI 面；DEC-P1-T03-OVERRIDE 不触发（属 M03）。新增 `application/workbench.py`（`SegmentProgress`/`ParamSpec`/`presented_parameters`/`initial_param_values`，Qt-free：只呈现 capability 支持参数、未知参数文本回退、`max_tokens` 钳制 capability.max_output_tokens）；`translate_segment(extra_params=None)`（缺省 profile.default_params）+ `start_attempt(request_params)`（快照于 `profile_snapshot.request_params`，JSON 追加键无 migration）；`SegmentAttemptRepository.list_by_document` + `TranslationRunService.list_segment_progress`（每 Segment 最新 Attempt 的 status/error + current_revision）；并发加固 `_seed_builtin_workflow`（refresh 与 translate 两线程并发构造时唯一冲突重读校验）；`TranslationWorker.start_translate_workbench`（复用既有 `translate` 循环）；`ui/workbench.py` `WorkbenchParamEditor`（float/int/bool 类型化 + 未知文本 + `max_tokens` 上限钳制 + `initial` 草稿保留）；`TranslationPage` 工作台面（真实进度/Profile+capability 摘要/参数编辑器，auto 模式不变，`set_document` 触发 refresh，`_on_finished` 工作台模式重读进度并保留草稿）。新增 `tests/test_p1_t03_m02.py` **20 项**（参数透传+快照、参数变更只影响下一 Attempt 不改历史 Revision、缺省 default_params、真实 adapter 过滤不支持参数、`list_segment_progress` 无 Attempt/成功+失败/repair 多 Attempt 取最新、纯参数策略、编辑器只呈现支持参数+max_tokens 编辑钳制+未知文本+`initial`、pytest-qt 工作台显示真实进度+Profile+仅支持参数+翻译用编辑器值+快照+完成后进度刷新且草稿保留+auto 隐藏工作台+无 active 引导）。受影响旧测试子集 **225 passed** + 全量 pytest **1229 passed**（基线 1209 + M02 20，1 skipped 因本环境无符号链接创建权限）、Ruff clean、mypy 120 files no issues；独立 Review **无 BLOCKER**，1 SHOULD-FIX（max_tokens 编辑超 capability 上限）已修复并补测试，2 MINOR（完成后进度列表不刷新——已修 `_on_finished` 重读且保留草稿；多 Attempt 每段选择未测——已补 repair 测试）处理；另对 M01 既有 `test_auto_translate_service_error_emits_failed` 做测试加固（先连接 `finished` 再 emit）。指针推进至 P1-T03-M03。 | completed |
| P1-T03-M03 | **受控 Prompt Override：** Reality Check **ADAPT**——M02 落地与 Prompt 模板 seam（`PromptRenderer.render(template=...)`/`RenderedPrompt`/`template_version`/`output_contract`，`test_p0_t05_m04` 已证未知变量拒绝 + 输出契约不可移除）齐备；缺口是预设模板无稳定身份/版本、override 无持久化/校验/渲染接入；DEC-P1-T03-OVERRIDE **不触发**（编辑边界/拒绝规则/父版本失效策略已由 `01` §16、`05` §2/§9、`07` §19 与 M03 目标文本裁决，余为可逆内部实现，先例同 P1-T04-M02/M03）。新增 `application/preset_templates.py`（`general` v1.0.0 预设注册表 = 既有默认模板文本，`ALLOWED_PLACEHOLDERS` 与 renderer 占位符对齐，`resolve_override_template` 父版本错配/持久化文本失效 fail-closed 拒绝）、`domain/prompt_override.py`（Qt-free 校验：未知变量/非法 `$` 语法/空文本拒绝，`string.Template.is_valid()`+`get_identifiers()` 纯替换不执行代码）、`migrations/012_add_prompt_override.sql`（`prompt_overrides` 1:1 with profile，`UNIQUE(model_profile_id)`，FK `model_profiles(id) ON DELETE CASCADE`，`schema_version` 保持 1）、`PromptOverrideRepository`、`ModelProfileService.set/get/clear_prompt_override`（保存时 parent = 当前预设版本，未知变量拒绝不持久化）、`PromptRenderer` 默认模板改从预设读取（行为不变）、`TranslationService.translate_segment` 有 override 解析渲染（失效先于 claim 拒绝）+ Attempt `context_summary` 追加 `override_parent_version`、`ui/workbench.py` `WorkbenchPromptEditor`（只读预设预览 + 可编辑拷贝 + Save/Clear + 草稿保留 + stale 警告）、`TranslationPage` 工作台 prompt 面（save/clear 经 worker 线程，错误经 status 显示）。新增 `tests/test_p1_t03_m03.py` **38 项**。受影响旧测试子集 **322 passed** + 全量 pytest **1267 passed**（基线 1229 + M03 38，1 skipped 因本环境无符号链接创建权限）、Ruff clean、mypy 124 files no issues；独立 Review **无 BLOCKER**，2 SHOULD-FIX（stale override UI 不可见、字面 `$` 报错无 `$$` 引导）已修复并补测试，见 `08`。指针推进至 P1-T03-M04。 | completed |
| P1-T03-M04 | **人工 Revision 与锁定：** Reality Check **ADAPT**——`TranslationRevision` 已有 `origin ∈ {ai,user,import}` + `is_locked`（`006` 即定义，**无需新 migration**）、`finalize_success` 已有"current 未变 + 非 locked" fencing、`recover_expired_leases` 对 locked current 转 completed 不重建、exporter 默认读 current + `revision_overrides` 显式选择；唯一缺口是追加 user Revision/lock/unlock/切换 current 的 use case 与 UI 编辑面。新增 `TranslationRunService.append_user_revision`（单事务 INSERT origin=user Revision + `UPDATE segments SET status='completed', current_revision_id=?, version=version+1 ... WHERE status != 'processing'`，processing 段拒绝、version bump 使 stale claim 失效）、`set_current_revision`（切换任一属于该 segment 的有效 revision，拒绝 processing 并校验归属）、`lock_current_revision`/`unlock_current_revision`（单条 `UPDATE ... WHERE id=(SELECT current_revision_id FROM segments WHERE id=?)` 原子切换 `is_locked`）；`SegmentProgress` 增 `revision_text`/`revision_locked`（`list_segment_progress` 经 `TranslationRevisionRepository.get_many_by_ids` 填充，向后兼容）；`ui/workbench.py` 新增 `WorkbenchRevisionEditor`（只发信号：source 只读 + 译文可编辑 + Save/Lock/Unlock）、`TranslationPage` 工作台编辑面（选中重建、Save/Lock/Unlock 经 ServiceWorker 线程调 service、刷新恢复选中并 `initial` 保留草稿）。锁定保护由既有 seam 兜底（finalize 拒绝 locked current 的迟到结果、recover 转 completed 不重建、auto worker 只翻译 pending 段）——自动迟到结果和重译不能覆盖 locked current；导出遵循选择语义。新增 `tests/test_p1_t03_m04.py` **21 项**（append/set/lock/unlock use case、finalize 拒绝 locked、auto 重译跳过 locked 段无新 request、recover locked 转 completed/unlocked 回 pending、导出默认 locked current + override 选历史、pytest-qt 编辑保存/lock-unlock/草稿跨 refresh 保留）。受影响旧测试子集 **413 passed** + 全量 pytest **1288 passed**（基线 1267 + M04 21，1 skipped 因本环境无符号链接创建权限）、Ruff clean、mypy 125 files no issues；独立 Review **无 BLOCKER**，1 SHOULD-FIX（草稿保留死代码——`set_revision` 无条件覆盖 `initial`）已修复并补测试，MINOR 已处理（lock TOCTOU 原子化、补 recover-unlocked-user-current 边界测试），MINOR 记录取舍（UI 暂无历史 revision 切换入口），见 `08`。指针推进至 P1-T03-M05。 | completed |
| P1-T03-M05 | **模式切换与 GUI Gate：** Reality Check **ADAPT**——M01–M04 落地与 seam 齐备，唯一缺口是模式切换 UI 无调用方、运行中切换无提示、无文档列表 + 切换 Project 保留旧 `_document_id`（M01 已知边界）、重启后文档/结果/锁定证据缺位。新增 `TranslationPage` 交互模式选择器（`QComboBox` Auto/Workbench，经 `ServiceWorker` 调 `ProjectService.set_mode` 持久化；运行中激活拒绝并提示 finish or Cancel、选择器恢复——运行中 Run 不原地改变 Profile/Workflow/参数；`set_mode` 成功路径同步 `_mode` 防 refresh 失败失同步）与每 Project 文档选择器（`TranslationRunService.list_source_documents` 只读转发 `SegmentRepository.list_source_documents_by_project`，UI 经 Application Service 不违反结构性守卫；提交时捕获 `current`/`project_id` 无跨线程读；有效文档属该 Project 则沿用否则清空，消除跨 Project 陈旧 `_document_id`；无有效文档组合框不高亮首项；`_on_document_selected` 重读进度、运行中切文档同样被拒）；`_set_running` 集中管理 `_running` 与 Translate/Cancel/Export/文档选择器按钮态；取消/关闭/重启门禁复用既有 seam（`request_stop` Segment 边界取消、closeEvent 有界 wait 收敛、共享 SQLite 重开自动恢复 mode/active/文档/结果/锁定）。新增 `tests/test_p1_t03_m05.py` **10 项**（`list_source_documents` 按 Project 有序/隔离；模式切换持久化 + 工作台面显隐 + 选择器同步、运行中切换拒绝并恢复、无 Project 切换报错 + 选择器恢复、workbench 无 active Profile 引导（主线程同步、无 Run 无请求）、workbench 取消 + Run cancelled、workbench 关闭收敛无 processing + Run cancelled + Attempt 全终态、重启保留 mode/active/文档选择器/翻译结果/锁定 Revision、跨 Project 切换重置文档上下文 + 组合框重选加载、运行中文档切换被拒绝 + 选择器锁定）。受影响旧测试子集 **461 passed** + 全量 pytest **1298 passed**（基线 1288 + M05 10，1 skipped 因本环境无符号链接创建权限）、Ruff clean、mypy 126 files no issues；实际可用性 smoke（真实窗口 + 完整旅程驱动：建 Project/active Profile/导入/切 workbench/文档选择/workbench 翻译/切回 auto/窗口关闭收敛）SMOKE PASS；独立 Review **无 BLOCKER**，2 SHOULD-FIX（运行中文档切换无守卫——选择器运行中禁用 + `_on_document_selected` 拒绝；set_mode 成功但 refresh 失败 UI/DB 失同步——成功路径同步 `_mode`）已修复并补测试，MINOR 已处理（组合框无有效文档不高亮首项、`_on_translate` 加 `_running` 守卫、refresh None 清空陈旧文档状态），MINOR 记录取舍（Project 切换后不自动选中文档，需显式选择；workbench 缺 active 主线程同步短路由），见 `08`。P1-T03 全部 M01–M05 完成，Task 标记 **completed**。指针推进至 P1-T05-M01。 | completed |
| P1-T05-M01 | **冻结可判定矩阵：** Reality Check **FIT**——新增 `03` §12（判定原则/支持环境 Windows 11 build≥22000 x64/依赖锁定/质量命令表/公共矩阵规范/P0-P1 release-blocking 等级）+ `requirements.lock`（pip freeze 快照 24 项，排除 transrealm/pip，与 pyproject 一致且等于安装版本）+ `tests/test_p1_t05_m01.py` 7 项（锁存在/格式/排除、锁==安装、锁满足 pyproject runtime+dev 范围、工具链入锁、支持环境 Windows 11 build≥22000+x64+py3.12）。全量 pytest **1305 passed**（基线 1298 + 7，1 skipped 本环境无符号链接创建权限）、Ruff clean、mypy 127 files no issues；独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER；1 SHOULD-FIX Windows 11 build 判定已修复；MINOR 2/4 已处理、3/5 记录取舍）见 `08`；DEC-P1-T05-RELEASE 评估**不触发**（打包工具 PyInstaller 已 smoke 验证并定首选、支持环境唯一确定、lockfile 形式可逆）。**解除条件：P1-T05 前置"工作树先形成可恢复 Git 基线"未满足（governance_commit_pending，需用户授权 commit），M02 由此 blocked。** | completed |

Agent 可以在实现中细化这些小目标，但不得扩大当前 Task 的范围。

## 6. 每次更新本文件的格式

完成或中断小目标时更新以下内容，不新增流水账文件：

### Last code checkpoint

- **时间：** 2026-08-08
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T05-M01 冻结可判定矩阵（Reality Check **FIT**）。现场核验（基线：全量 pytest 1298 passed/1 skip、Ruff clean、mypy 126 files 复现一致）：P1-T03/P1-T04 全链路落地（migrations 001–012 齐全、`set_mode`/`translate_auto`/`translate_workbench`/`list_segment_progress`/`list_source_documents_by_project` 等 seam 存在）；P1-T05 前置依赖中 P1-T03/P1-T04 已完成，但**"工作树先形成可恢复 Git 基线"未满足**（`governance_commit_pending`，需用户授权 commit）。**DEC-P1-T05-RELEASE 判定不触发**——打包工具 PyInstaller 已由 P0-T08-M06 build/start smoke 验证可行且 `03` §10 确定首选（`07` §20 参考方案授权"可验证的实现选择"）；支持环境 `01` §21.1/`02` Release Gate/`07` §20 唯一确定"Windows 11 x64（当前环境 AMD64）"，扩展 ARM64 才需 DECISION_REQUIRED（本 M01 不扩展）；lockfile 形式（pip freeze 快照）是可逆内部实现选择（`09` §7"不得把普通实现分歧升级为节点"）。**FIT（计划与现场一致，无内部调整）**：(1) `03` §12 新增 V1.0 Release Gate 冻结矩阵——判定原则（布尔 Gate、不把"执行过"当通过、已知失败/未覆盖如实记录）、支持环境（Windows 11 build≥22000 x64、Python 3.12、绿色版可写数据目录、清理不静默删外置数据）、依赖锁定（`requirements.lock` 更新规则、候选可定位性 = Git 基线 + 锁）、质量命令表（pytest/ruff/mypy/verify_task.py/check_candidate_hygiene.py 期望状态与证据位置；Git 基线形成后 verify_task/hygiene 才生效）、公共矩阵规范（fixture 由各 M 定义、允许重复调用、零数据损失、证据位置）、P0/P1 release-blocking 等级；(2) 生成 `requirements.lock`（`pip freeze` 快照排除 `transrealm`/`pip`，24 项：PySide6 6.11.1、pytest 9.1.1、pytest-qt 4.5.0、ruff 0.16.0、mypy 2.3.0、pyinstaller 6.21.0 等）；(3) 新增 `tests/test_p1_t05_m01.py` 7 项矩阵可重复性基座（锁存在/格式/排除、锁==安装、锁满足 pyproject runtime+dev 范围、dev/打包工具链入锁、支持环境 Windows 11 build≥22000+x64+py3.12）。Review 1 SHOULD-FIX（环境断言原只判 `system=='Windows'` 无法区分 10/11，补 `sys.getwindowsversion().build >= 22000`）与 MINOR 2（dev 工具范围补校验）、MINOR 4（`03` §12 的 `08 §8` 引用改为落地取舍记录）已修复；MINOR 3/5 记录取舍。
- **修改文件：** `03_Technical_Design.md`（§12 V1.0 Release Gate 冻结矩阵）；`requirements.lock`（新增）；`tests/test_p1_t05_m01.py`（新增 7 项）；权威文档 `07` §20（P1-T05 状态 in_progress、M01 completed 证据）、`08`（M01 取舍 + DEC 判定 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`（全量）；`py -3.12 -m pytest -q tests/test_p1_t05_m01.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`；`py -3.12 scripts/check_candidate_hygiene.py`；`py -3.12 -m pip freeze`
- **测试结果：** 全量 pytest **1305 passed**（基线 1298 + M01 新增 7，1 skipped 因本环境无符号链接创建权限 `test_p1_t02_m01.py:717`）；Ruff All checks passed；mypy Success: no issues found in 127 source files；check_candidate_hygiene.py exit 0（Candidate hygiene passed）。
- **未完成：** P1-T05-M02 未开始（当前指针，state **blocked**——P1-T05 前置"工作树先形成可恢复 Git 基线"未满足，需用户授权 commit 才能开始）。
- **风险/阻塞：** P1-T05 前置 Git 基线未形成（`governance_commit_pending`，全部 P1-T01..T05-M01 改动未提交）。这同时使候选门禁 `scripts/verify_task.py`（`git diff base...HEAD`）对 P1-T05 改动失效、`09` §6"发布候选仅能来自已验证、可定位的基线"不满足。**解除条件：用户授权 commit 形成 Git 基线（或明确允许在未提交工作树继续 M02）。** 无其他技术阻塞。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER；1 SHOULD-FIX Windows 11 build 判定已修复；MINOR 已处理：dev 工具范围校验、`08 §8` 引用修正；MINOR 记录取舍：单向漂移检测、`test_lock_file_exists` 冗余但含命令提示保留）。DEC-P1-T05-RELEASE 评估不触发（见 `08`）。工作树改动未提交，未获授权不 commit/push。
- **恢复动作：** 读取本文件；当前小目标 P1-T05-M02（翻译/恢复/长文本 Gate），状态 **blocked**（解除条件 = 用户授权 Git 基线 commit 或明确允许在未提交工作树继续）。授权后先完成 Reality Check：按 `01` §17/20/21、`02`、`03` §3/6/8/10/11/12、`07` §20 M02、`08` 核验，记录 FIT/ADAPT/REPLAN，从最小崩溃切点/长文本垂直闭环开始，不越出 P1-T05；M02 依赖 M01 已冻结的 `requirements.lock`/`03` §12 矩阵。

### Previous code checkpoint

- **时间：** 2026-08-08
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T03-M05 模式切换与 GUI Gate（Reality Check **ADAPT**）。现场核验（基线：受影响旧测试子集 413 passed、全量 1288 passed，复现 1288/1 skip 现场健康）：M01–M04 落地与 seam 齐备——`projects.mode`/`011`/`ProjectService.set_mode/get_project`、`TranslationWorker.translate_auto`/`translate_workbench` 共用 `translate` 循环 + `request_stop` 取消 + `finish_run(cancelled)`、`MainWindow.closeEvent` `_shutdown_workers` request_stop + 有界 wait、`list_segment_progress`、`SegmentRepository.list_source_documents_by_project`；**唯一缺口**：模式切换 UI 不存在（`set_mode` 无调用方）、运行中切换无提示、TranslationPage 无文档列表且切换 Project 后保留旧 `_document_id`（M01 已知边界）、重启后文档/结果/锁定证据缺位。DEC-P1-T03-OVERRIDE 不触发（属 M03 已裁决）。**ADAPT（范围内可逆调整，目标/硬约束/验收不变，不创建第二套 orchestrator/状态机）**：(1) `TranslationPage` 新增交互模式选择器（Auto/Workbench `QComboBox`）——经 `ServiceWorker` 调 `ProjectService.set_mode` 持久化 `projects.mode`；运行中（`_set_running` 集中管理 `_running` 与 Translate/Cancel/Export/文档选择器按钮态）激活切换被拒绝并提示"finish or Cancel"、选择器恢复当前持久化 mode（运行中 Run 不原地改变 Profile/Workflow/参数，满足硬约束）；`set_mode` 成功路径同步 `_mode` 再 refresh（refresh 失败不致 UI/DB 失同步）；(2) 新增每 Project 文档选择器——`TranslationRunService.list_source_documents(project_id)`（只读转发 `SegmentRepository.list_source_documents_by_project`，UI 经 Application Service 不违反结构性守卫）供 `_refresh_task` 填充组合框；`_refresh_task` 提交时捕获 `current`/`project_id`（无跨线程 UI 读），有效文档属该 Project 则沿用否则清空（不做自动选中，消除跨 Project 陈旧 `_document_id`；无有效文档组合框 `setCurrentIndex(-1)` 不高亮首项）；`_on_document_selected` 重读进度、运行中切文档同样被拒；(3) 取消/关闭/重启门禁复用既有 seam（`request_stop` Segment 边界取消、closeEvent 有界 wait 收敛、共享 SQLite 载体重开自动恢复 mode/active/文档/结果/锁定）。
- **修改文件：** `src/transrealm/ui/pages.py`（TranslationPage 模式选择器 + 文档选择器 + `_set_running`/`_on_mode_switch`/`_sync_mode_switch`/`_set_mode_task`/`_on_document_selected`/`_restore_document_selector`/`_populate_documents`、`_refresh_task` 返回 6 元组、`_handle_action` refresh/set_mode 分支、`_handle_error` 同步选择器）；`src/transrealm/application/translation_run_service.py`（`list_source_documents`）；`tests/test_p1_t03_m05.py`（新增 10 项）；权威文档 `03` §10（P1-T03-M05 段落）、`07` §19（M05 completed 证据、Task 状态 completed）、`08`（M05 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_model_profile.py tests/test_provider_connection.py tests/test_p0_t05_m04.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m03.py tests/test_p1_t04_m04.py tests/test_p1_t04_m05.py tests/test_p1_t03_m01.py tests/test_p1_t03_m02.py tests/test_p1_t03_m03.py tests/test_p1_t03_m04.py tests/test_p1_t01_m01.py tests/test_txt_parser.py tests/test_p1_t03_m05.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`；实际可用性 smoke：`py -3.12 /tmp/smoke_m05.py`
- **测试结果：** 全量 pytest **1298 passed**（基线 1288 + M05 新增 10，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **461 passed**（含 M05 新增 10）；Ruff All checks passed；mypy Success: no issues found in 126 source files；实际可用性 smoke SMOKE PASS（真实窗口启动 + 驱动完整旅程：建 Project/active Profile/导入/切 workbench/文档选择/workbench 翻译/切回 auto/窗口关闭收敛）。
- **未完成：** P1-T05-M01 未开始（当前指针，ready + pending_reality_check；P1-T05 前置"工作树先形成可恢复 Git 基线"未执行，未获授权不 commit）。
- **风险/阻塞：** 无技术阻塞。独立 Review **无 BLOCKER**，2 SHOULD-FIX 已修复并补测试：(a) 运行中切换文档无守卫——workbench 运行 doc A 时选中 doc B 会把 A 的进度写进共享进度条、结束后 `_on_finished` 对 B 刷新使 A 结果从页面消失；修复为 `_set_running` 运行中禁用文档选择器 + `_on_document_selected` 运行中拒绝并恢复、提示"finish or Cancel"，补 `test_document_switch_refused_while_running`；(b) set_mode 成功但后续 refresh 失败时 UI/DB 失同步——`_handle_error` 按旧 `_mode` 恢复选择器而 DB 已写新模式；修复为 set_mode 成功路径直接 `self._mode = result.mode` + 同步选择器再 refresh。MINOR 已处理：(1) 组合框无有效文档时 `setCurrentIndex(-1)` 不高亮首项（避免"高亮但未加载"矛盾）；(2) `_on_translate` 加 `_running` 守卫（程序化二次调用不进入）；(3) refresh None（Project 缺失）清空 `_document_id`/`_document_name`/进度。MINOR 记录取舍：Project 切换后不自动选中文档（用户从选择器显式选择，符合"不做自动选中"以消除跨 Project 泄漏的取舍）；workbench 缺 active Profile 在主线程同步短路由（`_active_profile_id` 来自 refresh），与 auto 的 worker 长路由（`translate_auto` 解析）语义一致但路径不同；运行中跨 Tab 切 Project 仍可使页面上下文变化（在途 Run 的 DB 结果安全、仅页面视图受影响，文档选择器锁定已防可见切换）。已知边界/回归修复：`_refresh_task` 改为"提交时捕获 current、不做自动选中文档"后，M01 `test_auto_mode_works_after_window_reopen` 原 1/5 波动（自动选中使 `_document_id` 提前置位致测试 wait 短路、翻译先于导入完成、迟到 `set_document` 重置进度条）已消除（8/8 稳定）。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T05-M01（冻结可判定矩阵），状态 ready，plan_alignment pending_reality_check。开工前先核验 P1-T03/P1-T04 全链路落地与 P1-T05 前置（`07` §20：工作树先形成可恢复 Git 基线——该 commit/push 需用户授权；固定版本/依赖锁/Windows 11 环境/fixture/命令/期望状态/release-blocking 等级），按 `01` §17/20/21、`02` Phase 1、`03` §3/6/8/10/11、`05`、`07` §20 核验后记录 FIT/ADAPT/REPLAN，从最小"冻结可判定矩阵"开始，不越出 P1-T05。

### Previous code checkpoint

- **时间：** 2026-08-08
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T03-M04 人工 Revision 与锁定（Reality Check **ADAPT**）。现场核验（基线：受影响旧测试子集 322 passed、全量 1267 passed，复现 360/1286 现场健康）：M03 落地（`preset_templates`/`PromptOverride`/`012`/`set_prompt_override`/`resolve_override_template`/`WorkbenchPromptEditor`）与 Revision/lock seam（`TranslationRevision.origin ∈ {ai,user,import}` + `is_locked` 在 `006` 即定义——**无需新 migration**；`finalize_success` 已在自动完成前校验"expected current 未变 + 非 locked"；`recover_expired_leases` 对 locked current 的 processing 段转 completed 不重建；exporter 默认读 `current_revision_id` + `revision_overrides` 显式选择并逐项校验归属）齐备；**唯一缺口**：无追加 user Revision/lock/unlock/切换 current 的 use case、无工作台译文编辑面。**ADAPT（范围内可逆调整，目标/硬约束/验收不变，不创建第二套 orchestrator/状态机）**：(1) `TranslationRunService` 新增 `append_user_revision`（单事务 INSERT origin=user Revision + `UPDATE segments SET status='completed', current_revision_id=?, version=version+1 ... WHERE status != 'processing'`——processing 段明确拒绝、version bump 使捕获的 stale claim 失效、rowcount 0 即回滚无孤儿 Revision）、`set_current_revision`（切换到任一属于该 segment 的有效 revision，拒绝 processing 并校验归属）、`lock_current_revision`/`unlock_current_revision`（单条 `UPDATE ... WHERE id=(SELECT current_revision_id FROM segments WHERE id=?)` 原子切换 `is_locked`，避免读-写窗口锁到陈旧 revision）；(2) `application/workbench.py` `SegmentProgress` 增 `revision_text`/`revision_locked`（带默认值向后兼容），`list_segment_progress` 经新增 `TranslationRevisionRepository.get_many_by_ids` 批量填充（无 N+1）；(3) `ui/workbench.py` 新增 `WorkbenchRevisionEditor`（只发信号不碰 DB：source 只读 + 译文可编辑 + Save/Lock/Unlock + `initial` 草稿保留），`TranslationPage` 工作台接入（`_segment_progress` 选中 → `_rebuild_revision_editor`，Save/Lock/Unlock 经 ServiceWorker 线程调 service、`_handle_action` 刷新，`_populate_workbench` 记录选中 segment 并在刷新后恢复、`initial` 保留未保存草稿跨 refresh）。锁定/人工 current 的自动覆盖保护由既有 seam 兜底：finalize fencing（locked/current 已变拒绝迟到结果）、recover 对 locked current 转 completed 不重建、auto worker 只翻译 pending 段（append/set 置 completed 后不再被 claim）——自动迟到结果和重译不能覆盖 locked current；导出遵循选择语义（P0-T08-M04 既有，锁定不改变导出）。
- **修改文件：** `src/transrealm/application/translation_run_service.py`（`append_user_revision`/`set_current_revision`/`lock_current_revision`/`unlock_current_revision`/`_set_current_locked` + `list_segment_progress` 填充 revision_text/revision_locked）；`src/transrealm/application/workbench.py`（`SegmentProgress` 增 `revision_text`/`revision_locked`）；`src/transrealm/infrastructure/repositories/translation_revision_repository.py`（`get_many_by_ids`）；`src/transrealm/ui/workbench.py`（新增 `WorkbenchRevisionEditor`）；`src/transrealm/ui/pages.py`（TranslationPage 工作台编辑面 + 选中恢复/草稿保留/action 分支）；`tests/test_p1_t03_m04.py`（新增 21 项）；权威文档 `03` §10（P1-T03-M04 段落）、`07` §19（M04 completed 证据、Task 状态 M04 completed/M05 pending）、`08`（M04 取舍 + DEC 判定 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_model_profile.py tests/test_provider_connection.py tests/test_p0_t05_m04.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m03.py tests/test_p1_t03_m01.py tests/test_p1_t03_m02.py tests/test_p1_t03_m03.py tests/test_p1_t01_m01.py tests/test_txt_parser.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1288 passed**（基线 1267 + M04 新增 21，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **413 passed**（不含 M04 新增 21）；Ruff All checks passed；mypy Success: no issues found in 125 source files。
- **未完成：** P1-T03-M05 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review **无 BLOCKER**，1 SHOULD-FIX 已修复并补测试：草稿保留是死代码——`WorkbenchRevisionEditor.__init__` 先 `setPlainText(initial)` 但随后 `set_revision` 无条件 `setPlainText(revision_text or "")` 覆盖，使页面传的 `initial=revision_draft` 完全无效（刷新时未保存编辑被持久化文本覆盖）；修复为 `set_revision(..., initial=...)` 内以 `initial` 优先，补 `test_workbench_revision_draft_preserved_across_refresh`。MINOR 已处理：(a) lock TOCTOU——`_set_current_locked` 原在事务外读 `current_revision_id` 再写 is_locked，期间并发 finalize 可能使锁落在历史 revision；改为单条 `UPDATE ... WHERE id=(SELECT current_revision_id FROM segments WHERE id=?)` 原子锁定 current；(b) 补 `test_recover_returns_unlocked_user_current_to_pending` 固化崩溃/损坏现场边界（未锁 user current 段异常 processing → recover 回 pending 可重译，正常流不可达因 append 置 completed）。MINOR 记录取舍：工作台 UI 暂无"从历史 revision 切换 current"入口（`set_current_revision` 服务层可用且有测试，Save 编辑即追加 user Revision 并成为 current；选择历史 AI 结果作 current 的 UX 归 M05 或后续，符合所列 UI 范围）。已知边界：(1) 追加空文本 user Revision 允许（与 `finalize_success` 不拒空译文一致），UI 层未强制非空（最小实现）；(2) 锁定不改变导出——导出遵循选择语义（默认 current / override 显式历史），锁定只保护"不被自动覆盖"。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T03-M05（模式切换与 GUI Gate），状态 ready，plan_alignment pending_reality_check。开工前先核验 M04 落地（`append_user_revision`/`set_current_revision`/`lock_current_revision`/`unlock_current_revision`、`WorkbenchRevisionEditor`、`SegmentProgress` revision_text/revision_locked）与 M01–M03 全链路（mode 持久化/自动模式、capability 工作台参数、Prompt override）、P0-T08 worker/关闭收敛与 T07 lease，按 `01`、`03` §3/§10、`05` §2/8/9、`07` §19 P1-T03-M05 核验后记录 FIT/ADAPT/REPLAN，从最小"自动/工作台模式切换 + 运行中取消 + 重启不丢配置/结果"垂直闭环开始，不越出 P1-T03。

### Previous code checkpoint

- **时间：** 2026-08-08
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T03-M03 受控 Prompt Override（Reality Check **ADAPT**）。现场核验（基线：受影响旧测试子集 225 passed、全量 1229 passed）：M02 落地（`WorkbenchParamEditor`/`translate_segment extra_params`/`start_attempt request_params` 快照/`list_segment_progress`/`start_translate_workbench`）与 Prompt 模板 seam（`PromptRenderer.render(template=...)`/`RenderedPrompt`/`template_version`/`output_contract`，`test_p0_t05_m04` 已证未知变量拒绝 + 输出契约不可移除）齐备；**唯一缺口**：预设模板无稳定身份/版本（`_DEFAULT_TEMPLATE` 硬编码）、override 无持久化/校验/渲染接入。DEC-P1-T03-OVERRIDE **不触发**——决策范围（编辑边界/拒绝规则/父版本失效策略）三个子项均已由权威文档给出唯一策略：`01` §16（预设不可修改、修改存为 Override）、`05` §2/§9（预设只读 + 父模板版本 + 不可关闭 Segment ID/输出包装/格式保护/校验/安全限制）、`07` §19 与 M03 目标文本（未知变量/失效父版本/边界移除拒绝、不执行模板代码）；余为可逆内部实现（`09` §7），与 P1-T04-M02/M03 DEC 不触发先例一致。**ADAPT（范围内可逆调整，硬约束/验收不变，不创建第二套 orchestrator/状态机）**：(1) 新增 `application/preset_templates.py`——`PresetTemplate` 注册表（`general` v1.0.0 = 既有默认模板文本），`ALLOWED_PLACEHOLDERS`（current/context/output_schema/source_language/target_language，与 renderer `_build_placeholders` 对齐），`get_preset_template`/`current_preset`/`resolve_override_template`（父版本错配或持久化文本失效时 fail-closed 拒绝并重校验防 DB 篡改）；(2) 新增 `domain/prompt_override.py`——`PromptOverrideError`/`PromptOverride`/`validate_override_template`（Qt-free：未知变量、非法 `$` 语法、空文本、非字符串拒绝；`string.Template.is_valid()`+`get_identifiers()` 纯替换、不执行模板代码）；(3) 新增 `migrations/012_add_prompt_override.sql`——`prompt_overrides`（model_profile_id/`parent_template_version`/template_text，`UNIQUE(model_profile_id)` 1:1，FK `model_profiles(id) ON DELETE CASCADE`，`schema_version` 保持 1）；(4) 新增 `infrastructure/repositories/prompt_override_repository.py`（save 先查后改/插不重复、get_by_profile/delete_by_profile）；(5) `ModelProfileService.set_prompt_override`/`get_prompt_override`/`clear_prompt_override`——保存时 parent = 当前预设版本，未知变量拒绝不持久化；(6) `PromptRenderer` 默认模板改从预设注册表读取（`_DEFAULT_TEMPLATE` 移除，行为不变）；(7) `TranslationService.translate_segment`——有 override 时 `resolve_override_template` 解析渲染（stale 先于 claim 拒绝、不 claim/不发请求），`_context_summary` 追加 `override_parent_version` 供 Attempt 审计；(8) `ui/workbench.py` 新增 `WorkbenchPromptEditor`（只读预设预览 `setReadOnly` + 可编辑拷贝 + Save/Clear 信号 + `initial` 草稿保留）；(9) `TranslationPage` 工作台面接入 prompt 编辑器（`_refresh_task` 返回 4 元组含 override，save/clear 经 worker 线程调 service，错误经 status 显示，`_hide_workbench`/`_clear_prompt_editor` 清理；`parent_template_version != 当前预设版本` 时给 `WorkbenchPromptEditor(stale=True)` 显示 "no longer matches the current preset" 警告 + Clear 恢复路径）。
- **修改文件：** `src/transrealm/application/preset_templates.py`（新增）；`src/transrealm/domain/prompt_override.py`（新增）；`src/transrealm/migrations/012_add_prompt_override.sql`（新增）；`src/transrealm/infrastructure/repositories/prompt_override_repository.py`（新增）；`src/transrealm/application/model_profile_service.py`（`set/get/clear_prompt_override` + `PromptOverrideRepository` + close）；`src/transrealm/application/prompt_renderer.py`（默认模板改从预设读取）；`src/transrealm/application/translation_service.py`（override 解析渲染 + `_context_summary` override 标记 + `PromptOverrideRepository`）；`src/transrealm/ui/workbench.py`（新增 `WorkbenchPromptEditor`）；`src/transrealm/ui/pages.py`（TranslationPage prompt 面 + save/clear 任务）；`tests/test_p1_t03_m03.py`（新增 38 项）；权威文档 `03` §10（受控 Prompt Override 段落）、`04` §2/§3/§5（012 + `prompt_overrides` 实体与约束）、`05` §2/§7/§9（override 校验/审计标记/结构性边界）、`07` §19（M03 completed 证据）、`08`（M03 取舍 + DEC 判定 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_model_profile.py tests/test_provider_connection.py tests/test_p0_t05_m04.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m03.py tests/test_p1_t03_m01.py tests/test_p1_t03_m02.py tests/test_p1_t03_m03.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1267 passed**（基线 1229 + M03 新增 38，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **322 passed**（不含 M03 新增 38）；Ruff All checks passed；mypy Success: no issues found in 124 source files。
- **未完成：** P1-T03-M04 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review **无 BLOCKER**，2 SHOULD-FIX 已修复并补测试：(a) stale override 在 UI 不可见（父版本错配时工作台仍显示健康、翻译才全段失败）——`_rebuild_prompt_editor` 在 `parent_template_version != 当前预设版本` 时给 `WorkbenchPromptEditor(stale=True)`，状态栏显示 "no longer matches the current preset…Re-save or clear"，补 widget + pytest-qt 测试；(b) 字面 `$` 报错 "Invalid template syntax." 无引导——错误消息改为提示 "a literal dollar must be written as '$$' (e.g. '$$5')"，测试断言含 `$$` 引导。已知限制/取舍：(1) `string.Template` 把裸 `$` 后接非标识符（如 `$5`）视为非法占位符，override 文本中的字面美元需 `$$` 转义——与现有 renderer 自定义模板行为一致，保存时提前拒绝且报错含 `$$` 引导（`test_literal_dollar_must_be_escaped` 固化）；(2) `parent_template_version` 独立于 `profile.template_version`（后者仍只是审计元数据，保留既有任意字符串语义）；父版本失效仅在应用升级更新预设后出现，渲染时 fail-closed，UI 显示 stale 警告；(3) 输出边界结构性不可移除——Output Contract wrapper 由渲染器固定追加（override 只替换正文），格式保护/校验/安全限制是独立流水线阶段，无模板特性可关闭；不执行模板代码有源码结构守卫测试；(4) 工作台 prompt 草稿跨 refresh 经 `initial` 保留，但未保存草稿不参与渲染（渲染只用持久化 override，与参数草稿同语义）；(5) 结构守卫只覆盖 pages/main_window 不直接查库，save/clear 经 `ModelProfileService`（worker 线程）是既有 worker 线程模式。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T03-M04（人工 Revision 与锁定），状态 ready，plan_alignment pending_reality_check。开工前先核验 M03 落地（`preset_templates`/`PromptOverride`/`012`/`ModelProfileService.set_prompt_override`/`resolve_override_template`/`WorkbenchPromptEditor`）与 Revision/lock seam（`TranslationRevision` origin/`current_revision_id`/`finalize_success`/`TranslationWorker.translate` 循环、`TxtExporter` Revision 选择语义），按 `01`、`03` §3/§10、`05` §6/9、`07` §19 P1-T03-M04 核验后记录 FIT/ADAPT/REPLAN，从最小"工作台编辑追加 user Revision + 锁定 current 不被自动结果覆盖"垂直闭环开始，不越出 P1-T03。

### Previous code checkpoint

- **时间：** 2026-08-08
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T03-M02 capability-aware 工作台（Reality Check **ADAPT**）。现场核验（基线：受影响旧测试子集 225 passed、全量 1209 passed）：active Profile（`009`）、capability snapshot seam（`profile.get_capability()`/`compose_adapter`/`OpenAICompatibleAdapter.filter_params` 终滤）、`TranslationWorker.translate` 循环、`WorkerPage` pytest-qt 模式与 M01 自动模式齐备；**唯一缺口**：`translate_segment` 无参数透传、Attempt 快照无实际请求参数、无按文档 Attempt 只读、无工作台 UI 面。DEC-P1-T03-OVERRIDE 不触发（属 M03）。**ADAPT（范围内可逆调整，硬约束/验收不变，不创建第二套 orchestrator/状态机）**：(1) 新增 `application/workbench.py`——`SegmentProgress` DTO、`ParamSpec` 注册表 + `presented_parameters`/`initial_param_values`（Qt-free 纯策略：只呈现 capability.supported_parameters、未知参数文本回退、`max_tokens` 钳制 capability.max_output_tokens）；(2) `TranslationService.translate_segment(extra_params=None)`——缺省用 `profile.default_params`，参数作为该 Attempt 请求参数；`TranslationRunService.start_attempt(request_params=...)` 记录于 `profile_snapshot.request_params`（JSON 快照追加键，无 migration）——参数变更只形成其后领取 Attempt 的快照、永不改写已完成 Attempt/Revision；(3) `SegmentAttemptRepository.list_by_document`（JOIN segments 限定列）+ `TranslationRunService.list_segment_progress`（每 Segment 最新 Attempt 的 status/error + current_revision）；并发加固 `_seed_builtin_workflow`（refresh 与 translate 两线程并发构造时唯一冲突重读并校验 hash/read_only）；(4) `TranslationWorker.start_translate_workbench`（显式 profile + 参数，复用既有 `translate` 循环）；(5) `ui/workbench.py` `WorkbenchParamEditor`（每 capability 支持参数一个控件，float/int/bool 类型化、未知参数文本，`max_tokens` 上限钳制，可注入 `initial` 保留草稿）；(6) `TranslationPage` 工作台面——workbench 模式显示真实进度 QListWidget + Profile/capability 摘要 + 参数编辑器，`set_document` 触发 refresh，`_on_finished` 工作台模式重读进度并保留草稿，auto 模式不变。
- **修改文件：** `src/transrealm/application/workbench.py`（新增）；`src/transrealm/application/translation_service.py`（`extra_params`）；`src/transrealm/application/translation_run_service.py`（`request_params` 快照 + `list_segment_progress` + seed 并发加固）；`src/transrealm/infrastructure/repositories/segment_attempt_repository.py`（`list_by_document`）；`src/transrealm/ui/worker.py`（`start_translate_workbench`）；`src/transrealm/ui/workbench.py`（新增 `WorkbenchParamEditor`）；`src/transrealm/ui/pages.py`（TranslationPage 工作台面 + `set_document` refresh + `_on_finished` 重读）；`tests/test_p1_t03_m02.py`（新增 20 项）；`tests/test_p1_t03_m01.py`（`test_auto_translate_service_error_emits_failed` 测试加固）；权威文档 `03` §3（capability-aware 工作台段落）、`04` §3（`profile_snapshot.request_params`）、`05` §7（Attempt 参数快照）、`07` §19（M02 completed 证据）、`08`（M02 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_model_profile.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m03.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m03.py tests/test_p1_t03_m01.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1229 passed**（基线 1209 + M02 新增 20，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **225 passed**；Ruff All checks passed；mypy Success: no issues found in 120 source files。
- **未完成：** P1-T03-M03 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review **无 BLOCKER**；1 SHOULD-FIX（`WorkbenchParamEditor` max_tokens 控件初始值已钳制但编辑范围未限，用户可旋到 1e6 发出 provider 拒绝的请求——修复：int 控件 high 钳制 `capability.max_output_tokens`，补"编辑后仍钳制"测试）；MINOR 已处理：(1) 完成后进度列表不刷新（`_on_finished` 在 workbench 模式重读进度，`_populate_workbench` 经编辑器 `initial` 参数保留当前草稿，补测试断言 completed 显示 + 0.9 草稿保留）；(2) `list_segment_progress` "取最高 id 为最新 Attempt"仅单 Attempt 测试覆盖（补 repair 路径双 Attempt 测试）。另对 M01 既有 `test_auto_translate_service_error_emits_failed` 做测试加固：该测试 worker 瞬间失败（BoomService），`_wait_finished` 在 emit 后才连接 `finished`，直连 callable 场景下信号可能先于连接发出而超时（单独复现 5 次 2 次超时）——改为先连接 `finished` 再 emit（断言不变，8/8 通过）。已知边界：(1) 工作台参数草稿仅会话内存，Override 持久化归 M03；(2) 进度列表是打开/文档选择/翻译完成时的快照，非实时订阅；(3) `WorkbenchParamEditor` 只呈现 capability 支持参数，`filter_params` 仍为发送前终滤（双保险）。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T03-M03（受控 Prompt Override），状态 ready，plan_alignment pending_reality_check。开工前先核验 M02 落地（`WorkbenchParamEditor`/`translate_segment extra_params`/`start_attempt request_params` 快照/`list_segment_progress`/`start_translate_workbench`）与 Prompt 模板 seam（`PromptRenderer.render`/`RenderedPrompt`/`template_version`/`output_contract`，`05` §2/§6/§9 预设只读 + parent template version + 不可移除输出/格式/校验边界），按 `01`（Prompt/Profile Override）、`03` §3/§10、`05` §2/6/9、`07` §19 P1-T03-M03 核验并评估 DEC-P1-T03-OVERRIDE 后记录 FIT/ADAPT/REPLAN，从最小"override 保存/重开/拒绝"垂直闭环开始，不越出 P1-T03。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T03-M01 自动模式最小旅程（Reality Check **ADAPT**）。现场核验（基线：受影响旧测试子集 68 passed、全量 1194 passed）：active Profile（`009`）、内置只读 Workflow（`TranslationRunService` seed + hash 防篡改）、ImportService 六格式、`TranslationWorker` 逐 Segment 循环（progress/cancel/close 收敛）、`TxtExporter`、P1-T04-M05 双形态持久化 seam 齐备；**唯一缺口**：mode 概念缺失、自动模式无"解析 active Profile"路径、缺配置无一步引导。DEC-P1-T03-OVERRIDE 不触发（属 M03）。**ADAPT（范围内可逆调整，硬约束/验收不变，不创建第二套 orchestrator/状态机）**：(1) 新增 `migrations/011_add_project_mode.sql`——`projects.mode`（`NOT NULL DEFAULT 'auto'` + `CHECK (mode IN ('auto','workbench'))`，`schema_version` 保持 1，旧行回填 'auto'）；`Project` 加 `MODE_AUTO`/`MODE_WORKBENCH`/`SUPPORTED_MODES`/`mode`/`with_mode()`（非法抛 `ProjectError`），`create`/`with_active_profile`/`with_updated_name` 透传；`ProjectRepository` 读写新列；`ProjectService.set_mode`（预检 project + mode 校验）+ `get_project`（单查）；(2) `TranslationWorker` 加 `start_translate_auto`/`config_missing`——worker 线程内 `ProjectService.get_project` 解析 active Profile，缺 active 发 `config_missing`（不建 Run、不发模型请求），异常 try/except 兜底发 `failed`+`finished`，否则复用既有 `translate` 核心循环；(3) `TranslationPage` 移除手动 Profile 组合框，Translate 走 `start_translate_auto`，加 mode/active-profile 状态标签（refresh 任务）与缺配置可操作引导（"设置 active Profile…"按钮 → `request_project_setup` → MainWindow 切 Project Tab），active Profile 解析后 `_hide_config_missing`；`ProjectPage` 加 `project_settings_changed` 信号（active 变更后刷新 Translation 页）；`_on_finished` 对无 pending 的 "Translating…" 占位替换为 "Translation finished."；(4) `MainWindow` 接线：project_ready + project_settings_changed → translation.set_project、request_project_setup → Project Tab、移除已死 `settings.profiles_changed → translation.set_profiles` 连接（`set_profiles`/`profiles_changed` 删除）。
- **修改文件：** `src/transrealm/migrations/011_add_project_mode.sql`（新增）；`src/transrealm/domain/project.py`（mode）；`src/transrealm/infrastructure/repositories/project_repository.py`（mode 列）；`src/transrealm/application/project_service.py`（`set_mode`/`get_project`）；`src/transrealm/ui/worker.py`（`translate_auto`/`config_missing`/`start_translate_auto`）；`src/transrealm/ui/pages.py`（TranslationPage 自动模式、ProjectPage `project_settings_changed`、移除 `set_profiles`/`profiles_changed`）；`src/transrealm/ui/main_window.py`（接线）；`tests/test_p1_t03_m01.py`（新增 15 项）；`tests/test_p0_t08_m05.py`（helper 设置 active Profile、移除 `_profile_combo` 等待）；权威文档 `03` §3（Project 交互模式段落）、`04` §2/§3/§5（011 + mode 列 + CHECK）、`07` §19（P1-T03 in_progress、M01 completed 证据）、`08` §8（M01 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_p0_t08_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m04.py tests/test_p1_t04_m05.py tests/test_p1_t03_m01.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1209 passed**（基线 1194 + M01 新增 15，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **83 passed**（68 旧 + M01 15）；Ruff All checks passed；mypy Success: no issues found in 117 source files。
- **未完成：** P1-T03-M02 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review 首轮 **REQUEST-CHANGES**（2 SHOULD-FIX 均已在 M01 新代码内修复并各补测试）：(1) `_handle_action("refresh")` 只更新状态标签、未在 active Profile 解析后隐藏配置引导横幅，会出现"无 active"与"Active profile: …"同时显示的矛盾——修复：refresh 处理器在 active Profile 解析时 `_hide_config_missing()`，补 `test_guidance_clears_after_setting_active_profile`（真实驱动：translate → 引导 → Project 页设 active → 横幅隐藏 + 标签更新）；(2) `translate_auto` 的 `ProjectService` 解析块原在 try/except 外，异常逃逸（Qt 吞掉）后无信号、UI 卡在 Translating…——修复：整体 try/except 发 `failed`+`finished`，补 `test_auto_translate_service_error_emits_failed`（monkeypatch `transrealm.ui.worker.ProjectService` 抛错断言 failed 触发）；另补缺 project 分支测试 `test_auto_translate_missing_project_emits_config_missing` 与零 pending 重翻终态测试 `test_translate_with_no_pending_segments_finishes`（Review 复核指出零 pending 修复缺测试，已补齐）。测试确定性注意：缺 project 测试预初始化 DB schema 再启动窗口（translation worker 的 ProjectService 会与 service worker 的 migration 步骤在全新 DB 上竞争）；引导清除测试读 active-profile 组合框前先 `project.refresh()`（create-project 的 refresh 结果被后续 import 请求经 WorkerPage 单 pending-request 槽丢弃）。已知边界：(1) 切换 Project 后 TranslationPage 保留旧 `_document_id`（预存在、非 M01 引入，M02/M05 document 列表落地时处理）；(2) `set_mode` 尚无 UI 调用方（供测试 + 未来 M05 模式切换）；(3) 结构守卫只覆盖 pages/main_window 不直接查库，worker 直用 `ProjectService`/`SegmentRepository` 是既有 worker 线程模式。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T03-M02（capability-aware 工作台），状态 ready，plan_alignment pending_reality_check。开工前先核验 M01 落地（`projects.mode`/`011`、`set_mode`/`get_project`、`translate_auto`/`config_missing`、TranslationPage 自动模式/`project_settings_changed`）与 P1-T04 全链路（active Profile/Glossary/locked 注入/薄管理 UI）、capability snapshot seam（`get_capability`/`compose_adapter`/`supported_parameters`），按 `01` §3.2、`03` §3/§10、`05` §2/8、`07` §19 核验后记录 FIT/ADAPT/REPLAN，从最小"工作台真实进度/参数垂直闭环"开始，不越出 P1-T03。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T04-M04 薄管理 UI（Reality Check **ADAPT**）。现场核验：M01–M03 与 P0-T08-M05 UI seam 齐备——`ProjectService.select_active_profile/clear_active_profile/list_projects`、`GlossaryService` CRUD（`priority DESC, id` 稳定排序）、`ModelProfileService.delete_profile` 预检 `ModelProfileInUseError`、`credential_status.report_credential_availability`、`ServiceWorker`/`WorkerPage`/`SettingsPage`/`ProjectPage` 与 pytest-qt 模式；基线复现全量 pytest **1177 passed, 1 skipped**（与交接一致）。**唯一缺口**：UI 无 active Profile 选择面、无 Glossary 管理面、`ProviderConnectionService.delete_connection` 对被 Profile 引用抛不透明 `SqlExecutionError`、未消费凭据报告。**ADAPT（可逆内部调整，硬约束/验收/契约不变）**：(a) `ProviderConnectionService.delete_connection` 删除前预检——被任一 `model_profiles.provider_connection_id` 引用时抛新增 `ProviderConnectionInUseError` 并列出 profile 名（镜像 M01 `delete_profile` 预检先例，DB FK RESTRICT 兜底并发写，缺失返回 False 语义不变，既有 `test_service_blocks_connection_delete_when_referenced` 用 `pytest.raises(Exception)` 不破坏）；(b) UI 落地于既有 `SettingsPage`（`_delete_connection`/`_delete_profile` 按钮 + `_credential_status` 凭据可用性提示经 `report_credential_availability`，缺引用显示可操作 hint 永不泄 secret）与 `ProjectPage`（项目选择 `select_project(project_id)` 程序化入口、active Profile 选择/清除、Glossary 增改删表单 source/target/scope/priority(QSpinBox 0-100)/locked(QCheckBox)），沿用 `ServiceWorker`+`WorkerPage`（`_submit` → worker 线程开闭 service 连接，Qt 主线程只持 `db_path`），UI 不直接查库；`MainWindow` 启动增 `_project.refresh()`。**新增 `tests/test_p1_t04_m04.py` 15 项**（见 §5 索引）。
- **修改文件：** `src/transrealm/ui/pages.py`（`SettingsPage` 删除按钮 + 凭据提示、`ProjectPage` 项目选择/active Profile/Glossary、`select_project`）；`src/transrealm/ui/main_window.py`（启动 `_project.refresh()`）；`src/transrealm/application/provider_connection_service.py`（`delete_connection` 预检 + 持 `ModelProfileRepository`）；`src/transrealm/domain/provider_connection.py`（`ProviderConnectionInUseError`）；`tests/test_p1_t04_m04.py`（新增 15 项）；权威文档 `03` §10（薄管理 UI 段落）、`04` §3/§5（Connection 删除预检约束）、`07` P1-T04（M04 completed 证据、Task 状态 M01–M04 completed）、`08` §8（M04 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_model_profile.py tests/test_provider_connection.py tests/test_p0_t08_m05.py tests/test_p1_t02_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m02.py tests/test_p1_t04_m03.py tests/test_p1_t04_m04.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1192 passed**（基线 1177 + M04 新增 15，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **185 passed**（170 旧 + M04 15，1 skipped）；Ruff All checks passed；mypy Success: no issues found in 115 source files。
- **未完成：** P1-T04-M05 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX，见 `08` §8）：MINOR 已处理——`SettingsPage._populate` 刷新保留 Add-Profile 连接下拉的当前选择（防中途选择被静默换掉），新增 `TestSettingsSelectionPreserved` 测试；其余 MINOR 记录取舍——`WorkerPage` 单 pending-request 槽（快速连点删除时前一次结果/错误被丢弃，但动作仍执行、随后 refresh 收敛到真实状态）是 P0-T08-M05 既有共享设计，修复需跨页面重构、风险大于收益，保留。已知取舍：(1) 被引用 Connection 删除异常类型从 `SqlExecutionError`（DatabaseError）变为 `ProviderConnectionInUseError`（ValueError）——in-tree 无调用方受影响，UI 按新类型捕获（与 M01 异常类型变化同先例）；(2) `delete_connection` 预检与删除非同一事务（预检读 + repository.delete 自开事务），单用户本地应用可接受，最坏退化为通用 `SqlExecutionError`，DB FK RESTRICT 保证引用完整、无数据破坏；(3) 结构性守卫只覆盖 `ui/pages.py`/`ui/main_window.py`——`ui/worker.py` 的 `TranslationWorker` 直用 `SegmentRepository` 是 P0-T08-M05 既有翻译 worker、非 M04 管理面，不在守卫范围；(4) active Profile/Glossary 持久化于共享 SQLite 载体，重开窗口经 service 自动恢复。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T04-M05（持久化/双形态/GUI Gate），状态 ready，plan_alignment pending_reality_check。开工前先核验 M04 落地（`ProviderConnectionInUseError`/`delete_connection` 预检、`SettingsPage`/`ProjectPage` 管理面、`select_project`、凭据提示）与 M01–M03 全链路，按 `01`、`03` §3/§7/§10、`04` §3/§5/§7、`05` §4/5、`07` §18 核验后记录 FIT/ADAPT/REPLAN，执行重启/Project 隔离/开放目录与 `.aiproject` round-trip/预算与 UI 回归验证，不越出 P1-T04。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T04-M03 确定性 Prompt 注入（Reality Check **ADAPT**）。现场核验：`ContextComposer.compose`（P0-T05-M03）已实现 glossary 候选 priority 归一化为 `_GLOSSARY_PRIORITY=1`、在邻居前选择、Manifest 记录 selected/pruned、预算不足对当前 Segment 抛 `ContextBudgetError` 不截断；`GlossaryService.list_locked_entries` → `GlossaryEntryRepository.list_locked_by_project`（`WHERE is_locked=1 ORDER BY priority DESC, id`）已提供确定稳定次序（M02）；基线复现全量 pytest **1165 passed, 1 skipped**（与交接一致）。**唯一缺口**：`TranslationService.translate_segment`（`application/translation_service.py`）调用 `compose` 未传 `glossary_candidates`，且无 `GlossaryEntry → ContextCandidate` 映射。**DEC-P1-T04-GLOSSARY 判定不触发**（priority 语义已由 `05` §5 裁决、scope 不参与注入过滤、重复词唯一域已委托——M02 已判定，M03 只接线不引入新决策）。**新增 `application/glossary_context.py`**：`build_glossary_candidates` 纯函数（`content = "{source_term} -> {target_term}"`、`segment_id = "glossary:{source_term}"`、`reason="locked glossary"`、metadata 带 entry_id/source_term/target_term/scope/priority/origin，共享 `default_estimate` 估算且 `estimate_method`/`estimator` 可注入，不复制估算逻辑）。**`context_composer.py`**：私有 `_default_estimate` 公开为 `default_estimate`（唯一内部引用更新，公共行为不变）。**`TranslationService`**：增持 `GlossaryEntryRepository`（`__init__`/`close()`），`translate_segment` 中 `build_glossary_candidates(self._glossary_repository.list_locked_by_project(run.project_id), estimate_method=budget.estimate_method)` 传入 `compose(glossary_candidates=...)`；不新建 Service/Manager、不新增依赖/schema、不改 composer 选择规则、不重写 Profile/Glossary CRUD。**新增 `tests/test_p1_t04_m03.py` 12 项**（见 §5 索引）。
- **修改文件：** `src/transrealm/application/glossary_context.py`（新增）；`src/transrealm/application/translation_service.py`（`GlossaryEntryRepository` + `compose(glossary_candidates=...)` + `close()`）；`src/transrealm/application/context_composer.py`（`default_estimate` 公开）；`tests/test_p1_t04_m03.py`（新增 12 项）；权威文档 `03` §7（确定性锁定 Glossary 注入段落）、`07` P1-T04（M03 completed 证据、Task 状态 M01–M03 completed）、`08` §8（M03 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_model_profile.py tests/test_provider_connection.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t05_m02.py tests/test_p0_t05_m03.py tests/test_p0_t05_m04.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m02.py tests/test_p1_t04_m03.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1177 passed**（基线 1165 + M03 新增 12，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **344 passed**（332 旧 + M03 12）；Ruff All checks passed；mypy Success: no issues found in 114 source files。
- **未完成：** P1-T04-M04 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX）；MINOR 已处理：(1) 补 composer 同 priority 平局测试——glossary 归一化 priority 1 与 distance-0 邻居 priority 1 相同时，"glossary 在前"依赖 `sorted` 稳定排序（glossary 先入列表），新增 `TestComposerGlossaryNeighborTie` 锁定该边界。其余 MINOR 记录取舍：(1) Manifest glossary 候选 priority 记录归一化值 1，条目真实 priority 仅存 `metadata`/`context_summary` 未逐条透出——§7 debug 如需按条目优先级可从 metadata 导出（当前无该消费方，保留）；(2) 只有 locked、仅当前 Project 的过滤放在 repository 查询（`list_locked_by_project(run.project_id)`），映射函数不做二次过滤——职责单一、可单测。已知边界：注入改变 rendered prompt 文本 → 对有 locked 项的 Project，Attempt `prompt_hash`/`context_summary` 相应变化（预期行为，`05` §5 契约）；无 locked 项的 Project 行为与 M03 前完全一致（回归测试覆盖）。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T04-M04（薄管理 UI），状态 ready，plan_alignment pending_reality_check。开工前先核验 M03 落地（`build_glossary_candidates`/`TranslationService` 注入 seam）与 M01/M02 的 use case 表面（`select_active_profile`/`clear_active_profile`、`GlossaryService` CRUD、`credential_status.report_credential_availability`、被引用删除错误），按 `01`（Glossary/Profile）、`03` §3/§10（UI worker/薄管理）、`05` §4/5、`07` §18 核验后记录 FIT/ADAPT/REPLAN，从最小"薄管理 UI 垂直闭环"开始（UI 只调 Application use case、不直接查库），不越出 P1-T04。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T04-M02 Project-scoped Glossary（Reality Check **ADAPT**）。现场核验：`04` §3 已列 `glossary_entries` schema、`05` §4/5 已裁决 locked 注入按 priority+稳定次序、`ContextComposer._GLOSSARY_PRIORITY=1` M03 seam 存在、`07` §18 硬约束（Project 隔离/source·target 非空/scope·priority 稳定验证/V1.0 只注入 locked）与执行 Agent 自决（同词唯一域/priority 数值范围）齐备；基线复现全量 pytest **1129 passed, 1 skipped**（与交接一致）。**DEC-P1-T04-GLOSSARY 判定不触发**（priority 语义已裁决+范围已委托；重复词唯一域已委托+要求明确结果且不污染；scope 是用户可编辑分类标签、M03 注入不用 scope 过滤）。**新增 `migrations/010_add_glossary_entry.sql`**：`glossary_entries`（id、project_id FK `projects(id) ON DELETE CASCADE`、source_term、target_term、scope、priority 0-100 默认 50 CHECK、is_locked 0/1 默认 0 CHECK、origin ('user','import') CHECK、created_at、updated_at、UNIQUE(project_id, source_term)、索引 (project_id, priority)）。**`domain/glossary_entry.py`**：`GlossaryEntryError` + `GlossaryEntry` dataclass（create/validate/with_updated_fields），validate 先校验 project_id 正整数，`_normalize_text` 对 source_term/target_term/scope 先 isinstance(str) 防御（非字符串给明确 `GlossaryEntryError`）再 strip 归一化 + 非空校验，priority 0-100 int（排除 bool）、is_locked bool、origin ∈ ('user','import')。**`infrastructure/repositories/glossary_entry_repository.py`**：save（INSERT/UPDATE）/get_by_id/find_by_project_and_source/list_by_project（`ORDER BY priority DESC, id`）/list_locked_by_project/delete，遵循 `transaction` 模式。**`application/glossary_service.py`**：`GlossaryService`（create_entry/update_entry/get_entry/list_entries/list_locked_entries/delete_entry），构造跑 migration 且失败关闭连接（对齐 ProjectService 加固）；create/update 前预检 project 存在（`_ensure_project_exists`）与同 Project source_term 重复（`_ensure_no_duplicate`，先 isinstance(str) 防御再 strip，exclude_id 处理 update 自保），明确 `GlossaryEntryError`，DB UNIQUE 兜底并发写。**新增 `tests/test_p1_t04_m02.py` 36 项**（见 §5 索引）。
- **修改文件：** `src/transrealm/migrations/010_add_glossary_entry.sql`（新增）；`src/transrealm/domain/glossary_entry.py`（新增）；`src/transrealm/infrastructure/repositories/glossary_entry_repository.py`（新增）；`src/transrealm/application/glossary_service.py`（新增）；`tests/test_p1_t04_m02.py`（新增 36 项）；权威文档 `03` §3（Project-scoped Glossary 段落）、`04` §2（迁移清单 009/010）/§3（GlossaryEntry 细节）/§5（UNIQUE/CHECK/FK CASCADE 约束）、`07` P1-T04（M02 completed 证据）、`08` §8（M02 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_model_profile.py tests/test_migrations.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_provider_connection.py tests/test_p1_t02_m01.py tests/test_p1_t02_m02.py tests/test_p1_t02_m03.py tests/test_p1_t02_m04.py tests/test_p1_t02_m05.py tests/test_p1_t04_m01.py tests/test_p1_t04_m02.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1165 passed**（基线 1129 + M02 新增 36，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **447 passed**（411 旧基线 + M02 36，1 skipped）；Ruff All checks passed；mypy Success: no issues found in 112 source files。
- **未完成：** P1-T04-M03 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX）；MINOR 已处理：(1) domain/service 对非字符串 term 原抛裸 AttributeError，改为明确 `GlossaryEntryError`（isinstance 防御）；(2) `GlossaryService.__init__` 迁移失败泄漏 DB 连接，改为 try/except 关闭（对齐 `ProjectService` 加固）；(3) 补 5 项测试缺口——repository 直存重复 `SqlExecutionError`+回滚（事务故障证据）、update 校验失败不污染、origin='import' 合法、同 priority 按 id 决胜、跨 Project 删除隔离。其余 MINOR 记录取舍：(1) 负/零 project_id 经 service 先走 `_ensure_project_exists` 报"Project with id 0 does not exist"而非"positive integer"（cosmetic，与 M01 负 id 文案同源，保留）；(2) service 预检与写入非同一事务，并发重复最坏退化为通用 `SqlExecutionError` 且事务回滚无污染（单用户本地应用可接受，与 M01 删除预检同一取舍，DB UNIQUE 兜底）。已知边界：`010` 为追加式 migration，不改 001-009；`glossary_entries.project_id` ON DELETE CASCADE 与 `translation_runs`/`segments` 项目级 CASCADE 一致；V1.0 只注入 locked 项（unlocked 可保存/编辑但不进 Prompt）。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T04-M03（确定性 Prompt 注入），状态 ready，plan_alignment pending_reality_check。开工前先核验 M02 落地（`glossary_entries`/`010` migration、`GlossaryService.list_locked_entries` 排序 seam）与 `ContextComposer.glossary_candidates` 注入 seam（`05` §2/4/5 locked 优先、`ContextSource.LOCKED_GLOSSARY`/`_GLOSSARY_PRIORITY` 归一化、Manifest selected/pruned），按 `01`（Glossary）、`03` §7（Context）、`05` §2/4/5、`04` §3/5 核验后记录 FIT/ADAPT/REPLAN，从最小"locked 术语确定性注入垂直闭环"开始，不越出 P1-T04。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T04-M01 Project active Profile 选择（Reality Check **ADAPT**）。现场核验：基线复现全量 pytest **1111 passed, 1 skipped**（与交接一致）；`ModelProfileService`/`ProviderConnectionService` 及仓库 CRUD 齐备（不重写）；`005` `model_profiles.provider_connection_id` RESTRICT、`006` `segment_attempts.model_profile_id` RESTRICT 提供删除兜底、`profile_snapshot` 历史快照满足可解释性；capability snapshot seam（`get_capability`/`compose_adapter`）齐备；`projects` 表**无** active 字段、`delete_profile` 遇 RESTRICT 抛不透明 `SqlExecutionError`（唯一缺口）。**新增 `migrations/009_add_project_active_profile.sql`**：`ALTER TABLE projects ADD COLUMN active_profile_id INTEGER REFERENCES model_profiles(id) ON DELETE RESTRICT`（可空、NULL 默认满足 SQLite ALTER ADD COLUMN REFERENCES 限制；`projects.schema_version` 保持 1；1:1 单一值用列而非规范化表；开放目录/`.aiproject` 共享同一 SQLite 载体重开自动恢复，无需容器逻辑）。**`domain/project.py`**：加 `ProjectError`/`active_profile_id: int | None = None`/`with_active_profile`。**`infrastructure/repositories/project_repository.py`**：INSERT/UPDATE/SELECT 读写新列 + `list_by_active_profile(profile_id)`。**`application/project_service.py`**：新增 `select_active_profile(project_id, profile_id)`/`clear_active_profile(project_id)`（预检 project/profile 存在，缺任一抛 `ProjectError`；不新建转发型 Service）。**`application/model_profile_service.py`**：`delete_profile` 删除前预检——被任一 Project 的 `active_profile_id` 引用（抛 `ModelProfileInUseError` 列出项目名，两 Project 共享列出双名）或被历史 `segment_attempts.model_profile_id` 引用（抛 `ModelProfileInUseError` 给引用计数）时拒绝，FK RESTRICT 兜底；未引用删除与缺失返回 False 的既有语义不变。**`infrastructure/repositories/segment_attempt_repository.py`**：新增 `count_by_model_profile(profile_id)`。**新增 `tests/test_p1_t04_m01.py` 18 项**（见 §5 索引）：新建默认无 active、select/更换/clear 重开恢复、clear 幂等、跨 Project 隔离（同词/同 profile 互不影响）、缺失 project/profile 明确错误、仓库缺项目 ValueError、被 Project active 删除明确错误含项目名、被历史 Attempt 引用（经 `TranslationRunService.start_attempt` 真实 seam）删除明确错误、clear 后释放删除保护、未引用可删、DB 级 FK 兜底（raw DELETE 被 RESTRICT 拒绝）、旧库(001-008)升级 009 加列 NULL + 升级后可选。
- **修改文件：** `src/transrealm/migrations/009_add_project_active_profile.sql`（新增）；`src/transrealm/domain/project.py`（`ProjectError` + `active_profile_id` + `with_active_profile`）；`src/transrealm/domain/model_profile.py`（`ModelProfileInUseError`）；`src/transrealm/infrastructure/repositories/project_repository.py`（新列 + `list_by_active_profile`）；`src/transrealm/infrastructure/repositories/segment_attempt_repository.py`（`count_by_model_profile`）；`src/transrealm/application/project_service.py`（`select_active_profile`/`clear_active_profile`）；`src/transrealm/application/model_profile_service.py`（`delete_profile` 预检）；`tests/test_p1_t04_m01.py`（新增 18 项）；权威文档 `03` §3（Project active Profile 段落）、`04` §3（projects 列）/§5（FK RESTRICT 约束）、`07` P1-T04（状态 in_progress、M01 completed 证据）、`08` §8（M01 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_model_profile.py tests/test_migrations.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_provider_connection.py tests/test_p1_t02_m01.py tests/test_p1_t02_m02.py tests/test_p1_t02_m03.py tests/test_p1_t02_m04.py tests/test_p1_t02_m05.py tests/test_p1_t04_m01.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1129 passed**（基线 1111 + M01 新增 18，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **411 passed**（393 旧 + M01 18，1 skipped）；Ruff All checks passed；mypy Success: no issues found in 108 source files。
- **未完成：** P1-T04-M02 未开始（当前指针，ready + pending_reality_check；Reality Check 时评估 `DEC-P1-T04-GLOSSARY` 是否触发）。
- **风险/阻塞：** 无技术阻塞。独立 Review **APPROVE-WITH-MINORS**（无 BLOCKER/SHOULD-FIX）；MINOR 已处理：(1) 误导性测试 `test_repository_rejects_missing_project_on_update` 改名 `test_repository_save_rejects_missing_project` 并补仓库缺项目 ValueError 路径；(2) 补两 Project 共享同一 active profile 删除错误列出双名；(3) 补 `clear_active_profile` 幂等。其余 MINOR 记录取舍：(1) `delete_profile` 预检与删除不在同一事务（预检读 + repository.delete 自开事务），单用户本地应用可接受，并发最坏退化为通用 `SqlExecutionError`，FK RESTRICT 保证引用完整、无数据破坏；(2) 被历史 Attempt 引用的删除异常类型从 `SqlExecutionError`（DatabaseError）变为 `ModelProfileInUseError`（ValueError）——in-tree 无调用方受影响，UI 落地（M04）按新类型捕获；(3) `with_active_profile` 不校验 profile_id 正负（负 id 报 "ModelProfile with id -1 does not exist"）——cosmetic，保留。已知边界：`009` 为追加式 migration，不改 001–008；`active_profile_id` 对既有行 NULL backfill；删除保护不破坏历史 Attempt 的 `profile_snapshot` 快照。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T04-M02（Project-scoped Glossary），状态 ready，plan_alignment pending_reality_check。开工前先核验 P1-T04-M01 落地（`projects.active_profile_id`/`009` migration）与 Glossary 落点（`04` §3 GlossaryEntry 已列 schema：project_id/source_term/target_term/scope/priority/is_locked/origin）、Context 注入 seam（`05` §2/5 locked 优先、`ContextComposer.glossary_candidates`），按 `01`（Glossary）、`03` §7（Context）、`04` §3/5、`05` §2/4/5/9 核验并评估 `DEC-P1-T04-GLOSSARY`（scope/重复词冲突/priority 语义）是否已在权威文档裁决后记录 FIT/ADAPT/REPLAN，从最小"Glossary CRUD 垂直闭环"开始，不越出 P1-T04。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T02-M04 双形态迁移与原子安装（Reality Check **ADAPT**）。现场核验：M03 `import_archive_to_staging` staging 契约、M01 `create_open_directory_project`/`open_open_directory_project`、`ProjectService`/`MigrationRunner`/P0-T03-M00 备份 seam、`projects.schema_version` 语义（001–008 均不 bump，恒为 1）；按 `04` §7（迁移/原子安装/名称冲突规则）、`03` §3、`01`、`08` §7/8 核验后确认 seam 齐备，并**实测复现 M01 已知边界缺陷**：打开旧 schema（001–007）开放目录容器经 `ProjectService` 迁移 008 后留 stale manifest（DB 135168→143360 未重算）+ 容器内残留 `project.pre-upgrade-*.db.bak`，二次打开 `FileIntegrityError: hash mismatch`。**新增 `application/install_service.py`**：`InstallError`/`TargetConflictError`（带 `suggested_name`）、`migrate_container`（就地迁移：只读校验 manifest/路径/大小/hash + `check_container_identity`（恰一 Project + schema 匹配）→ `ProjectService` 跑 pending migration（P0-T03-M00 先建备份）→ 成功 `cleanup_after_container_migration`（先 purge sidecars + 重算并写 manifest、最后删 pre-upgrade 备份）复验；无 pending 不动容器）、`install_staging_to_target`（迁移 staging → 写 manifest `source_id` 供追踪 → `_target_state` 分类 new/empty/container/other → 新/空目标：同父 `mkdtemp` + copytree + `open_open_directory_project` 验证 + `os.rename` 原子安装；container 目标未 `confirm_overwrite` 拒绝（同名给 `Alpha (2)` 确定性后缀建议名）；确认覆盖 `shutil.move` 旧目标为 `.<target>.pre-replace-<ts>.bak` 恢复备份再 rename、失败还原（还原失败点名恢复路径）；OSError 包装 `InstallError`）。**`infrastructure/open_directory.py`**：`ManifestInfo` 增可选 `source_id`（`build_manifest`/`_validate_structure` round-trip）、`check_container_identity`/`purge_sqlite_sidecars`/`remove_pre_upgrade_backups`/`regenerate_manifest`（重算 DB entry、保留附件与既有 `source_id`）/`cleanup_after_container_migration`。**`application/project_service.py`**：`applied_migrations` 属性 + `__init__` 失败关闭连接。**`application/open_directory_service.py`**：重写 `open_open_directory_project`——先 `check_container_identity`（pre-migration）→ 首次 `ProjectService` 跑迁移 → 关闭连接触发 WAL checkpoint → `applied` 时 `cleanup_after_container_migration` + 复验 → 重开 service → schema 校验。`archive_service` 复用共享 `purge_sqlite_sidecars`。新增 `tests/test_p1_t02_m04.py` **29 项**（见 §5 索引）。手工 smoke 验证 create→export→import→install→open round-trip（project identity/source_id/segment 数据/008 全保留、目标容器干净）。
- **修改文件：** `src/transrealm/application/install_service.py`（新增）；`src/transrealm/infrastructure/open_directory.py`（`ManifestInfo.source_id` + 5 个共享 helpers + `__all__`）；`src/transrealm/application/project_service.py`（`applied_migrations` + init 失败关闭连接）；`src/transrealm/application/open_directory_service.py`（`open_open_directory_project` 迁移路径重写、`_remove_pre_upgrade_backup` 委托共享 helper）；`src/transrealm/application/archive_service.py`（复用共享 purge helper）；`tests/test_p1_t02_m04.py`（新增 29 项）；权威文档 `03` §3（M04 双形态迁移与原子安装段落）、`04` §7（M04 段落 + manifest 可选 `source_id`）、`07` P1-T02（状态 in_progress、M04 completed 证据）、`08` §8（M04 取舍 + Review 结果）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p1_t01_m01.py tests/test_p1_t02_m01.py tests/test_p1_t02_m02.py tests/test_p1_t02_m03.py tests/test_p1_t02_m04.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1085 passed**（基线 1056 + M04 新增 29，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **282 passed**（253 旧 + M04 29，1 skipped）；Ruff All checks passed；mypy Success: no issues found in 105 source files。
- **未完成：** P1-T02-M05 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review 无 BLOCKER；2 SHOULD-FIX 已修复并补测试：(1) `cleanup_after_container_migration` 原"先删 pre-upgrade 备份、后重算 manifest"，若 manifest 写失败容器被卡死（DB 已迁移 + stale manifest + 无备份），改为"先重算并写 manifest、最后删备份"；(2) `install_staging_to_target` 恢复失败分支未测且双重失败会掩蔽原错误并丢恢复指针，改为链原错误 + 点名恢复备份路径，补 2 项测试。MINOR 已处理：只读目标检查不再 purge 既有目标 sidecars（不改动既有目标）、`sqlite3.connect` 越界改报领域错误、`suggest_available_name` 简化内联。其余 MINOR 记录取舍：(1) 不支持 schema 版本的目标在安装时以"目标非空且非项目容器"拒绝（fail-closed 但掩蔽真实原因）；(2) post-migration `list_projects` 读失败会跳过清理（容器留 stale manifest + pre-upgrade 备份，需手动恢复，低频）；(3) `migrate_container` 与 open 路径的"校验→迁移→清理→重算"编排少量重复（两处返回类型/生命周期不同，为避免 install↔open 循环导入未合并）。已知边界：开放目录 manifest 只在 create/export/migrate/install 时重算，直接改库后不重算会使下次打开 fail-closed（M01-M03 既有契约，非 M04 回归）。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T02-M05（跨机器与双形态 Gate），状态 ready，plan_alignment pending_reality_check。开工前先核验 M01–M04 的 export/import/install seam 与 P1-T02 全链路，确定"另一台 Windows"隔离夹具（模拟跨机环境，不真调远程/付费服务）与缺 credential 的恢复路径、secret scan 范围与篡改/资源限额矩阵，按 `04` §7、`03` §3、`01`、`08` §7/8 核验后记录 FIT/ADAPT/REPLAN，从最小跨机 round-trip 垂直闭环开始，不越出 P1-T02。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T02-M03 隔离安全导入（Reality Check **ADAPT**）。现场核验：M01 manifest/路径/hash/link seam（`infrastructure/open_directory.py` `load_manifest`/`validate_manifest`/`canonical_relpath`/`resolve_within_root`/`is_link`/`verify_entries`）、M02 归档 seam（`write_archive`/`create_consistent_snapshot`）、`ProjectService.list_projects()`；无 import/staging/限额逻辑，整条隔离导入链缺失。DEC-P1-T02-CONTAINER 不触发：限额维度已在 `04` §7 裁决（必须存在 + fixture/Release Gate 固定数值），现场无预置数值，故 M03 用 fixture 固定实现阈值（entry 数 ≤1000、单项未压缩 ≤256 MiB、总未压缩 ≤512 MiB、压缩比 ≤5000:1），属已裁决实现范围。**新增 `infrastructure/open_directory.py`**：`parse_manifest_text`（从 `load_manifest` 抽取 JSON 解析+结构校验，供归档内 manifest 字节校验复用）、`ArchiveLimits`（frozen dataclass 默认值 + 调用方可传更窄）、`ArchiveError`/`ArchiveLimitError`、`extract_archive`——读归档内 `manifest.json`（存在性/常规文件/≤1 MiB 上限/UTF-8/`parse_manifest_text`/`validate_manifest`），`_check_member_duplicates`/`_check_undeclared_members` 拒绝重复/未声明/目录成员，`_check_size_limits`+`_check_compression_ratio` 对 ZIP 头（`file_size`/`compress_size`，含 `compress_size==0` 非空文件 ratio→∞ 拒绝）在写盘前施加限额，`_extract_entry` 只解声明 entry（`canonical_relpath`+`resolve_within_root` 手工流式写，64 KiB 分块），最后 `verify_entries` 复核大小/`sha256:`/link；写 manifest.json 原字节入 staging。**`application/archive_service.py`**：`ArchiveImportError`、`import_archive_to_staging`——`_prepare_staging_dir`（staging 须空/新建，拒绝非空）、`extract_archive`、`_verify_staging_database`（只读 `PRAGMA query_only=ON`，恰一个 Project + `projects.schema_version` 与 manifest 一致，非整数/NULL schema 报 `ArchiveImportError` 不泄漏 `ValueError`）、失败 `_cleanup_failed_import`（清理自身产物含子目录、保留既有空目录）、成功后 `_purge_sqlite_sidecars` 清 WAL 附属。新增 `tests/test_p1_t02_m03.py` **37 项**（见 §5 索引）。手工 smoke 验证 export→import→reopen 同契约、staging 无 sidecar。
- **修改文件：** `src/transrealm/infrastructure/open_directory.py`（`parse_manifest_text`/`ArchiveLimits`/`ArchiveError`/`ArchiveLimitError`/`extract_archive` 及私有 helpers、`_MAX_MANIFEST_BYTES`、`__all__`）；`src/transrealm/application/archive_service.py`（`ArchiveImportError`/`import_archive_to_staging`/`_prepare_staging_dir`/`_cleanup_failed_import`/`_verify_staging_database`/`_purge_sqlite_sidecars`）；`tests/test_p1_t02_m03.py`（新增 37 项）；权威文档 `03` §3（隔离安全导入段落）、`04` §7（导入流程 + 限额 fixture 值）、`07` P1-T02（状态 in_progress、M03 completed 证据）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p1_t01_m01.py tests/test_p1_t02_m01.py tests/test_p1_t02_m02.py tests/test_p1_t02_m03.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1056 passed**（基线 1019 + M03 新增 37，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **252 passed**（216 旧 + M03 37，1 skipped）；Ruff All checks passed；mypy Success: no issues found in 103 source files。
- **未完成：** P1-T02-M04 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review 无 BLOCKER；1 SHOULD-FIX（`_verify_staging_database` 对非整数 `projects.schema_version` 泄漏原始 `ValueError`，改报 `ArchiveImportError`）已修复并补测试；其余 MINOR 记录取舍：(1) `zipfile` 构造时已物化中央目录，`namelist()` 先物化后限数，条目上限是防御性启发（内存受归档中央目录大小约束）；(2) `extract_archive` 不自行判空（由 app 层 `_prepare_staging_dir` 保证，与 M02 `write_archive` 风格一致）；(3) `_purge_sqlite_sidecars` 无条件删除 `-wal`/`-shm`，当前只读校验不会产生非空 WAL 故安全；(4) `_verify_staging_database` 只查行数+schema_version，不查其余表结构（自洽恶意 DB 可构造，但属 M03 契约"同契约开放目录"边界，M04 打开/迁移时兜底）。已知边界：长路径（>260）staging/DB 路径未做 `\\?\` 归一化（与整个容器模块既有行为一致，非 M03 回归）。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T02-M04（双形态迁移与原子安装），状态 ready，plan_alignment pending_reality_check。开工前先核验 M03 `import_archive_to_staging` staging 契约与 M01 `create_open_directory_project`/`open_open_directory_project`、`ProjectService`/`MigrationRunner`/P0-T03-M00 备份 seam、`projects.schema_version` 语义，按 `04` §7（迁移/原子安装/名称冲突规则）、`03` §3、`01`、`08` §7/8 核验后记录 FIT/ADAPT/REPLAN，从最小"staging → 目标"安装垂直闭环开始，不越出 P1-T02。

### Planning checkpoint

- **时间：** 2026-07-30
- **Agent：** Claude（规划 Agent）
- **完成内容：** 对当前 P0-T07-M01 至 V1.0 Release Candidate Gate 完成 Reality Audit、依赖重排、垂直 Milestone 重构和规划级架构审查；P0-T06 无剩余 Milestone，不安排重写；P1-T04 调整到 P1-T03 前，先提供 Project Profile 选择与基础 Glossary；明确 Attempt/lease/retry/Revision、格式保真、Project 双形态与安全导入、UI worker 和 Release Gate 契约。用户裁决 V1.0 保留 `.aiproject` + 高级用户开放目录两种互通 Project 形态。
- **修改文件：** 仅规划/治理文档：`02_Development_Roadmap.md`、`03_Technical_Design.md`、`04_Database_Schema.md`、`05_Prompt_Architecture.md`、`07_Developer_Task_List.md`、`08_Architecture_Review.md`、`DEVELOPMENT_STATE.md`、旧 Agent 路由文档。
- **验证方式：** 只读检查 Git、代码、migration、依赖和测试分布；规划文档 diff/交叉引用/路径/编号审查。未运行测试；历史 `333 passed` 仅保留为上一代码检查点证据，不作为本次重新验证。
- **未完成：** P0-T07-M01 仍未开始；current pointer、state 和 `plan_alignment` 未推进。
- **风险/阻塞：** 工作树仍包含大量未提交业务代码和 tests，HEAD 只到 P0-T02-M02；开始实现前必须形成受保护、可恢复的代码基线并重新验证。V1.0 Project 双形态已由用户裁决，无剩余需要改变产品范围/技术栈的规划裁决项。
- **恢复动作：** 执行 Agent 读取本文件，进入 P0-T07-M01；先检查 Git 未提交现场，在不覆盖当前工作树的前提下形成可恢复 Git 基线（保护范围含 tracked、untracked、staged additions/deletions，并记录恢复方法），再执行 M01 Reality Check 和 `07` 的行为切片。保护完成前不得新增 M01 业务实现，禁止从 HEAD 覆盖现有工作或提前进入未来 Task。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T02-M02 一致性单文件导出（Reality Check **ADAPT**）。现场核验：M01 manifest 契约（`infrastructure/open_directory.py`：entry 相对路径/类型/大小/`sha256:` hash、`software`/`format_version`/`schema_version`）、P0-T03-M00 一致性备份 seam（`sqlite3.Connection.backup` + 重开校验）、`ProjectService.list_projects()`、附件/秘密边界均满足 M02 前提；`.aiproject` 用 stdlib `zipfile`（零新依赖），零/多 Project 明确拒绝（契约允许拒绝或隔离 snapshot 二选一）。**新增 `application/archive_service.py`**：`ArchiveExportError`；`export_open_directory_archive`（源开放目录经 `open_open_directory_project` 校验 manifest/路径/hash/恰一个 Project 后导出）；`export_database_archive`（源裸活动数据库 `ProjectService.list_projects()` 恰一个才导出，零/多 Project 拒绝 `ArchiveExportError`）；共享 `_build_archive`——`tempfile.TemporaryDirectory` staging 构建一致容器：`create_consistent_snapshot` 生成一致性 snapshot → 复制声明附件 → `compute_entry` 重建 manifest → `validate_manifest` + `write_manifest` + `verify_entries` 自校验 → `write_archive` 原子 zip。**`infrastructure/migrations/backup.py`**：抽取模块级 `_verify_openable_sqlite(path, *, open_message, verify_message)`（还原 `create_pre_upgrade_backup` 原文案"Backup is not a valid SQLite database"/"Backup verification failed"），新增 `create_consistent_snapshot(db_path, target_path)`（缺源拒绝 `MigrationBackupError`、`sqlite3.Connection.backup` 一致性 snapshot、`backup_ok` 标志失败统一清理半成品 target）。**`infrastructure/open_directory.py`**：`ARCHIVE_EXTENSION = ".aiproject"`、`write_archive(directory, archive_path)`（`_iter_container_files` 逐层 iterdir 拒绝 link/junction 目录、跳过非文件；`zipfile.ZIP_DEFLATED`；`_mkstemp_sibling` 后 `os.close(fd)`；`os.replace` 原子写；`PathValidationError` 透传、OSError 包装 `OpenDirectoryError`；失败清理临时文件）。新增 `tests/test_p1_t02_m02.py` **23 项**（见 §5 索引）。
- **修改文件：** `src/transrealm/application/archive_service.py`（新增）；`src/transrealm/infrastructure/migrations/backup.py`（`_verify_openable_sqlite` + `create_consistent_snapshot`）；`src/transrealm/infrastructure/open_directory.py`（`ARCHIVE_EXTENSION` + `write_archive`/`_iter_container_files`）；`tests/test_p1_t02_m02.py`（新增 23 项）；权威文档 `03` §3（`.aiproject` 归档段落）、`04` §7（归档视图段落）、`07` P1-T02（状态 in_progress、M02 完成证据）、`08` §8（M02 取舍记录）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p1_t01_m01.py tests/test_p1_t02_m01.py tests/test_p1_t02_m02.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **1019 passed**（基线 996 + M02 新增 23，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **216 passed**（193 旧 + M02 新增 23，1 skipped）；Ruff All checks passed；mypy Success: no issues found in 102 source files。
- **未完成：** P1-T02-M03 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review 无 BLOCKER；1 SHOULD-FIX（`create_consistent_snapshot` 首段 try 失败残留半成品 target）与 MINOR 加固（`_verify_openable_sqlite` 错误文案漂移还原、`write_archive` junction 目录洞改逐层 link 拒绝、`write_archive` OSError 包装 `OpenDirectoryError`、缺源静默建空快照改显式拒绝）已修复并补测试；其余 MINOR 记录取舍：(1) 导出打开同 schema 容器（M02 前提）；跨版本迁移的容器内 pre-upgrade 备份属 P1-T02-M04 范围；(2) 归档不设 entry 数/大小/压缩比限额、无 zip-bomb 防护——隔离导入与资源限额属 P1-T02-M03；(3) `software` 记录导出时 app_version（生成该归档的应用版本），与容器 `software` 同义；(4) 附件经 staging 复制一次后 zip（V1.0 附件规模可接受）；(5) 裸 DB 导出只含 project.sqlite（无附件/无日志）。已知边界：`write_archive` 逐层拒绝 link，Windows junction 在列出时即被 `is_link`（reparse point）捕获。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T02-M03（隔离安全导入），状态 ready，plan_alignment pending_reality_check。开工前先核验 M01/M02 已落地的 manifest 契约与归档布局（`infrastructure/open_directory.py` `write_archive`、`application/archive_service.py`）、M01 路径/hash/link 校验 seam 与隔离 staging 落点；按 `04` §7 判定 `DEC-P1-T02-CONTAINER` 是否触发（M01 因范围已在 `04` §7 裁决未触发；M03 的 entry 数/单项/总大小/压缩比阈值按 `04` §7 属 fixture 与 Release Gate 固定，若现场证实属已裁决实现阈值则照常执行，否则按 `06` 决策节点流程处理）；完成 Reality Check 后从最小隔离导入垂直闭环开始，不越出 P1-T02。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T02-M01 共享 manifest 与开放目录容器（Reality Check **ADAPT**）。现场核验：`ProjectService`/`MigrationRunner`/P0-T03-M00 备份 seam 与 `03` §3、`04` §2/7 双形态契约齐备且满足 M01 范围；无 schema 变更故不引入新 migration；M01 范围已在 `04` §7 裁决，不触发 DEC-P1-T02-CONTAINER（资源限额/Unicode 冲突/隔离 staging 属 M03）。**新增 `infrastructure/open_directory.py`**：`MANIFEST_FORMAT_VERSION=1`/`SUPPORTED_SCHEMA_VERSIONS={1}`/`DATABASE_FILENAME`/`ManifestInfo`/`ManifestEntry`，`build_manifest`/`write_manifest`（mkstemp+os.replace 原子写）/`load_manifest`（`object_pairs_hook` 拒绝顶层与嵌套重复 key；格式/软件/文件结构校验；hash 须 `sha256:<64hex>`）/`validate_manifest`（恰一个 `database` 型 `project.sqlite` 关键 entry、manifest 自别名（含大小写/尾部点/空格归一化）拒绝、identity 折叠 NFC+casefold+尾部点空格别名后的等价重复拒绝）/`canonical_relpath`（拒绝绝对路径/盘符/UNC/`..`/`:` 段/控制字符/空路径，`\` 与 `/` 归一化）/`resolve_within_root`（resolve 后 containment，父目录 link 逃逸拒绝）/`is_link`（lstat：S_ISLNK 或 Windows `st_file_attributes & FILE_ATTRIBUTE_REPARSE_POINT` 捕获 junction/symlink，含 NUL 路径 ValueError）/`verify_entries`（字面路径先查 link、逐 entry 常规文件+大小+流式 sha256）/`compute_entry`。**新增 `application/open_directory_service.py`**：`OpenDirectoryProject` 上下文句柄（`directory`/`db_path`/`manifest`/`project`/`close`）；`create_open_directory_project`（目录必须为空拒绝覆盖、`_prepare_directory` 区分新建/既有空目录、经 `ProjectService` 初始化 SQLite 复用 migration+备份 seam、`_remove_pre_upgrade_backup` 清理新建空库的 `*.pre-upgrade-*.db.bak`、`_finalize_database_file` 仅在 `wal_checkpoint(TRUNCATE)` 成功后删 WAL/shm、原子写 manifest、失败 `_cleanup_failed_create` 清理自身产物且保留用户既有空目录、末尾 reopen 自验证）；`open_open_directory_project`（先 `load_manifest`+`validate_manifest`+`verify_entries` 全部通过才打开数据库，`list_projects()` 强制恰一个 Project 身份，`projects.schema_version` 与 manifest 一致，任一失败不触碰数据库/不创建服务泄漏）。**`ProjectService`** 增只读 `list_projects()`（安全薄转发）。新增 `tests/test_p1_t02_m01.py` **76 项**：create 干净容器（无 .bak/WAL）、manifest 字段/hash 正确、重开保持同一 Project、非空目录/文件路径拒绝、失败清理（含既有空目录保留）；open 缺目录/文件路径/多 Project/篡改 db 拒绝、合法附件声明可打开、重开保持容器清洁、篡改 format_version round-trip 拒绝；manifest 缺失/非法 JSON/根非对象/未知 format·schema 版本（5+6 参数化）/缺软件/files 缺失或非对象/entry 结构错误（6 参数化）/顶层与嵌套重复 JSON key 拒绝；critical entry 缺失/重复/改名、manifest 自别名（含尾部点）、大小写/Unicode NFC-NFD/尾部点等价重复拒绝；16 种根外路径与控制字符（绝对/盘符/UNC/`..`/归一化逃逸/`\0`/`\n`/`\t`）拒绝；缺声明文件/大小错/hash 错/目录 entry/junction entry/符号链接 entry/父目录 junction 逃逸拒绝；`is_link` reparse point 属性单测 + 常规文件 false。
- **修改文件：** `src/transrealm/infrastructure/open_directory.py`（新增）；`src/transrealm/application/open_directory_service.py`（新增）；`src/transrealm/application/project_service.py`（`list_projects`）；`tests/test_p1_t02_m01.py`（新增 76 项）；权威文档 `03` §3（开放目录容器/创建/打开校验）、`04` §7（manifest 具体 schema 与校验规则）、`07` P1-T02（状态 in_progress、M01 完成证据）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_project.py tests/test_migrations.py tests/test_database.py tests/test_exceptions.py tests/test_smoke.py tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p1_t01_m01.py tests/test_p1_t02_m01.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **996 passed**（基线 920 + M01 新增 76，1 skipped 因本环境无符号链接创建权限）；受影响旧测试子集 **193 passed**；Ruff All checks passed；mypy Success: no issues found in 100 source files。
- **未完成：** P1-T02-M02 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。独立 Review 无 BLOCKER；1 SHOULD-FIX（NUL 字节路径经 `canonical_relpath` 拒绝 + `is_link` 捕获 ValueError）与 2 项 MINOR 加固（Windows 尾部点/空格别名折入 identity 判重、`_finalize_database_file` 仅在 checkpoint 成功后删 WAL 附属）已修复并补测试；其余 MINOR 记录取舍：(1) manifest `software` 不比对打开时的 `app_version`（前向兼容靠 format/schema 版本，属 M01 范围）；(2) `_HASH_RE` 拒绝大写十六进制 hash（fail-closed，本应用始终小写）；(3) create 尾部 reopen 若失败保留已写完整容器（reopen 对刚写 manifest 是确定的，实际不可达）。已知边界：打开旧 schema 开放目录会经 `ProjectService` 运行 pending migration 并在容器内产生 pre-upgrade 备份，跨版本迁移属 P1-T02-M04 范围，M01 只保证同 schema 可重开。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T02-M02（一致性单文件导出），状态 ready，plan_alignment pending_reality_check。开工前先核验 M01 已落地的 manifest 契约（`infrastructure/open_directory.py`）与 P0-T03-M00 一致性备份 seam，确定 `.aiproject` 归档布局（标准 ZIP + manifest；ZIP 库选择/压缩级别属执行 Agent 可自决项），完成 Reality Check 后从最小单文件导出垂直闭环开始，不越出 P1-T02。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T01-M06 全格式 Gate（Reality Check **FIT**）。复核 M01–M05 六格式 no-op/translated/error/不可写/旧库升级矩阵证据与共享 `FidelityExporter` seam/envelope 一致性：`fidelity.py` 六格式 envelope 字段统一（schema_version/format/encoding/bom/newline/parser_version/source_hash + locator{type, segments}）；`exporter.py` 各 exporter 用与 parser 同一 extract 函数重解析原始 bytes 并逐项验证 locator（TXT span 越界/重叠/sequence/count/decode==source_text；JSON path/span/value；SRT·VTT cue index/id/timing/settings/span/text；ASS·SSA event start/end/span/text）；`008` migration 追加式（可空列 + `(project_id, source_hash, format, parser_version)` 唯一域替换 `003` 索引，旧行满足放宽域）；`ImportService.backfill_fidelity` 校验源 hash + Segment 映射（数量/stable_key/source_text/sequence）一致后同事务写载体。统一执行矩阵：全量 pytest 920 passed、受影响旧测试子集 617 passed、Ruff clean、mypy 97 files no issues。独立 Review Agent 验收 **无 BLOCKER/SHOULD-FIX**；4 MINOR 记录为已知取舍：(1) `TxtExporter` 不校验 envelope `parser_version`（其余五格式 fail-closed；TXT 经 span 越界/重叠/sequence/count + `raw_bytes[start:end].decode()==segment.source_text` 逐项校验，无损坏路径，属策略不一致非安全缺口）；(2) Revision 解析/编码/追加块在五子类重复（当前无漂移，未来编码策略改动需五处同步）；(3) `mkstemp`+`os.replace` 使导出文件 0600（Windows 下权限近装饰性，与 P0-T08-M04 既有行为一致）；(4) `save_fidelity_carrier` 不更新行 `parser_version`/`encoding`，backfill 后行身份可能与 envelope 分歧（当前旧/新 TXT parser 均为 1.0.0，无实际重复导入路径，属潜在隐患）。`07` 状态同步：P1-T01 标记 completed、M06 补完成证据、M05 计数修正 141→155/906→920。
- **修改文件：** `07_Developer_Task_List.md`（P1-T01 completed、M06 completed 证据、M05 计数修正）；`DEVELOPMENT_STATE.md`（frontmatter 指针推进至 P1-T02-M01、§4 当前 Task/小目标、§5 索引加 M06 行、本检查点）。`03`/`04` 无需改动（六格式契约已在 M01–M05 同步）。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_migrations.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p1_t01_m01.py tests/test_p1_t01_m02.py tests/test_p1_t01_m03.py tests/test_p1_t01_m04.py tests/test_p1_t01_m05.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **920 passed**（M01=35、M02=71、M03=75、M04=86、M05=155）；受影响旧测试子集 **617 passed**（462 旧基线 + 155）；Ruff All checks passed；mypy Success: no issues found in 97 source files。
- **未完成：** P1-T02-M01 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。已知取舍见完成内容 4 项 MINOR。`P1-T02` 前置依赖已满足（P1-T01 completed）；工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T02-M01（共享 manifest 与开放目录），状态 ready，plan_alignment pending_reality_check。开工前先核验 P1-T02 现场（P0-T03-M00 一致性备份/migration history/checksum 复用、`03` §3 与 `04` §2/7 双形态契约、一致性 snapshot seam 复用点），完成 Reality Check 后从最小开放目录垂直闭环开始，不越出 P1-T02。

### Previous code checkpoint

- **时间：** 2026-08-07
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T01-M05 ASS/SSA 垂直闭环（裁决方案 2 落地，Reality Check **FIT**）。**新增 `infrastructure/parsers/ass_ssa_parser.py`**：`AssSsaParseError`/`AssSsaEvent`/`extract_events`（单遍 `_scan_lines` 行扫描，含 BOM 偏移）+ `AssParser`/`SsaParser`（共享 `_AssSsaParserBase`，只差 format/ScriptType，不创建两套无必要框架）。规则：文件必须以 `[Script Info]` 开头（SRT 等非 SubStation 内容不静默降级）；`ScriptType:` 必须匹配 format（ass=`v4.00+`、ssa=`v4.00`，错配拒绝防静默降级）；行分类经 strip + `^key\s*:` 归一化匹配（尾空格 section 头/缩进行/`Key :` 变体均识别，不会静默吞掉事件）；`[Events]` 必须声明标准 `Format:` 字段表（v4 `Marked,Start,End,Style,Name,MarginL,MarginR,MarginV,Effect,Text`；v4+ `Layer,Start,...`；大小写不敏感、按逗号 strip 后比较，缺/重复/非标准字段表拒绝，避免猜测 Text 分割逗号数）；每个 `Dialogue:` 事件按字段数 `split(",", maxsplit=N-1)` 取最后 Text 字段（逗号/行内 override 标签/`\N`/`\h` 转义/首尾空格原样保留）生成 Segment，`ass.events`/`ssa.events` locator 记录 sequence/原样 start/end/Text byte span（含 BOM 偏移）；Start/End（字段 1/2）按 `h:mm:ss.cc` 校验（小时 1+ 位、分钟/秒 00-59、百分秒恰 2 位）；空/纯空白 Text 拒绝（与 SRT/VTT 一致）；重复 start/end 时间轴允许（ASS/SSA 多行同时对话按样式区分是合法结构，与 SRT/VTT 的拒绝不同，stable key 含 sequence）；`Comment:` 与其他事件类型（Picture/Sound/Movie 等）、section 头、`Format:` 行、Style 定义、`;` 注释、所有非 Text 字段与行尾换行一律不翻译、逐字节保留（ASS/SSA 事件是单物理行，换行经 `\N`/`\h` 转义）。**`fidelity.py`** 加 `build_ass_envelope`/`build_ssa_envelope`/`LOCATOR_TYPE_ASS_EVENTS`/`LOCATOR_TYPE_SSA_EVENTS`（共享 `_build_event_envelope`）。**`exporter.py`** 新增 `_AssSsaExporter(FidelityExporter)`：复用共享 seam，替换前用同一 `extract_events` 重解析原始 bytes，逐 Segment 验证 sequence/start/end/byte span/文本==source_text 与 parser_version，译文以内容编码直接替换目标 Text 字段（ASS/SSA 无转义机制，行内 override 标签属目标 span）；`AssExporter`/`SsaExporter` 只设 format。**`parser.py`** `default_registry()` 注册 `AssParser`/`SsaParser`。新增 `tests/test_p1_t01_m05.py` **155 项**：no-op 字节一致（LF/CRLF/CR/UTF-8 BOM/UTF-16 LE/BE/全文件含 section/style/comment/Comment 事件/标签/转义/逗号文本/EOF 无尾换行/header-only）；翻译仅改 Dialogue Text（单/多 Dialogue、Comment 不译、逗号、标签转义、首尾空格整字段替换、CRLF、UTF-16、Revision override、round-trip 重导入）；空白变体不静默吞掉（尾空格 section 头/缩进 Dialogue/`Key :`/小写 key/缩进注释结构性）；畸形拒绝无污染（缺签名/错配 ScriptType/缺 ScriptType/缺或非标准或重复 Format/字段数不足/空或纯空白文本/非法时间轴含坏分隔与分钟秒超 59 与百分秒位数错）；envelope 篡改（start/end/sequence/span/source_text/hash/BOM/newline/schema/parser version/format/raw bytes/locator/数量/re-parse 失败）拒绝 + `locator.type` 单独篡改无害（固化共享 seam 行为）；encoding override 匹配/错配；编码失败/不可写目标/缺载体不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize；同身份幂等 + 同 bytes ass/ssa/txt 分离。独立 Review Agent 验收 **APPROVE-WITH-MINORS**（1 SHOULD-FIX 行分类空白敏感会静默吞掉全部事件，已用 strip + `^key\s*:` 归一化修复并补 10 项空白变体测试；其余 MINOR 为已记录取舍/共享 seam 行为，`locator.type` 单独篡改无害与 `\h` 转义各补 2 项测试固化）。
- **修改文件：** `src/transrealm/infrastructure/parsers/ass_ssa_parser.py`（新增）；`src/transrealm/infrastructure/fidelity.py`（`build_ass_envelope`/`build_ssa_envelope`/`LOCATOR_TYPE_ASS_EVENTS`/`LOCATOR_TYPE_SSA_EVENTS`）；`src/transrealm/application/exporter.py`（`_AssSsaExporter`/`AssExporter`/`SsaExporter`）；`src/transrealm/infrastructure/parsers/parser.py`（注册 `AssParser`/`SsaParser`）；`tests/test_p1_t01_m05.py`（新增 155 项）；权威文档 `03` §9（ASS/SSA 定位/签名与 ScriptType 判别/Events Format 校验/时间戳规则/拒绝策略/取舍）、`04` §3（envelope 补 `ass.events`/`ssa.events` locator）、`07` P1-T01-M05（completed）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集同 M04 并含 M05：`py -3.12 -m pytest -q tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_migrations.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p1_t01_m01.py tests/test_p1_t01_m02.py tests/test_p1_t01_m03.py tests/test_p1_t01_m04.py tests/test_p1_t01_m05.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **920 passed**（基线 765 + M05 新增 155）；受影响旧测试子集 **617 passed**（462 旧基线 + 155）；Ruff All checks passed；mypy Success: no issues found in 97 source files。
- **未完成：** P1-T01-M06 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。已知取舍：严格 `[Script Info]` 开头与 ScriptType 匹配（VTT 签名先例，防静默降级；非标准 section 头如 `[Events]extra` 按结构性行跳过不报错）；仅标准 Events Format 字段表可解析（非标准字段表 fail-safe 拒绝）；重复 start/end 时间轴允许（ASS/SSA 合法，与 SRT/VTT 拒绝不同）；Dialogue Text 含首尾空格时按整字段替换（目标 span，允许改变）；`locator.type` 与 envelope `encoding` 篡改校验为共享 seam 行为，与 TXT/JSON/SRT/VTT 一致。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T01-M06（全格式 Gate），状态 ready，plan_alignment pending_reality_check。开工前先复核 M01–M05 六格式 no-op/translated/error/旧库升级矩阵证据、共享 seam 与 envelope 一致性，完成 Reality Check 后统一执行矩阵、全回归与 Code Review，不越出 P1-T01。

### Previous code checkpoint

- **时间：** 2026-08-06
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T01-M04 VTT 垂直闭环（裁决方案 2 落地，Reality Check **FIT**）。**新增 `infrastructure/parsers/vtt_parser.py`**：`VTTParser` + 单遍物理行扫描 `extract_cues`（`_scan_lines` 逐行累积字节偏移，含 BOM 偏移）：先校验文件签名——解码文本必须以 `WEBVTT` 开头（可带 BOM，后随空格标题或行尾），否则拒绝（SRT 等非 WebVTT 内容不静默降级）；`NOTE`/`STYLE`/`REGION` 块按规范消费到空行为结构性 bytes 永不生成 Segment；每 cue = 可选标识行（非空、不含 `-->`、后随 timing 行）+ timing 行（W3C 规则：完整 `HH:MM:SS.mmm` 小时 1+ 位 / 省略小时 `MM:SS.mmm`，点分隔、毫秒恰 3 位、分钟/秒 00-59）+ 非空文本行直到空行或含 `-->` 的行（该行按规范结束上一 cue 并以之开启新 cue，VTT 不要求 cue 间空行分隔），文本块内部换行原样保留；`vtt.cues` locator 记录 sequence/id/start/end/settings/文本块 byte span；严格校验拒绝缺签名/签名后非空格/坏时间轴/重复 cue 时间轴（规范化 start/end，含完整/省略小时等价）/空 cue 文本/bad cue 标识（正文裸文本行）/含 `-->` 非 timing 文本行，导入期拒绝且不污染 Project。**`fidelity.py`** 加 `build_vtt_envelope`/`LOCATOR_TYPE_VTT_CUES`。**`exporter.py`** 新增 `VttExporter`：复用 `FidelityExporter` seam，替换前用同一 `extract_cues` 重解析原始 bytes，逐 Segment 验证 sequence/id/start/end/settings/byte span/文本==source_text 与 parser_version，译文以内容编码直接替换目标文本块（VTT 无转义机制；内联 `<b>/<v>` 等标签属目标 span，no-op 逐字节保留、翻译由译文整体替换）。**`parser.py`** `default_registry()` 注册 `VTTParser`。新增 `tests/test_p1_t01_m04.py` **86 项**：no-op 字节一致（LF/CRLF/CR-only/UTF-8 BOM/UTF-16 LE/BE/header+NOTE/STYLE/REGION/内联标签/部分+完整时间戳/多行/首尾空行/header-only/EOF 无尾换行/无空行分隔 cue）；翻译仅改 cue 文本块（单/多 cue、带 ID、settings+点分隔、多行源压缩为单行、译文含换行、内联标签 round-trip、blocks/header title 保留、CRLF 多行、UTF-16 替换、Revision override、round-trip 重导入）；非法导入拒绝无污染（空文件/缺签名/SRT 逗号时间戳/签名后非空格/坏时间轴含毫秒位数与分钟秒越界/重复定位/空文本/bad cue 标识/含 `-->` 非 timing/缺 start 或 end 时间戳）；envelope 篡改（id/timing/settings/span/sequence/source_text/hash/BOM/newline/schema/parser version/format/raw bytes/locator/数量/re-parse 失败）拒绝；encoding override 匹配成功/错配拒绝；编码失败/不可写目标/缺载体不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize；同身份幂等 + 同 bytes vtt/txt 分离。独立 Review Agent 验收 **APPROVE-WITH-MINORS**（无 BLOCKER；1 SHOULD-FIX 时间戳分钟/秒超 59 未校验，已修复并补测试；其余 MINOR 为已记录取舍/共享 seam 行为）。
- **修改文件：** `src/transrealm/infrastructure/parsers/vtt_parser.py`（新增）；`src/transrealm/infrastructure/fidelity.py`（`build_vtt_envelope`/`LOCATOR_TYPE_VTT_CUES`）；`src/transrealm/application/exporter.py`（`VttExporter`，含 ruff isort 拆分 vtt_parser 别名导入）；`src/transrealm/infrastructure/parsers/parser.py`（注册 `VTTParser`）；`tests/test_p1_t01_m04.py`（新增 86 项）；权威文档 `03` §9（VTT 定位/时间戳规则/校验/拒绝策略/取舍）、`04` §3（envelope 补 `vtt.cues` locator）、`07` P1-T01-M04（completed）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集同 M01/M02/M03 并含 M03：`py -3.12 -m pytest -q tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_migrations.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p1_t01_m01.py tests/test_p1_t01_m02.py tests/test_p1_t01_m03.py tests/test_p1_t01_m04.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **765 passed**（基线 679 + M04 新增 86）；受影响旧测试子集 462 passed（195 旧基线 + M01/M02/M03/M04 各 35/71/75/86）；Ruff All checks passed；mypy Success: no issues found in 95 source files。
- **未完成：** P1-T01-M05 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。已知取舍：`Kind:`/`Language:`/`X-TIMESTAMP-MAP` 等 WHATWG 扩展 metadata 头与缩进 `STYLE` 按 fail-safe 拒绝（保留项，不属 M04 范围，拒绝而非静默重排，满足"非法 metadata 不静默降级"）；`-->` 周围空白宽容（`\s*`，与 SRT 一致，settings 尾部含 `-->` 被原样保存不解释）；`locator.type` 未单独校验、envelope `encoding` 篡改对纯 ASCII 内容不可察觉（Txt/Json/Srt 同样，逐字段校验已覆盖，属共享 seam 既有行为）；整块 cue payload（含内联标签）为目标 span，与 SRT 文本块语义一致。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T01-M05（ASS/SSA 垂直闭环），状态 ready，plan_alignment pending_reality_check。开工前先复用 M01–M04 已批准的 envelope/唯一域/exporter seam，按裁决为 ASS/SSA 定义格式专属定位信息（不套用 TXT/JSON/SRT/VTT 语义），核验 Parser Registry/Import/Exporter 落点后从最小 ASS 垂直闭环开始，不越出 P1-T01。

### Previous code checkpoint

- **时间：** 2026-08-06
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T01-M03 SRT 垂直闭环（裁决方案 2 落地，Reality Check **ADAPT**）。**新增 `infrastructure/parsers/srt_parser.py`**：`SRTParser` + 单遍物理行扫描 `extract_cues`（`_scan_lines` 逐行累积字节偏移，含 BOM 偏移）：可选 index 行（index-less cue 支持）+ timing 行（`HH:MM:SS[,.]mmm --> ...`，逗号/点分隔符原样保留）+ 非空文本行直到空行，文本块内部换行原样保留；`srt.cues` locator 记录 sequence/index/start/end/settings/文本块 byte span；严格校验拒绝非法时间轴（缺 `-->`/坏分隔/分钟秒>59/毫秒位数错）、重复 cue 时间轴（规范化 start/end，含逗号/点差异）、空 cue 文本、缺失空行分隔（文本行形似下一 cue 序号+timing）、序号行后缺失 timing，导入期拒绝且不污染 Project；形似 timing 的文本行按规范视为文本。**`fidelity.py`** 加 `build_srt_envelope`/`LOCATOR_TYPE_SRT_CUES`。**`exporter.py`** 新增 `SrtExporter`：复用 `FidelityExporter` seam，替换前用同一 `extract_cues` 重解析原始 bytes，逐 Segment 验证 sequence/index/start/end/settings/byte span/文本==source_text 与 parser_version，译文以内容编码直接替换目标文本块（SRT 无转义机制）。**`parser.py`** `default_registry()` 注册 `SRTParser`。新增 `tests/test_p1_t01_m03.py` **75 项**：no-op 字节一致（LF/CRLF/CR-only/UTF-8 BOM/UTF-16 LE/BE/多行/首尾空行/settings/点分隔/index-less/EOF 无尾换行）；翻译仅改 cue 文本块（单/多 cue、index-less、多行源压缩为单行、译文含换行、settings/点分隔保留、CRLF 多行、UTF-16 替换、Revision override、round-trip 重导入）；非法时间轴/重复定位/空文本/缺分隔/序号缺 timing 拒绝无污染；envelope 篡改（index/timing/settings/span/sequence/source_text/hash/BOM/newline/schema/parser version/format/raw bytes/locator/数量/re-parse 失败）拒绝；encoding override 匹配成功/错配拒绝；编码失败/不可写目标/缺载体不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize；同身份幂等 + 同 bytes srt/txt 分离。独立 Review Agent 验收 **APPROVE**（无 BLOCKER/SHOULD-FIX；4 MINOR 为已记录取舍/既有共享行为，其中 3 项测试缺口已补齐）。
- **修改文件：** `src/transrealm/infrastructure/parsers/srt_parser.py`（新增）；`src/transrealm/infrastructure/fidelity.py`（`build_srt_envelope`/`LOCATOR_TYPE_SRT_CUES`）；`src/transrealm/application/exporter.py`（`SrtExporter`）；`src/transrealm/infrastructure/parsers/parser.py`（注册 `SRTParser`）；`tests/test_p1_t01_m03.py`（新增 75 项）；权威文档 `03` §9（SRT 定位/校验/拒绝策略）、`04` §3（envelope 补 `srt.cues` locator）、`07` P1-T01-M03（completed）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集同 M01/M02：`py -3.12 -m pytest -q tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_migrations.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p1_t01_m01.py tests/test_p1_t01_m02.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **679 passed**（基线 604 + M03 新增 75）；受影响旧测试子集 301 passed；Ruff All checks passed；mypy Success: no issues found in 93 source files。
- **未完成：** P1-T01-M04 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。已知取舍：SRT 小时限 `\d{1,2}`（>99h 拒绝而非误解析，安全）；`locator.type` 未单独校验（Txt/Json 同样，逐字段校验已覆盖，篡改无害）；`_validate_encoding_override` 对比内容 codec（UTF-16 文档传 `encoding="utf-16"` 被拒，既有共享行为，M01/M02 一致）；多行文本块内部换行属目标 bytes 可随译文改变（块边界换行保持）；index-less 与 text 形似 timing 均按规范处理，不静默重排 cue。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T01-M04（VTT 垂直闭环），状态 ready，plan_alignment pending_reality_check。开工前先复用已批准的 envelope/唯一域/exporter seam，按裁决为 VTT 定义格式专属定位信息（不套用 TXT byte span、JSON path 或 SRT cue 语义），核验 Parser Registry/Import/Exporter 落点后从最小 VTT 垂直闭环开始，不越出 P1-T01。

### Previous code checkpoint

- **时间：** 2026-08-06
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T01-M02 JSON 垂直闭环（裁决方案 2 落地，Reality Check **ADAPT**）。**新增 `infrastructure/parsers/json_parser.py`**：`JSONParser` + 单遍迭代 tokenizer（`extract_string_leaves`，深度上限 512、无递归、按字符累加字节偏移）只把 value 位置的字符串 token 生成为 Segment，逐 Segment 记录 JSON Pointer 结构路径（`~`/`/` 经 `~0`/`~1` 转义）与 value token byte span（含引号、含 BOM 偏移）；strict 校验拒绝 malformed（未闭合/尾逗号/非法数字/非法转义/字符串内控制字符/根值后尾随 token/空文件）、重复对象 key、超 512 深度、未配对 `\u` 代理项。**`fidelity.py`** 加 `build_json_envelope`/`LOCATOR_TYPE_JSON_PATHS`（`json.paths` locator）。**`exporter.py`** 提取 `FidelityExporter` 基类（共享 carrier 加载、envelope 验证、Revision 归属校验、`mkstemp`+`os.replace` 原子写、context manager），`TxtExporter` 公共 API 与字节行为不变；新增 `JsonExporter`：替换前用同一 tokenizer 重解析原始 bytes，逐 Segment 验证 sequence/path/byte span/value==source_text 与 parser_version，译文经 `json.dumps(ensure_ascii=False)` 转义为合法 JSON string literal 后替换目标 token。**`parser.py`** `default_registry()` 注册 `JSONParser`。新增 `tests/test_p1_t01_m02.py` **71 项**：no-op 字节一致（嵌套/空白/tab/转义/非 ASCII/UTF-8 BOM/UTF-16 LE/BE BOM/CRLF/空容器/无字符串文档）；string leaf 稳定结构路径（`/a/b/0`、转义 key、根裸字符串、空字符串 value）；翻译仅改 value token（key/order/number/whitespace/转义逐字节保留、UTF-16 替换、Revision override、语义 round-trip 重导入）；malformed/深度/重复 key/未配对代理项拒绝无污染；envelope 篡改拒绝（path/byte span/value/sequence/source_hash/bom/newline/schema_version/parser_version/format/raw bytes/locator 缺失/数量）；编码失败/不可写目标/缺载体不污染；真实 TranslationService 垂直闭环 round-trip + adapter 失败 finalize；同身份幂等 + 同 bytes txt/json 分离。独立 Review Agent 验收发现 1 项已修复：根值后单尾随 token 误接受（`{"a":"x"} 5`、`5 6`、`true null`、`{} 5` 等被旧实现误收，`json.loads` 均拒绝），修复 `_build_leaves` 后加 6 用例；其余 TXT 回归、验证充分性、范围、测试缺口均无问题。
- **修改文件：** `src/transrealm/infrastructure/parsers/json_parser.py`（新增）；`src/transrealm/infrastructure/parsers/parser.py`（注册 `JSONParser`）；`src/transrealm/infrastructure/fidelity.py`（`build_json_envelope`/`LOCATOR_TYPE_JSON_PATHS`）；`src/transrealm/application/exporter.py`（提取 `FidelityExporter` 基类 + 新增 `JsonExporter`）；`tests/test_p1_t01_m02.py`（新增 71 项）；权威文档 `03` §9（JSON 定位/校验/拒绝策略）、`04` §3（envelope 描述补 JSON locator）、`07` P1-T01-M02（completed）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集同 M01：`py -3.12 -m pytest -q tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_migrations.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py tests/test_p1_t01_m01.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **604 passed**（基线 533 + M02 新增 71）；受影响旧测试子集 230 passed；Ruff All checks passed；mypy Success: no issues found in 91 source files。
- **未完成：** P1-T01-M03 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。已知取舍：翻译导出时任一 Segment 缺 Revision 即失败（保留 M04 契约，含空字符串 value 的 JSON，与 TXT 一致）；no-op 需显式 `apply_revisions=False`；envelope `newline` 字段对含 `\n` 转义的 JSON 值按解码文本自洽判定（parser/exporter 同一函数，自校验一致，不影响字节保真）；未配对代理项拒绝（非 Unicode 标量，避免 stable_key 哈希与 prompt 编码崩溃）；`TxtExporter` 默认 `encoding=None` 与 `encoding` 覆盖一致性校验为 M01 已裁决行为（非 M02 回归）。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T01-M03（SRT 垂直闭环），状态 ready，plan_alignment pending_reality_check。开工前先复用已批准的 envelope/唯一域/共享 `FidelityExporter` seam，按裁决为 SRT 定义其格式专属定位信息（不套用 TXT byte span 或 JSON path 语义），核验 Parser Registry/Import/Exporter 落点后从最小 SRT 垂直闭环开始，不越出 P1-T01。

### Previous code checkpoint

- **时间：** 2026-08-06
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P1-T01-M01 保真载体与 TXT 证明（裁决方案 2 落地，Reality Check **ADAPT**）。**新增 `008_add_format_fidelity.sql`**：`source_documents` 加 `raw_bytes`（BLOB）/`format_metadata`（JSON envelope）列；逻辑唯一域索引换为 `(project_id, source_hash, format, parser_version)` 并删除 `003` 的 `(project_id, source_hash)` 索引（迁移在显式事务内原子执行，旧数据必满足放宽后的唯一域）。**新增 `infrastructure/fidelity.py`**：`FIDELITY_SCHEMA_VERSION`、`build_txt_envelope`/`load_envelope`、`detect_bom`/`resolve_encodings`/`detect_newline`，parser 与 exporter 共享。**重写 `TxtParser`**：单遍 `_extract_segments`（无逐字符偏移列表），匹配 `splitlines()` 全边界（\n/\r/\v/\f/\x1c-\x1e/\x85/U+2028/U+2029），逐 Segment 生成 byte span（含 BOM 偏移），Segment 映射与旧 parser 一致、`version` 保持 `1.0.0`；UTF-8 多字节/BOM、UTF-16 LE/BE BOM、CRLF/CR 正确处理。**重写 `TxtExporter`** 为保真 seam：`export_document(..., encoding=None, apply_revisions=True)`；缺载体安全拒绝；`_verify_envelope` 对称校验 hash/encoding/BOM（双向 `detect_bom`）/newline；`_build_replacement_plan` 逐 span 校验越界/重叠/sequence/解码等于 source_text 后按 Revision 编码替换；`apply_revisions=False` 逐字节输出 raw bytes；`encoding` 覆盖仅作一致性校验；原子写（mkstemp+os.replace）失败不污染目标。**`ImportService.backfill_fidelity`**：用户提供原文件，校验源 hash 与既有 Segment 映射（数量/stable_key/source_text/sequence）一致后同事务写载体。**`SourceDocument`** 加 `raw_bytes`/`format_metadata`；**`SegmentRepository`** 更新 INSERT/SELECT/映射、去重冲突目标换为身份域、新增 `save_fidelity_carrier`/`find_source_document_by_identity`。独立 Review Agent 验收发现 3 项已修复：splitlines 边界回归（U+2028 等）、BOM 单向校验、逐字符偏移内存。
- **修改文件：** `src/transrealm/migrations/008_add_format_fidelity.sql`、`src/transrealm/infrastructure/fidelity.py`（新增）；`src/transrealm/infrastructure/parsers/txt_parser.py`、`src/transrealm/application/exporter.py`（重写）；`src/transrealm/application/import_service.py`（`backfill_fidelity`）；`src/transrealm/domain/segment.py`；`src/transrealm/infrastructure/repositories/segment_repository.py`；`tests/test_p1_t01_m01.py`（新增 35 项）；`tests/test_migrations.py`（2 项按 008 索引更新）；权威文档 `03` §9、`04` §2/3/5/6、`07` P1-T01、`08`（DEC-P1-T01-FOUNDATION 取舍记录）；`DEVELOPMENT_STATE.md`。
- **测试命令：** `py -3.12 -m pytest -q`；受影响旧测试子集：`py -3.12 -m pytest -q tests/test_txt_parser.py tests/test_p0_t08_m04.py tests/test_p0_t08_m05.py tests/test_migrations.py tests/test_p0_t07_m01.py tests/test_p0_t07_m02.py tests/test_p0_t07_m03.py tests/test_p0_t07_m04.py tests/test_p0_t07_m05.py tests/test_p0_t08_m01.py tests/test_p0_t08_m02.py tests/test_p0_t08_m03.py`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量 pytest **533 passed**（基线 498 + M01 新增 35）；受影响旧测试子集 195 passed；Ruff All checks passed；mypy Success: no issues found in 89 source files。
- **未完成：** P1-T01-M02 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。已知取舍：缺载体旧库必须用户提供原文件补齐后才能保真导出（按裁决不从 source_text 猜造 bytes）；`apply_revisions=True` 时任一 Segment 缺 Revision 仍失败（保留 M04 契约），no-op 需显式 `apply_revisions=False`；`encoding` 覆盖只作一致性校验，不再改写输出编码；解析整文件进内存（偏移已优化为单遍），极端文件大小限制属 P1-T01 全格式 Gate 的通用安全验收项。工作树改动未提交（`governance_commit_pending`），未获授权不 commit/push。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T01-M02（JSON 垂直闭环），状态 ready，plan_alignment pending_reality_check。开工前先复用已批准的 envelope/唯一域/exporter seam，按裁决为 JSON 定义格式专属定位信息（不套用 TXT byte span 语义），核验 Parser Registry/Import/Exporter 落点后从最小 JSON 垂直闭环开始，不越出 P1-T01。

### Previous code checkpoint

- **时间：** 2026-08-06
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P0-T08-M06 Phase 0 Gate。E2E/GUI review：复核无 UI 与 GUI 两条入口对成功/失败/关闭/reopen 的证据——M01 无 UI 成功闭环（可重开 Revision、真实 adapter+fake transport、空 Segment/缺 Profile/预算不足/非 running 不发请求）、M02 生产调用边界（stdlib transport 重定向 Authorization 边界、401/429/500/418 稳定归类、secret 不泄、真实本地 server 闭环）、M03 失败与有限修复 E2E（repair 新建 Attempt、永久错误不业务重试、崩溃重启 lease 恢复重译）、M04 Revision 驱动 TXT 导出（原子写、缺 Revision 不回退源文）、M05 非阻塞 GUI（完整闭环/主线程 QTimer 不阻塞/取消在 Segment 边界/关闭收敛无 processing、Attempt 全终态）；GUI 错误路径经 `task_error`→`_handle_error`→status 标签确认存在。全量质量命令通过：pytest 488 passed、Ruff All checks passed、mypy Success no issues in 87 source files。**打包 smoke：** PyInstaller 6.21.0（dev 工具，不写 pyproject、不冻结）+ hooks-contrib 2026.6；`--onedir --windowed` 单目录 build ~58s、体积约 123 MB（PySide6 主导）；exe 启动进程存活、主窗口 "TransRealm" 经 EnumWindows 可见、`~/.transrealm/project.sqlite` 由 frozen app 创建。Qt 插件：platforms/qwindows.dll/styles/iconengines/imageformats/tls 等自动收集；应用只用 stdlib sqlite3、不导入 QtSql，故 sqldrivers 缺失属预期。**关键发现并验证修复：** 迁移 SQL 数据文件（`transrealm/migrations/*.sql`）不被 PyInstaller 自动收集——初始冻结应用仅建空 `schema_migrations`、无法应用迁移；加 `--add-data "src/transrealm/migrations;transrealm/migrations"` 后全部 6 个迁移应用、11 张业务表创建成功。记录观察：DPI 150% 下窗口物理约 1754×597（Qt 布局最小宽度+WM，外观非阻塞）。smoke 产物在 `%TEMP%\transrealm_smoke_20260806\`（build/dist/截图），属临时证据，已清理进程与 smoke 创建的 `~/.transrealm`。
- **修改文件：** `03_Technical_Design.md`（§10 补充 Phase 0 build/start smoke 与迁移 SQL `--add-data` 发现）；`README.md`（新增"打包 smoke"节与命令）；`07_Developer_Task_List.md`（P0-T08-M06 completed、P0-T08 标记 completed）；`DEVELOPMENT_STATE.md`（指针推进至 P1-T01-M01，Phase 1/V1.0）。`02/04/05/index` 无需改动：无 schema/数据格式/Prompt 契约/路由变化，打包已在 `03` §10 覆盖，Phase 0 打包承诺在 `02` 已写。
- **测试命令：** `py -3.12 -m pytest -q`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`；`py -3.12 -m PyInstaller --noconfirm --clean --onedir --windowed --name transrealm --paths src --add-data "src/transrealm/migrations;transrealm/migrations" <入口>`
- **测试结果：** 全量回归 pytest 488 passed；Ruff All checks passed；mypy Success: no issues found in 87 source files；PyInstaller build/start 可行性 smoke 成功（见完成内容）。
- **未完成：** P1-T01-M01 未开始（当前指针，ready + pending_reality_check）。
- **风险/阻塞：** 无技术阻塞。PyInstaller 未写进 `pyproject.toml`（Phase 0 不冻结工具），正式依赖锁/体积/UPX/图标/签名/杀毒/WAL 恢复属 V1.0 Release Gate；smoke 时窗口物理尺寸受 DPI/布局影响为外观观察，V1.0 再做布局 QA。
- **恢复动作：** 读取本文件；当时的小目标为 P1-T01-M01，状态 ready，plan_alignment pending_reality_check。开工前先核验 SourceDocument/Segment 持久化、`TxtExporter` seam 与旧 TXT Project 升级路径，完成 Reality Check 后从保真载体与 TXT 证明的垂直切片开始，不越出 P1-T01。

### Previous code checkpoint

- **时间：** 2026-08-04
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P0-T08-M05 非阻塞桌面闭环。新增 `src/transrealm/ui/worker.py`（`ServiceWorker` 通用任务执行器 + `TranslationWorker` 逐 Segment 翻译循环）、`ui/pages.py`（`SettingsPage`/`ProjectPage`/`TranslationPage`，全部经 worker 调 Application Service，含 `import_file`/`export_to`/`translate` 免对话框测试入口）、`ui/main_window.py`（三 Tab 主窗口，构造即启动 ServiceWorker 与 TranslationWorker 线程；`closeEvent` 先 `request_stop()` 再 `thread.quit()`+有界 `wait()` 收敛）。**关键修复：** 直接调用 `moveToThread` 后对象的 `@Slot` 方法会在调用线程执行（线程亲和性只对信号连接生效），故 worker 槽改为经 `run_requested`/`start_translate` 信号触发（排队到各自线程），SQLite/Provider/模型调用真正不阻塞主线程。取消经主线程直接置位的 `_stop_requested` 标志在 Segment 边界生效。**依赖：** 安装 PySide6 6.11.1 + pytest-qt 4.5.0，`pyproject.toml` `dependencies=["PySide6>=6.8,<6.12"]`、dev 加 `pytest-qt>=4.2`。Reality Check 结论 **FIT**（P0-T08 预授权；版本/许可 LGPL+MIT/安全/锁定/打包已记录）。新增 `tests/test_p0_t08_m05.py` 4 项（pytest-qt）：UI 完整闭环（建项目/导入/建 Profile/翻译/导出，翻译在 worker 线程、主线程 QTimer 在翻译中触发且 progress<total 证明不阻塞）、取消在 Segment 边界停止（progress<total）、关闭收敛（closeEvent 后无 processing Segment、Attempt 全部终态）。测试用 `_wait_finished`（连接 flag + `waitUntil`）替代跨线程 `waitSignal`（后者会在 worker 槽运行前提前返回）。
- **修改文件：** `src/transrealm/ui/worker.py`、`ui/pages.py`、`ui/main_window.py`（新增）；`src/transrealm/ui/__init__.py`（docstring）；`src/transrealm/infrastructure/repositories/segment_repository.py`（`list_source_documents_by_project`）；`pyproject.toml`（PySide6/pytest-qt 依赖）；`tests/test_p0_t08_m05.py`（新增）；`07_Developer_Task_List.md`（P0-T08-M05 标记 completed）；`03_Technical_Design.md`（§10 补充 Phase 0 桌面壳实现说明）；`DEVELOPMENT_STATE.md`（指针推进至 P0-T08-M06）。`02/04/05/index` 无需改动：无 schema/数据格式/Prompt 契约变化，TXT E2E/GUI 路由已覆盖 `ui/`。
- **测试命令：** `py -3.12 -m pytest -q`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量回归 pytest 488 passed（M05 新增 4 项）；Ruff All checks passed；mypy Success: no issues found in 86 source files。
- **未完成：** P0-T08-M06 未开始。
- **风险/阻塞：** 无技术阻塞。已知取舍：关闭时 `thread.wait(5000)` 若当前段模型调用超长会超时（Phase 0 可接受，T07 lease 兜底）；`request_stop` 跨线程布尔标志依赖 GIL 可见性（Phase 0 可接受）；PySide6 wheel 大（数百 MB），正式打包/体积/杀毒属 V1.0 Release Gate。
- **恢复动作：** 读取本文件；当时的小目标为 P0-T08-M06，状态 ready，plan_alignment pending_reality_check。开工前先复核全量质量命令与 M01–M05 E2E 证据，确认打包候选（PyInstaller）及 PySide6 hook 支持，完成 build/start 可行性 smoke 后同步文档与状态，不越出 P0-T08。

### Previous code checkpoint

- **时间：** 2026-08-03
- **Agent：** Claude（执行 Agent）
- **完成内容：** 完成 P0-T08-M04 Revision 驱动 TXT 导出。新增 `src/transrealm/application/exporter.py`（`TxtExporter`/`ExportError`）：构造运行 migration（与其他 Application Service 一致），只读连接；`export_document(source_document_id, target_path, *, revision_overrides=None, encoding='utf-8')` 按 `ORDER BY sequence` 原顺序取 Segment，默认写 `current_revision_id` 对应 Revision 文本；`revision_overrides: dict[segment_id, revision_id]` 显式选择并逐 Revision 校验存在/归属（`segment_id`），未知 segment 键报错；任一 Segment 缺可用 Revision 即失败、不回退源文；同目录 `tempfile.mkstemp` + `os.replace` 原子写，编码失败（UnicodeEncodeError/LookupError）/目标不可写清理临时文件并保持目标不变；默认 UTF-8（源编码/BOM 保真属 P1-T01）。`SegmentRepository` 增 `get_source_document_by_id` 只读方法（文档存在性校验）。Reality Check 结论 **FIT**：零新依赖（stdlib tempfile/os）。新增 `tests/test_p0_t08_m04.py` 13 项：默认 current 按原顺序导出（含顺序/UTF-8 round-trip）、缺失/部分翻译失败无文件、invalid/cross-segment/未知 segment override 失败、显式非当前有效 Revision 覆盖生效、编码失败保留旧目标且无临时残留、Revision 校验失败保留旧目标、目标目录缺失失败、文档不存在失败。
- **修改文件：** `src/transrealm/application/exporter.py`（新增）；`src/transrealm/infrastructure/repositories/segment_repository.py`（`get_source_document_by_id`）；`tests/test_p0_t08_m04.py`（新增）；`07_Developer_Task_List.md`（P0-T08-M04 标记 completed）；`03_Technical_Design.md`（§9 补充 Phase 0 TxtExporter 实现说明）；`DEVELOPMENT_STATE.md`（指针推进至 P0-T08-M05）。`02/04/05/index` 无需改动：无 schema/数据格式/Prompt 契约/新领域变化，Exporter 路由已覆盖。
- **测试命令：** `py -3.12 -m pytest -q`；`py -3.12 -m ruff check src tests`；`py -3.12 -m mypy src tests`
- **测试结果：** 全量回归 pytest 484 passed（M04 新增 13 项）；Ruff All checks passed；mypy Success: no issues found in 82 source files。
- **未完成：** P0-T08-M05 未开始。
- **风险/阻塞：** 无技术阻塞。已知取舍：TXT 导出默认 UTF-8（源编码/BOM 保真属 P1-T01 `03` §9 V1.0 基线）；空文档导出为空文件（非静默回退）；`os.fdopen` 极端异常路径（fd 泄漏）未覆盖（实际不可达）。
- **恢复动作：** 读取本文件；当时的小目标为 P0-T08-M05，状态 ready，plan_alignment pending_reality_check。开工前先核验 P0-T08 GUI/线程/关闭契约与 Application Service 线程安全边界，评估 PySide6/pytest-qt 版本/许可/安全/锁定/打包影响，完成 Reality Check 后从最小主窗口垂直切片开始，不越出 P0-T08。

### Completed milestones

- **P0-T01-M01** — Python 3.12 `src/` 工程与 pytest 骨架（completed）
  - 交付物：`pyproject.toml`、分层 `src/transrealm/` 包、`tests/test_smoke.py`、`.gitignore`、`README.md`
  - 验收证据：pytest 4 passed；Ruff/mypy 无问题；domain 层无 PySide6/SQLite/HTTP 依赖
- **P0-T01-M02** — SQLite 连接、PRAGMA 与事务基础（completed）
  - 交付物：`src/transrealm/infrastructure/database.py`、事务与错误类型、`tests/test_database.py`
  - 验收证据：pytest 12 passed（合计 16）；Ruff/mypy 无问题；外键/WAL/Unicode/长路径/只读路径已覆盖
- **P0-T01-M03** — migration discovery、顺序和 history/checksum（completed）
  - 交付物：`infrastructure/migrations/` 包、`schema_migrations` 表与 runner、`tests/test_migrations.py`
  - 验收证据：pytest 10 passed（合计 26）；Ruff/mypy 无问题；发现/顺序/幂等/checksum/回滚已覆盖
- **P0-T01-M04** — `001_init` 与 Project create/open/save API（completed）
  - 交付物：`001_init.sql`、Project 实体、Repository、Application Service、`tests/test_project.py`
  - 验收证据：pytest 8 passed（合计 34）；Ruff/mypy 无问题；创建/打开/保存/幂等已覆盖
- **P0-T01-M05** — migration 失败、checksum、只读/Unicode/长路径异常测试（completed）
  - 交付物：`tests/test_exceptions.py`
  - 验收证据：pytest 7 passed（合计 41）；Ruff/mypy 无问题；失败回滚/checksum/只读/Unicode/长路径已覆盖
- **P0-T01-M06** — P0-T01 Code Review、文档同步与完成验收（completed）
  - 交付物：范围内 Code Review 修复、`07_Developer_Task_List.md` 状态同步、`DEVELOPMENT_STATE.md` 推进
  - 验收证据：全部 41 测试通过；ruff/mypy 无问题；MigrationRunner.close 与错误检查已加固
- **P0-T02-M01** — TXT 解析器与最小 Segment 模型（completed）
  - 交付物：`domain/segment.py`、TXT parser、Import Service、Segment Repository、`002` migration、`tests/test_txt_parser.py`
  - 验收证据：pytest 8 passed（合计 49）；Ruff/mypy 无问题；TXT 分段/stable key/重复导入/空文件/编码错误已覆盖
- **P0-T02-M02** — Parser 接口抽象与 TXT 异常测试（completed）
  - 交付物：`infrastructure/parsers/parser.py`、Parser Protocol、ParserRegistry、TxtParser 类、扩展测试
  - 验收证据：pytest 7 passed（合计 56）；Ruff/mypy 无问题；协议/注册表/空行/重复行/不支持格式已覆盖
- **P0-T02-M03** — Segment 稳定 key 碰撞与重复导入策略（completed）
  - 交付物：`003_add_import_uniqueness.sql`、原子导入 Repository API、不可变内容版本策略、Agent 路由文档、回归测试与契约同步
  - 验收证据：pytest 62 passed；Ruff/mypy 无问题；同 hash 幂等、内容变化保留旧版本、空文件、约束碰撞回滚、旧库冲突保留已覆盖
- **P0-T03-M00** — SQLite migration 升级前一致性备份（completed）
  - 交付物：`infrastructure/migrations/backup.py`、runner 集成、`MigrationBackupError`、备份发现/打开/阻断/失败保留/WAL/命名/不可写目标测试
  - 验收证据：pytest 69 passed；Ruff/mypy 无问题；仅 pending migration 时创建备份、备份失败阻止 migration、备份保留失败前数据、WAL 下一致性复制、备份文件可发现可打开已覆盖
- **P0-T03-M01** — Provider Connection 非敏感配置模型与持久化（completed）
  - 交付物：`004_add_provider_connection.sql`、`domain/provider_connection.py`、Repository、Application Service、`tests/test_provider_connection.py`；`credential_reference` 仅接受 `env:`/`wincred:` 语法；同步更新 `04_Database_Schema.md`
  - 验收证据：pytest 102 passed；Ruff/mypy 无问题；CRUD/唯一名/关闭重开/空名/非法 endpoint/超时/重试/原始 API Key 拒绝已覆盖

- **P0-T03-M02** — Model Capability/Profile 模型、持久化、Connection 外键与关闭重开测试（completed）
  - 交付物：`005_add_model_profile.sql`、`domain/model_profile.py`（`ModelCapability`/`ModelProfile`）、`ModelProfileRepository`、`ModelProfileService`、`tests/test_model_profile.py`；`model_profiles.provider_connection_id` 外键 RESTRICT 保护引用完整性；同步更新 `04_Database_Schema.md`
  - 验收证据：pytest 129 passed；Ruff/mypy 无问题；Capability 验证/snapshot、Profile CRUD、外键约束、关闭重开一致性、缺失 Connection 预校验、被引用 Connection 删除阻断已覆盖
- **P0-T03-M03** — timeout/retry policy、JSON 配置和关闭重开一致性验收（completed）
  - 交付物：`tests/test_p0_t03_m03.py`；覆盖 timeout/retry 边界、复杂嵌套 JSON round-trip、部分 JSON 更新保真、跨 service 关闭重开一致性、多 Profile 共享 Connection、capability snapshot 刷新
  - 验收证据：pytest 140 passed；Ruff/mypy 无问题
- **P0-T03-M04** — Task review、全回归、编号文档/索引/状态同步（completed）
  - 交付物：删除未使用 `_REQUIRED_PARAMETERS`/`ClassVar`、空 capability snapshot 防护与测试；更新 `04_Database_Schema.md`、`07_Developer_Task_List.md`、`DEVELOPMENT_STATE.md`
  - 验收证据：pytest 140 passed；Ruff/mypy 无问题
- **P0-T04-M01** — Adapter Protocol、DTO、capability contract 与 normalized error（completed）
  - 交付物：`adapters/dto.py`、`adapters/protocol.py`、`adapters/errors.py`、`adapters/openai_adapter.py`、`tests/test_adapter_protocol.py`；`ModelAdapter`/`Transport`/`CredentialResolver` Protocol、normalized error taxonomy、OpenAI-compatible 请求/响应映射与 capability 校验
  - 验收证据：pytest 177 passed；Ruff/mypy 无问题；DTO 校验、错误分类、capability 过滤、请求/响应映射、HTTP 错误映射、凭据边界与脱敏均已覆盖
- **P0-T04-M02** — OpenAI-compatible 成功请求/响应映射（completed）
  - 交付物：`tests/test_p0_t04_m02.py`；修复 `openai_adapter.py` 中 `stream=True` 请求体映射，加固 usage 解析对非整数/负值的处理
  - 验收证据：pytest 198 passed；Ruff/mypy 无问题；endpoint、model、参数、usage、request ID、finish reason、多 choices、空 content、raw_response 保留均已覆盖
- **P0-T04-M03** — timeout、限流、网络/服务/认证/参数错误分类与有限 transport retry（completed）
  - 交付物：`openai_adapter.py` 有限重试循环、`tests/test_p0_t04_m03.py`、指数退避与 `Retry-After` 支持、retry 参数校验
  - 验收证据：pytest 211 passed；Ruff/mypy 无问题；非重试错误、5xx/429/408/超时/断连重试、retry-after 头、timeout 每 attempt 一致、请求体跨重试保持一致均已覆盖
- **P0-T04-M04** — capability 降级、credential resolver 边界与脱敏验收（completed）
  - 交付物：`DegradationPolicy`、capability 降级重试、`tests/test_p0_t04_m04.py`、resolver 调用边界与错误脱敏加固
  - 验收证据：pytest 224 passed；Ruff/mypy 无问题；结构化输出/流式降级、无策略/非 capability 400 不降级、降级保留参数、降级失败、resolver 仅按需调用、错误信息无 secret 泄露均已覆盖
- **P0-T04-M05** — Review、全回归和文档/状态同步（completed）
  - 交付物：最终 Code Review 修复（`openai_adapter.py` `_compute_delay` mypy 类型显式转换）、`03_Technical_Design.md` §5 Adapter retry/降级/凭据边界同步、`07_Developer_Task_List.md` P0-T04 完成标记、`DEVELOPMENT_STATE.md` 推进至 P0-T05-M01
  - 验收证据：pytest 224 passed；Ruff All checks passed；mypy Success: no issues found in 47 source files
- **P0-T05-M01** — Context Candidate / Manifest / 预算 DTO（completed）
  - 交付物：`src/transrealm/application/context.py`（`BudgetEstimate`、`ContextCandidate`、`ContextSource`、`EstimateMethod`、`ContextBudget`、`ContextManifest`、`OutputItem`、`OutputContract`）、`tests/test_p0_t05_m01.py`
  - 验收证据：pytest 250 passed；Ruff All checks passed；mypy Success: no issues found in 49 source files；DTO 创建、预算可用空间、预留校验、候选优先级/来源、Manifest 版本记录、输出契约唯一性与空值校验均已覆盖
- **P0-T05-M02** — 当前与相邻 Segment 的最小规则预算（completed）
  - 交付物：`src/transrealm/application/context_composer.py`（`ContextComposer`、`ContextBudgetError`）、`tests/test_p0_t05_m02.py`
  - 验收证据：11 passed；Ruff/mypy 无问题；当前 Segment 必入/不截断、相邻优先级排序、预算不足裁剪、空输入、字符/token 估算、自定义 estimator 均已覆盖
- **P0-T05-M03** — 锁定 Glossary 候选接口与预算不足行为（completed）
  - 交付物：`src/transrealm/application/context_composer.py`（`glossary_candidates` 参数、优先级归一化）、`tests/test_p0_t05_m03.py`
  - 验收证据：8 passed；Ruff/mypy 无问题；空 Glossary 候选、锁定 Glossary 优先于相邻 Segment、自定义估算保留、预算不足时 Glossary 保留/邻居裁剪、Manifest 记录已裁剪 Glossary 均已覆盖
- **P0-T05-M04** — Profile 模板渲染与不可破坏输出契约（completed）
  - 交付物：`src/transrealm/application/prompt_renderer.py`（`PromptRenderer`、`RenderedPrompt`、`PromptRenderError`、`DEFAULT_OUTPUT_SCHEMA`）、`tests/test_p0_t05_m04.py`
  - 验收证据：7 passed；Ruff/mypy 无问题；Prompt 文本与输出契约生成、输出 schema 始终存在、自定义模板变量替换、未知变量报错、缺失当前 Segment 报错、输出契约仅含当前 Segment、自定义 schema 均已覆盖
- **P0-T05-M05** — 可复现性 review、全回归和同步（completed）
  - 交付物：全量回归通过、lint/type 通过、DEVELOPMENT_STATE.md 与 07_Developer_Task_List.md 同步
  - 验收证据：pytest 275 passed；Ruff All checks passed；mypy Success: no issues found in 54 source files
- **P0-T06-M01** — 解析/校验 DTO、错误分类和 fixture 规范（completed）
  - 交付物：`src/transrealm/application/output_parser.py` 中的 DTO 与错误分类（`IssueCategory`、`OutputParseError`、`TranslationCandidate`、`ParseIssue`、`RepairDescriptor`、`ValidationReport`）、`tests/test_p0_t06_m01.py`
  - 验收证据：pytest 15 passed；Ruff/mypy 无问题；DTO 创建、错误分类、RepairDescriptor 尝试预算、ValidationReport 唯一性均已覆盖

- **P0-T06-M02** — 单 Segment parser/validator（completed）
  - 交付物：`src/transrealm/application/output_parser.py` 单 Segment 解析/校验实现（`OutputParser.parse/parse_or_raise`、markdown fence 提取、ID/类型/空值校验）、`tests/test_p0_t06_m02.py`
  - 验收证据：pytest 15 passed；Ruff All checks passed；mypy Success: no issues found in 57 source files；全量回归 pytest 305 passed；覆盖有效单 item、fence 修复、无效 JSON、缺 items/字段/ID、错 ID、重复 ID、空/非字符串译文、repair limit、repair descriptor 更新与 parse_or_raise 成功/失败。

- **P0-T06-M03** — 批量 items 的 ID/数量/顺序交叉校验（completed）
  - 交付物：`src/transrealm/application/output_parser.py` 批量顺序交叉校验逻辑、`tests/test_p0_t06_m03.py`
  - 验收证据：pytest 10 passed；Ruff All checks passed；mypy Success: no issues found in 58 source files；全量回归 pytest 315 passed；覆盖批量正序/错序/缺项/多余/空数组/部分反转/重复/未知 ID/warnings/notes/多问题。

- **P0-T06-M04** — 有限修复分类与恶意/异常输出矩阵（completed）
  - 交付物：`src/transrealm/application/output_parser.py` 翻译污染检测（`_CONTAMINATION_MARKERS`、`_is_contaminated`、CONTAMINATION issue）、`tests/test_p0_t06_m04.py`
  - 验收证据：pytest 18 passed；Ruff All checks passed；mypy Success: no issues found in 59 source files；全量回归 pytest 333 passed；覆盖修复分类（INVALID_JSON/EMPTY_TRANSLATION/DUPLICATE_ID/REPAIR_LIMIT 等可修复性）、非 dict item、缺 segment_id、冗余字段、markdown fence 污染、顶层数组、items 类型错误、修复消耗/上限、parse_or_raise、空数组、多问题报告。

- **P0-T06-M05** — Review、全回归和同步（completed）
  - 交付物：P0-T06 全 Task Code Review、全量回归（pytest 333 passed）、Ruff/mypy 通过；同步 `DEVELOPMENT_STATE.md` 与 `07_Developer_Task_List.md`，P0-T06 标记 completed，指针推进至 P0-T07-M01。
  - 验收证据：pytest 333 passed；Ruff All checks passed；mypy Success: no issues found in 59 source files；M05 现场结论 **ADAPT**（`07_Developer_Task_List.md` 中 M03 状态陈旧，已同步）。
- **P0-T08-M01** — 无 UI 成功闭环（completed）
  - 交付物：`src/transrealm/application/translation_service.py`（`TranslationService`/`TranslationServiceError`）、`tests/test_p0_t08_m01.py`
  - 验收证据：pytest 430 passed（M01 新增 11 项）；Ruff All checks passed；mypy Success: no issues found in 75 source files；成功闭环生成可重开 Revision，Attempt 审计完整；空 Segment/缺 Profile/预算不足/非法 budget/非 running Run 不发请求；adapter 错误与无效输出 finalize failed Attempt。
- **P0-T08-M02** — 生产调用边界（completed）
  - 交付物：`src/transrealm/adapters/http_transport.py`（`StdlibHttpTransport`）、`adapters/credential_resolvers.py`（`EnvironmentResolver`/`WindowsCredentialResolver`/`build_resolver`）、`application/adapter_factory.py`（`compose_adapter`）、`tests/test_p0_t08_m02.py`
  - 验收证据：pytest 463 passed（M02 新增 33 项）；Ruff All checks passed；mypy Success: no issues found in 79 source files；stdlib urllib transport + ctypes wincred 零新依赖；同源重定向保留 Authorization、跨源/HTTPS 降级剥离；超时/断连/截断稳定分类；401/429/500/418 分类与 secret 不泄（错误与审计）；TranslationService 对真实本地 server 完整闭环。
- **P0-T08-M03** — 失败与有限修复 E2E（completed）
  - 交付物：`src/transrealm/application/translation_service.py`（repair 有界重呼）、`tests/test_p0_t08_m03.py`
  - 验收证据：pytest 471 passed（M03 新增 8 项）；Ruff All checks passed；mypy Success: no issues found in 80 source files；repairable 无效输出（空译文/污染）经一次重呼创建新 Attempt（新幂等键）并记账于 validator_summary；非 repairable/repair 耗尽 finalize 永久失败；永久 adapter 错误不业务重试；崩溃重启后 T07 lease 恢复并重译成功。
- **P0-T08-M04** — Revision 驱动 TXT 导出（completed）
  - 交付物：`src/transrealm/application/exporter.py`（`TxtExporter`/`ExportError`）、`src/transrealm/infrastructure/repositories/segment_repository.py`（`get_source_document_by_id`）、`tests/test_p0_t08_m04.py`
  - 验收证据：pytest 484 passed（M04 新增 13 项）；Ruff All checks passed；mypy Success: no issues found in 82 source files；默认 current Revision 按原顺序导出；显式 override 校验存在/归属；缺失/跨 Segment/无效 Revision 失败无文件；编码/目标不可写失败原子、保留旧目标、无临时残留；不回退源文。
- **P0-T08-M05** — 非阻塞桌面闭环（completed）
  - 交付物：`src/transrealm/ui/main_window.py`、`ui/pages.py`、`ui/worker.py`、`tests/test_p0_t08_m05.py`；`pyproject.toml`（PySide6>=6.8,<6.12 运行时、pytest-qt>=4.2 dev）
  - 验收证据：pytest 488 passed（M05 新增 4 项）；Ruff All checks passed；mypy Success: no issues found in 86 source files；三 Tab 主窗口经 worker 线程完成导入/Profile/翻译/导出；翻译中主线程 QTimer 触发且 progress<total 证明不阻塞；取消在 Segment 边界停止；关闭收敛无 processing 遗留、Attempt 全终态。

## 7. 状态更新规则

- `ready`：要求明确，可立即开工；
- `in_progress`：已修改工作区但小目标未完成；
- `blocked`：缺少用户决定、凭据或外部资源；必须写清解除条件；
- `verification`：实现完成，正在测试或 Review；
- `completed`：功能测试、异常测试、Review 和必要文档同步全部完成。

`plan_alignment` 与状态分开维护：

- `pending_reality_check`：当前 Milestone 尚未完成现场核验；
- `FIT`：计划假设与现场一致；
- `ADAPT`：硬约束和验收不变，已记录内部实现调整及证据；
- `REPLAN`：存在重大偏差，必须记录完整偏差模板和解除条件。

禁止为了显得有进度而虚报状态。测试失败时保持 `in_progress` 或 `verification`；只有确有外部解除条件时才使用 `blocked`。`REPLAN` 只阻塞冲突部分，不自动阻塞可独立推进的范围内工作。
## 8. Agent 结束本次工作的条件

仅在以下任一条件成立时结束：

- 当前小目标完成、验证并已把下一小目标写入本文件；
- 遇到真实阻塞，已尽可能完成不受阻部分，并把解除条件写入本文件；
- 用户明确要求停止。

结束回复先报告实际结果，再报告测试和下一检查点。不要以“如果你愿意，我可以开始”结尾。
