# 译境 / TransRealm

# 无人开发治理

## 1. 目的与权威边界

本文件定义持续无人开发的决策、执行和验收机制，不维护第二份路线图、任务状态或实现快照。

- 产品范围与版本顺序：`01_PRD.md`、`02_Development_Roadmap.md`；
- 架构与持久化契约：`03_Technical_Design.md`、`04_Database_Schema.md`、`05_Prompt_Architecture.md`、`08_Architecture_Review.md`；
- 唯一 Agent 交接与运行现场：`DEVELOPMENT_STATE.md`；
- 候选发布执行清单：`RELEASE_CHECKLIST.md`。

代码、Git 和可复现测试是实现事实的唯一依据；文档中历史测试数量或完成声明不能替代可重建证据。

## 2. 角色与裁决

| 角色 | 权限与责任 | 不得做的事 |
|---|---|---|
| 技术负责人（主 Agent） | 裁决路线图内优先级、任务激活、实现边界、架构取舍、风险等级、验收与回滚准入；拆分可执行任务单；独立验收交付。 | 以未经验证的规划替代代码事实；独自提出、批准并验收高风险变更。 |
| 开发 Agent | 仅在已激活 Task/Milestone 的硬约束、修改范围和验收内实现、测试、修复和同步文档；记录 FIT/ADAPT/REPLAN。 | 扩大需求、改公共契约/Project 格式、升级高风险依赖、发布、推送或覆盖既有改动。 |
| Review Agent | 独立检查行为、数据、秘密、迁移、兼容性和测试缺口；报告可复现发现。 | 修改验收标准，或把未验证交付宣告完成。 |
| 用户 | 只在产品方向显著变化、外部影响、凭据/权限、远程托管或正式发布时裁决。 | — |

高风险包括数据库 migration、Project/文件格式、公开接口、身份凭据、依赖升级、构建/发布链、删除或迁移用户数据。高风险变更必须由技术负责人定义方案和门禁，并由独立 Review Agent 验收；需要外部权限或改变产品方向时再提交用户裁决。

## 3. 基线、准入与任务生命周期

### 3.1 可执行基线

一个可执行基线必须记录：Git commit 或明确受保护的工作树清单、Python/依赖环境、完整验证命令及原始结果、已知风险、恢复方式和证据位置。没有这些证据时，工作树只能称为候选现场，不能作为后续 Task 已满足的可复现依赖。

基线门禁要求每个可执行依赖都具备可定位 commit、完整质量证据和回滚 ref。v0.1.0 已发布；2026-08-22 规划核验时 `master`/`origin/master` 为 `1542291`，工作树在规划写入前干净。v0.2 代码开工前必须把本次 Markdown 方案形成用户授权的可恢复基线，并在 `DEVELOPMENT_STATE.md` 重新核验 HEAD、工作树和授权范围。

### 3.2 生命周期

```text
proposed -> ready -> in_progress -> review -> verification -> completed
                                      \-> blocked
```

1. 技术负责人按路线图、风险和依赖创建或激活 `07` 中的 Task。
2. `proposed` Task、`code_authorized: false` 或不覆盖目标文件类型的授权范围不可自动执行；开发 Agent 只能读取并报告解除条件。
3. 开发 Agent 检查基线和当前现场，写入 FIT、ADAPT 或 REPLAN。
4. `FIT` 直接实施；`ADAPT` 仅限不改变硬约束的内部调整；`REPLAN` 停止冲突部分，提交证据与最小替代方案。
5. 实现后先完成目标、异常和安全/兼容性验证，再交独立 Review。
6. 只有全部门禁、审查和文档同步通过，技术负责人才能把交付标为 `completed`。

紧急修复可跳过排期但不可跳过事实核验、最小回归、回滚方案和事后 Review；若影响数据、秘密或公开行为，仍须技术负责人审批。

## 4. 优先级与风险

- **P0 / Release-blocking：** 数据丢失、秘密泄露、迁移/恢复损坏、无法启动、已承诺格式或核心翻译闭环失败。停止相关发布与后续依赖开发。
- **P1 / High：** 用户可见错误、兼容性回归、错误恢复缺口或显著安全风险。进入最近可行切片。
- **P2 / Normal：** 已批准路线图能力和非阻塞缺陷。按 `02` 的依赖顺序执行。
- **P3 / Deferred：** 优化、体验改进或未获批准的构想。不得挤占 P0/P1 或偷渡进入当前 Task。

风险等级决定附加门禁：低风险要求范围测试和常规质量检查；中风险要求异常测试；高风险要求迁移/回滚演练、安全审查和独立 Review；发布风险还要求 `RELEASE_CHECKLIST.md` 全部通过。

## 5. 开发、审查与质量门禁

每个 Task 单必须定义：目标、非目标、前置基线、授权修改范围、风险等级、验收矩阵、观测证据、回滚点和升级条件。开发 Agent 的完成报告必须给出实际修改文件、命令及结果、未覆盖项、风险、恢复动作和状态更新。

当前没有远程 CI 时，本机门禁为：

```bash
py -3.12 -m pytest -q
py -3.12 -m ruff check src tests
py -3.12 -m mypy src tests
```

涉及 GUI 必须实际启动并操作；v0.2 起还必须按 Task 冻结的中英文、分辨率、DPI 和关键页面场景保存截图证据，检查截断、重叠、焦点、空/错/禁用状态，并由独立 Review Agent 做视觉签核。截图不采用跨 Qt 版本的逐像素相等作为唯一门禁。涉及 migration、Project 删除、导入/导出、恢复或网络边界必须增加对应的端到端或故障注入证据。接入远程仓库后，保持相同命令并设为 PR 必需检查，不另行降低标准。

格式化/静态检查、单元测试、集成/E2E、构建、依赖/安全检查、文档同步均应适用时通过。每个新增能力必须显式声明兼容性基线：受影响旧能力、不可破坏不变量、旧测试和实测结果；并新增至少一条测试。全量 pytest 是底线，不替代旧功能回归清单。受影响旧测试失败、未运行或无独立 Review 理由的删改测试一律不得放行；P0、秘密、迁移和发布门禁不得用例外直接放行。

## 6. 依赖、安全、发布与回滚

- 依赖新增或升级必须说明当前问题、兼容范围、许可、安全影响、锁定和打包影响；高风险升级先独立验证，不与功能改动混合。
- API Key、凭据正文、用户数据库、导出和日志不得提交；凭据仅通过既有 `env:`/`wincred:` 边界引用。发现疑似泄露时停止传播，隔离证据，撤销/更换受影响凭据需用户权限后执行，并补充回归测试。
- migration 只能追加编号文件，不重写已发布 migration；必须测试旧库升级、失败回滚、备份与恢复。每项数据变更必须有回滚或前向修复说明。
- 发布候选仅能来自已验证、可定位的基线。构建、签名、推送、创建远程资源和正式发布均需用户单独授权。候选发布与回滚执行 `RELEASE_CHECKLIST.md`。
- 用户禁止 Agent 创建、切换或删除分支；所有工作必须留在用户当前分支。用户仍授权在 Milestone 的验收、回归、质量门禁、适用 Review 和状态同步全部通过后，暂存范围内文件并创建一个本地 checkpoint commit。commit 前后必须核对 staged diff、允许路径、作者身份、文件清单和状态指针；不得改写既有历史。该本地提交授权不等于代码开发授权，也不包含 push、merge、tag、Release、远程资源或其他外部动作。

## 7. 决策节点与 Agent 切换

决策节点只用于会改变多个后续切片、持久化/安全契约或用户可见语义，且权威文档尚未给出唯一答案的选择。每个 Task 最多一个基础节点；只有新证据与已裁决契约冲突时才允许第二个节点。

开发 Agent 必须继续自主处理私有类/文件组织、SQL 组织、fixture、控件布局、已批准依赖内的实现和可逆 `ADAPT`。不得把普通实现分歧升级为节点。

节点决策包必须包含：

- `decision_id`、触发 Milestone、问题、截止点和恢复 Milestone；
- 不可变约束、代码/测试现场证据和待确认事实；
- 两到三个可行方案，每项的兼容性、安全、迁移/回滚、测试和后续影响；
- 技术负责人推荐及理由；
- 必须更新的权威文档、验收条件和回滚条件。

开发 Agent 命中节点时，停止冲突范围、不修改相关代码，在 `DEVELOPMENT_STATE.md` 写入 `pending_decision` 和完整提示词，并提醒用户切换到决策 Agent。决策 Agent 只评估和更新裁决/契约，不实现。裁决完成后，状态文件必须给出切回开发 Agent 的提示词；开发 Agent 重读裁决和相关文档、重新完成 Reality Check 后才恢复目标 Milestone。

## 8. 观测、反馈与持续改进

运行证据优先复用现有 Run、Attempt、Revision、migration history、测试输出和 Git commit。每个已关闭 P0/P1 事件至少归档触发条件、影响、根因、修复、验证、回滚结果和预防门禁；技术负责人将可复用结论回写到相关 Task、设计文档或开发指南，不创建平行状态日志。

## 9. 待决策项

待决策项仅记录需要外部授权或产品裁决的问题，不复制开发现场：

1. **已决定：** P0-T07 至 P0-T08 候选工作树已在本地基线 `5ef3be6` 固化；
2. **已决定：** 创建公开 GitHub `TransRealm` 仓库作为长期备份与未来正式发布仓库；首次只推送源码、测试、文档和 CI，不创建 Release 或上传 artifact；
3. 发布签发人，以及凭据泄露/安全事件的通知和处置渠道；
4. CI 建成前是否接受“技术负责人 + 独立 Review + 本机全量门禁”作为临时候选门禁。
