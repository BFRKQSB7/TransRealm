# TransRealm Release Candidate Checklist

> 本清单用于 P1-T05 及之后的候选发布签核；候选构建不等于提交、推送或对外发布。后两项均需用户单独授权。

## 1. 候选身份与可恢复性

- [ ] 记录候选 Git commit、分支、工作树状态、版本、构建时间、Windows 11 架构和执行者。
- [ ] 工作树只包含已审查交付；未跟踪用户数据库、备份、日志、导出和凭据未纳入候选。
- [ ] 记录回退 ref/候选 artifact、数据库备份位置和恢复步骤。
- [ ] 已知问题、未覆盖项和发布阻塞项已记录；任何 P0/P1 阻塞项均未被标为通过。

## 2. 固定环境与质量门禁

- [ ] Python、全部运行/开发依赖和构建工具版本已锁定并记录许可证。
- [ ] `py -3.12 -m pytest -q` 通过，结果已归档。
- [ ] `py -3.12 -m ruff check src tests` 通过。
- [ ] `py -3.12 -m mypy src tests` 通过。
- [ ] 格式、迁移、恢复、导入/导出、长文本和 GUI 的 Release Gate 场景均有可复现证据。
- [ ] 独立 Review 无未解决的数据、安全、兼容性或发布阻塞发现。

## 3. 安全与数据完整性

- [ ] 已扫描候选源码、配置、测试输出和 artifact，确认不含 API Key、token、凭据正文或用户数据。
- [ ] `env:` / `wincred:` 凭据引用在支持场景下可操作，错误与日志不泄露 secret。
- [ ] 已演练旧库 migration、升级前备份、失败恢复和崩溃后的 lease/Attempt/Revision 状态恢复。
- [ ] 已验证 `.aiproject` / 开放目录（适用时）的 hash、路径、资源限制、篡改和秘密排除门禁。

## 4. Windows 绿色版候选

- [ ] 从锁定环境构建 artifact，记录工具、命令、hash、大小及许可证清单/SBOM（如可用）。
- [ ] 迁移 SQL、Qt 平台插件和必要资源已包含；在干净支持的 Windows 11 环境实际解压并启动。
- [ ] 在可写目录完成创建或打开 Project、迁移、导入、调用受控兼容端点、关闭恢复和导出。
- [ ] 只读目录、Unicode/长路径、网络/模型中断和不可写目标的失败信息与恢复动作可操作。
- [ ] 删除程序目录不删除外置 Project、备份或凭据；清理说明经实际演练。

## 5. 签核与回滚

- [ ] 技术负责人确认所有适用门禁通过，或记录已批准且未触及 P0/秘密/migration/发布门禁的例外。
- [ ] 独立 Review Agent 已签核或列出阻塞项。
- [ ] 用户已明确授权构建上传、推送、创建远程资源或正式发布中的每一项外部动作。
- [ ] 发布后问题的回滚路径已验证：停止分发、回退到候选 artifact/ref、保留用户数据与备份、发布恢复说明。
