# 译境 / TransRealm

# AI Development Guide V3

## Agent职责

负责：

- 编码；
- 功能测试与异常测试；
- 文档同步；
- 诚实报告测试结果和风险。

禁止：

- 未确认修改产品范围或架构；
- 删除已有功能或破坏 Project 兼容性；
- 引入无必要依赖；
- 把未完成、测试失败或未验证内容标记为完成；
- 将 API Key 写入代码、Project、Prompt 快照或日志。

## 自动开工规则

当用户提供或要求遵循 `DEVELOPMENT_STATE.md` 时，这本身就是开始当前小目标的授权。Agent 不得只总结计划、询问是否继续或等待第二次“开始”指令。

Agent 必须先验证检查点、代码和测试是否一致；无真实阻塞时立即更新状态为 `in_progress` 并执行。常规、可逆、范围内的实现选择自行决定。只有产品/架构变更、破坏性或对外操作、用户专属输入，或会导致显著不同结果的歧义才暂停询问。

每完成一个小目标或发生中断，必须更新 `DEVELOPMENT_STATE.md`，使下一 Agent 不依赖聊天记录即可继续。

## 开发前

1. 读取 `DEVELOPMENT_STATE.md` 的当前 Task、小目标、状态和恢复动作；
2. 读取任务清单中的完整 Task 定义和依赖；
3. 读取 Task/小目标列出的输入文档；
4. 检查现有代码、测试和工作区状态，确认检查点没有过时；
5. 确认修改范围、验收标准和异常场景；
6. 检查是否涉及数据库 migration、Project 格式、Prompt/Model Profile、默认行为、依赖或数据安全等高风险变更。

## 标准流程

```text
需求确认
 -> 实现
 -> 功能测试
 -> 异常测试
 -> Code Review
 -> 更新文档
```

异常测试至少考虑：外部 API 断开、本地模型停止、超时、无效输出、程序关闭、断电恢复；仅适用的场景可标记 N/A 并说明原因。

## 架构或兼容性修改

必须先提出：

- 原方案；
- 新方案；
- 优势；
- 缺点；
- 风险；
- 迁移和回滚成本；
- 受影响文档和任务。

等待确认后再修改。数据库结构必须通过编号 migration，不能直接改旧结构。

## 完成报告

每个任务完成后输出：

1. 修改文件及内容摘要；
2. 功能测试命令和结果；
3. 异常测试命令和结果；
4. 已知风险、未覆盖项和阻塞项；
5. 是否需要更新其他编号文档；
6. 下一项可执行小目标；
7. `DEVELOPMENT_STATE.md` 已更新的检查点。

每个测试必须有显式断言或可核验输出；“程序没有崩溃”不等于测试通过。