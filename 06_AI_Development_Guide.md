# 译境 / TransRealm

# AI Development Guide V4

## 决策权与 Agent 职责

本项目不采用“规划模型决定一切、代码模型机械执行”的单向流程。计划是基于有限信息形成、等待代码现场验证的执行假设，不是高于代码与测试事实的命令。

- **用户：** 决定产品范围、用户可见行为、兼容性承诺、技术栈和重大架构变更。
- **规划 Agent：** 定义目标、非目标、硬约束、依赖和验收标准；可以提出参考实现，但不得把未经现场验证的类名、模块名或存储方案伪装成唯一答案。
- **执行 Agent：** 对 Git、现有代码和测试事实负责；在目标与硬约束内决定最小实现，并有权用证据否定参考实现。
- **Review Agent：** 独立检查行为回归、架构偏移、过度设计和测试缺口；不因模型能力或角色名称自动拥有最终决策权。

发生冲突时，产品与架构权威顺序仍按项目文档执行；关于“现有代码实际上是什么、测试是否通过、接口是否存在”的事实，以 Git、代码和可复现测试为准。任何单一 Agent 都不能同时把未经验证的方案提出、批准并宣告完成。

## 硬约束与参考实现

`07_Developer_Task_List.md` 中以下内容属于执行硬约束：目标、非目标、前置依赖、用户可见验收、兼容性/安全要求和明确标记为“强制契约”的公共接口或数据格式。

以下内容默认只是规划导航或参考实现：内部类名、Manager/Service 名称、私有函数、文件落点、局部算法和未被权威文档批准的存储建议。`修改范围` 约束不得越界的领域和行为，不代表列出的每个文件都必须修改；`交付物` 描述应交付的能力，不自动强制某个内部抽象。

执行 Agent 可以不采用参考实现，但必须保持硬约束与验收不变，并在 `DEVELOPMENT_STATE.md` 记录现场依据。

## Agent 职责

负责：

- 编码；
- 功能测试与异常测试；
- 文档同步；
- 诚实报告测试结果和风险。

禁止：

- 未确认修改产品范围或架构；
- 删除已有功能或破坏 Project 兼容性；
- 引入无必要依赖；
- 把未完成、测试失败或未验证内容标记为完成；
- 将 API Key 写入代码、Project、Prompt 快照或日志。

## 自动开工规则

新对话先读取 `AI_CONTEXT_INDEX.md`，再读取 `DEVELOPMENT_STATE.md` 当前指针，并按索引只加载命中的文档章节与代码。禁止无差别通读全部编号文档；只有发生冲突、首次进入新领域、索引失效或当前 Task 明确要求时扩大阅读范围。

当用户提供或要求遵循 `DEVELOPMENT_STATE.md` 时，这本身就是开始当前小目标的授权。Agent 不得只总结计划、询问是否继续或等待第二次“开始”指令。

Agent 必须先验证检查点、代码和测试是否一致，并完成下述 Reality Check；无真实阻塞时立即更新状态为 `in_progress` 并执行。常规、可逆、范围内的实现选择自行决定。只有 `REPLAN`、破坏性或对外操作、用户专属输入，或会导致显著不同产品结果的歧义才暂停。

`07_Developer_Task_List.md` 中预拆的未来 Task/Milestone 只负责导航和验收，不代表已激活。纯规划或文档同步不得提前改变 `DEVELOPMENT_STATE.md` 的 current pointer；只有实际开工、依赖已核验且工作区无冲突时，才原子推进运行状态。规划顺序与当前指针不一致时，先核验 Git、未提交改动、测试证据和依赖。

每完成一个小目标或发生中断，必须更新 `DEVELOPMENT_STATE.md`，使下一 Agent 不依赖聊天记录即可继续。

## Reality Check 与计划反馈

每个 Milestone 在新增测试或代码前必须进行一次简短现场核验：

1. 计划依赖的类、模块、接口和数据是否真实存在；
2. 现有实现是否已经满足部分或全部验收；
3. 参考方案是否符合当前职责边界；
4. 能否复用现有机制完成更小的垂直切片；
5. 是否会影响当前 Task 之外的公共行为、数据或依赖。

将结论记录为以下一种：

- **FIT：** 计划假设与现场一致，按计划执行。
- **ADAPT：** 目标、硬约束和验收不变，只调整可逆的内部实现；执行 Agent 自主实施并记录理由，不必等待批准。
- **REPLAN：** 需要改变用户行为、持久化/Project 格式、跨模块公共契约、未在当前 Task/已批准技术栈中授权的外部依赖、已批准架构，或显著扩大到其他 Task；停止相关实现，提交偏差证据并等待重新规划或用户裁决。当前 Task 明确要求且技术栈已批准的依赖（例如 P0-T08 的 PySide6/pytest-qt 落地）不因“新增”二字自动再次等待裁决，但仍必须完成版本、许可、安全、锁定和打包影响审查。

只有同时满足以下条件才能使用 `ADAPT`：不改变用户可见行为；不改变持久化或 Project 兼容性；不新增当前 Task/已批准技术栈未授权的外部依赖；不改变跨模块公共契约；不越出当前 Task；改动可测试且容易回退。否则使用 `REPLAN`。

发现计划不适合现场时，禁止为了“严格执行”而硬建空壳抽象、把职责塞入无关 Manager，或复制第二套存储。`REPLAN` 记录至少包含：计划假设、代码/测试证据、冲突、最小替代方案、影响范围、迁移/回滚成本和待裁决事项。

## 最小设计门禁

- 没有当前验收需要的第二个真实实现或调用方，不提前建立插件式抽象。
- 没有当前可复现的异步协作、吞吐或隔离需求，不引入事件总线、任务队列或微服务。
- 现有存储满足当前可靠性与查询验收时，不增加第二套存储；存储选择必须依据原子性、恢复、隔离和查询需求，而不是“看起来更专业”。
- 新增 Manager/Service 必须拥有独立状态、策略或生命周期；只转发调用的组件默认不创建。
- 新依赖必须解决当前 Task 已存在且可验证的问题，“以后可能需要”不是理由。
- 优先完成测试保护下的最小垂直闭环；在第二个真实用例出现时再提取扩展点。

## 开发前

1. 读取 `DEVELOPMENT_STATE.md` 的当前 Task、小目标、状态和恢复动作；
2. 读取任务清单中的完整 Task 定义和依赖；
3. 读取 Task/小目标列出的输入文档；
4. 检查现有代码、测试和工作区状态，确认检查点没有过时；
5. 确认修改范围、验收标准和异常场景；
6. 检查是否涉及数据库 migration、Project 格式、Prompt/Model Profile、默认行为、依赖或数据安全等高风险变更；
7. 若文件职责、编号文档章节、Task 依赖或验证命令变化，同步更新 `AI_CONTEXT_INDEX.md`。

## 标准流程

```text
读取目标、硬约束和验收
 -> Reality Check（FIT / ADAPT / REPLAN）
 -> 测试或验收样例
 -> 实现
 -> 功能测试
 -> 异常测试
 -> Code Review
 -> 更新文档
```

异常测试至少考虑：外部 API 断开、本地模型停止、超时、无效输出、程序关闭、断电恢复；仅适用的场景可标记 N/A 并说明原因。

## 架构或兼容性修改

必须先提出：

- 原方案；
- 新方案；
- 优势；
- 缺点；
- 风险；
- 迁移和回滚成本；
- 受影响文档和任务。

等待确认后再修改。数据库结构必须通过编号 migration，不能直接改旧结构。

局部、私有、可逆且不改变硬约束的 `ADAPT` 不属于架构修改，不需要为普通代码组织问题请求批准。执行 Agent 也不能用 `ADAPT` 绕过公共契约、兼容性、依赖或 Task 范围门禁。

## 完成报告

每个任务完成后输出：

1. 修改文件及内容摘要；
2. 功能测试命令和结果；
3. 异常测试命令和结果；
4. 已知风险、未覆盖项和阻塞项；
5. 是否需要更新其他编号文档；
6. 下一项可执行小目标；
7. `DEVELOPMENT_STATE.md` 已更新的检查点。

每个测试必须有显式断言或可核验输出；“程序没有崩溃”不等于测试通过。