# 译境 / TransRealm

# 开发 Agent 执行规则

> 唯一交接入口：`DEVELOPMENT_STATE.md`。先读该文件的当前任务、必读文档、约束和恢复动作；不要从本文件或历史聊天猜测当前进度。

## 1. 职责边界

- **技术负责人：** 激活任务，裁决优先级、风险、架构偏差、验收和回滚准入。
- **开发 Agent：** 只在当前 Task/Milestone 的硬约束、修改范围和验收内实现、测试、修复与同步。
- **Review Agent：** 独立检查行为回归、数据/秘密风险、兼容性、范围偏移和测试缺口。

代码、Git 和可复现测试优先于规划或状态文档。无人开发的角色、风险、例外和发布门禁见 `09_Unattended_Development_Governance.md`。

开发 Agent 不得擅自：扩大产品范围、改变 Project/公开契约、引入无必要依赖、重写已发布 migration、泄露凭据、覆盖现有工作、暂存/提交/推送/发布。

## 2. Reality Check

每个 Milestone 在写代码前必须核验：

1. 计划依赖的模块、接口、数据和测试是否真实存在；
2. 现有实现是否已满足部分验收；
3. 最小实现是否仍符合职责边界；
4. 是否影响当前 Task 外的用户行为、持久化、公共契约或依赖。

记录一种结论到 `DEVELOPMENT_STATE.md`：

- **FIT：** 计划与现场一致，直接执行。
- **ADAPT：** 目标、硬约束和验收不变，只做可逆的内部调整；记录依据后执行。
- **REPLAN：** 将改变用户行为、Project/数据格式、公共契约、已批准架构、未授权依赖或 Task 范围；停止冲突部分，记录证据、最小替代方案、影响、迁移/回滚成本和待裁决项。

## 3. 最小设计门禁

- 没有当前验收需要的真实调用方，不建通用插件/扩展框架。
- 没有可复现的异步隔离或吞吐需求，不引入队列、事件总线或微服务。
- 现有存储满足验收时，不增加第二套存储。
- 新 Service/Manager 必须拥有独立状态、策略或生命周期，不能只是转发调用。
- 新依赖必须解决当前可验证的问题，并说明版本、许可、安全、锁定和打包影响。
- 优先完成受测试保护的最小垂直闭环。

## 4. 标准开发流程

```text
读取当前 Task 的目标、非目标、硬约束和验收
 -> 锁定 base_commit 与兼容性基线
 -> 运行声明的旧功能测试并记录结果
 -> Reality Check（FIT / ADAPT / REPLAN）
 -> 新增测试或验收样例
 -> 最小实现
 -> 新增测试 + 同一旧测试集 + 全量 pytest/Ruff/mypy
 -> 功能、异常及适用的安全/兼容性测试
 -> 独立 Review
 -> 同步受影响文档与 DEVELOPMENT_STATE.md
```

全量 pytest 是最低门槛，不能替代 Task 中声明的旧功能清单。新增能力必须有至少一条新增测试；受影响旧测试失败、未运行或无理由删改时不得标记 `completed`。GUI 改动必须实际启动和操作；migration、导入/导出、恢复、网络边界必须有对应端到端或故障证据。

## 5. 架构与数据变更

涉及 Project 格式、数据库、公开接口、默认用户行为、依赖或安全边界时，先说明原方案、新方案、收益、缺点、风险、迁移/回滚成本和受影响任务。数据库只允许追加编号 migration，不能修改已发布 migration。

## 6. 决策节点切换

仅当 `09_Unattended_Development_Governance.md` 的决策节点阈值成立时，开发 Agent 才暂停冲突范围。此时必须把以下提示词完整写入 `DEVELOPMENT_STATE.md`，并提醒用户切换：

```text
请切换至决策 Agent。读取 D:\TransRealm\DEVELOPMENT_STATE.md 中的 pending_decision。
只评估，不写业务代码。基于其中的不可变约束、现场证据和候选方案，输出唯一推荐或明确保留项；说明否决理由、要更新的权威文档、验收、迁移/回滚条件和恢复 Milestone。完成后把 decision_result 与开发恢复提示词写回 DEVELOPMENT_STATE.md，并提醒用户切回开发 Agent。
```

决策 Agent 完成后，状态文件必须提供以下恢复提示词：

```text
请切回开发 Agent。读取 D:\TransRealm\DEVELOPMENT_STATE.md 的 decision_result、resume_milestone 及更新后的权威文档。只实施已裁决范围；先重新完成 FIT/ADAPT/REPLAN，再继续目标 Milestone。不得重新讨论已裁决的产品/契约选择。
```

## 7. 完成报告

每个完成或中断的切片必须在 `DEVELOPMENT_STATE.md` 留下：

1. 修改文件和实际行为；
2. 测试命令及结果；
3. 异常/安全覆盖与未覆盖项；
4. 风险、阻塞和恢复动作；
5. 已同步文档；
6. 下一可执行小目标。
