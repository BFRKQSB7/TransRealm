# TransRealm 用户交接提示词

项目目录：`D:\TransRealm`

## 继续开发

```text
读取 D:\TransRealm\DEVELOPMENT_STATE.md 并按其中“唯一 Agent 交接入口”继续。
先核验 Git 状态、当前代码和测试证据；不得覆盖、丢弃、reset、restore、clean 或删除已有工作。
严格按 current_task/current_milestone 的范围推进：Reality Check → 测试/实现 → 异常测试 → Review → 更新 DEVELOPMENT_STATE.md。
不要暂存、提交、推送、创建远程资源或调用付费服务，除非我明确授权。
```

## 只读审查

```text
读取 D:\TransRealm\DEVELOPMENT_STATE.md。现在只做只读审查，不修改文件。
核验当前任务、Git staged/unstaged/untracked、代码、测试和文档是否一致。
按严重程度报告数据损坏、安全、兼容性、验收缺口和测试缺口；每项给路径、触发条件、影响和最小修复方向。
```

## 暂停并交接

```text
暂停继续开发，不再新增业务修改。
更新 D:\TransRealm\DEVELOPMENT_STATE.md：实际完成内容、未完成内容、命令与结果、风险、阻塞和下一步恢复动作。
不要暂存、提交、推送、覆盖或删除已有工作。
```

日常只需使用“继续开发”模板。