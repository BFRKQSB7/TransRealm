# 已废弃：项目负责人旧入口

日常开发和 Agent 交接的唯一入口是 `DEVELOPMENT_STATE.md`。

负责人职责、风险分级、任务准入、验收、例外、发布与回滚规则见 `09_Unattended_Development_Governance.md`。稳定 Task 与验收见 `07_Developer_Task_List.md`。

保留本文件只为兼容旧链接；不要将它交给 Agent 作为开发入口。