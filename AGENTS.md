# TransRealm Agent 入口

- 唯一状态入口是 `D:/TransRealm/DEVELOPMENT_STATE.md`，文件名为 `DEVELOPMENT_STATE.md`，位于项目根目录。本文件只做入口路由，不维护第二份状态。
- 提示词和 Markdown 中统一使用正斜杠路径并放在行内代码或代码块中；不得在文件名的下划线前插入反斜杠，不得把文件名拆成目录和 `_STATE.md`。
- 若输入的状态路径与上述入口不同，且用户未明确要求迁移入口，先只读核对根目录的实际文件名并报告差异；不要反复增删反斜杠猜路径，也不要创建副本、别名或第二个状态入口。
- 先读状态文件及当前任务的权威文档；用户当前指令优先。决策、Review 或仅 Markdown 授权不得自动转为开发、环境整理、验收、构建或提交。
- 项目开发输出只能写入 `D:/TransRealm/` 内，包括环境、缓存、临时文件、验证数据、截图、构建产物和历史归档；Git ignored 不等于允许写到仓库外。完整目录契约见 `09_Unattended_Development_Governance.md` §6.1。
- 现有项目外目录不因本规则获得移动或删除授权；先保留现场，再取得针对具体来源、目标和动作的授权。
